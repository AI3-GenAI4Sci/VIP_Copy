---
phase: 09-acceptance-metrics-evolution-algorithm-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - seers_harness/evolution/uplift.py
  - seers_harness/evolution/portfolio_journal.py
  - seers_harness/evolution/status_machine.py
  - seers_harness/validation/runner.py
  - tests/test_uplift.py
  - tests/test_portfolio_journal.py
  - tests/test_status_machine.py
  - tests/test_validation_runner.py
requirements:
  - D9-EVO-07
  - D9-EVO-08
  - D9-EVO-09
  - D9-EVO-10
  - D9-GATE-04
autonomous: true
must_haves:
  truths:
    - "Trial reward success is true only when trial_mean_rubric_score is greater than baseline_mean_rubric_score per D9-EVO-07/08 and D9-GATE-04."
    - "Posterior updates use alpha/beta/sample_count from binary rubric win/loss reward only per D9-EVO-09."
    - "Delta lifecycle transitions use explicit rubric win/loss posterior evidence, not token cost blocking, per D9-EVO-10."
  artifacts:
    - path: seers_harness/evolution/uplift.py
      provides: "Rubric mean score and trial reward provenance helper."
    - path: seers_harness/evolution/portfolio_journal.py
      provides: "Journal event shape carrying rubric means and fold-compatible binary success."
    - path: seers_harness/evolution/status_machine.py
      provides: "Status transition thresholds based on win/loss posterior evidence."
    - path: seers_harness/validation/runner.py
      provides: "Runner journal append migration from old uplift proxies to rubric-only reward provenance."
  key_links:
    - from: seers_harness/evolution/uplift.py
      to: seers_harness/domain/models.py
      via: "PersonalizedCopyRubricArtifact.judgments[*].total_score"
      pattern: "PersonalizedCopyRubricArtifact"
    - from: seers_harness/validation/runner.py
      to: seers_harness/evolution/uplift.py
      via: "baseline/trial rubric artifacts passed to reward helper before journal append"
      pattern: "trial_mean_rubric_score"
---

<objective>
Replace behavioral/token uplift reward with rubric-only reward provenance and keep posterior/status updates tied to rubric win/loss evidence per D9-EVO-07..10 and D9-GATE-04.

Purpose: Phase 8 wrote a real journal row, but reward provenance included token and behavioral metric lift. Phase 9 requires baseline and patched trial rubric means as the source of truth.
Output: Tested reward helper, journal evidence fields for rubric means, and status transitions that do not use token-cost gates as lifecycle blockers.
</objective>

<execution_context>
@/Users/macbook/.codex/get-shit-done/workflows/execute-plan.md
@/Users/macbook/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-01-SUMMARY.md
@seers_harness/domain/models.py
@seers_harness/evolution/uplift.py
@seers_harness/evolution/portfolio_journal.py
@seers_harness/evolution/status_machine.py
@seers_harness/validation/runner.py
@tests/test_uplift.py
@tests/test_portfolio_journal.py
@tests/test_status_machine.py
@tests/test_validation_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute reward from baseline-vs-trial rubric means</name>
  <files>seers_harness/evolution/uplift.py, tests/test_uplift.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-EVO-07..09, D9-GATE-04)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (rubric construction and old uplift tests)
    - seers_harness/domain/models.py (PersonalizedCopyRubricArtifact and judgment total_score validation)
    - seers_harness/evolution/uplift.py (current TrialUplift and compute_uplift)
    - tests/test_uplift.py (old behavioral/token reward tests to replace)
  </read_first>
  <behavior>
    - Trial with mean rubric score greater than baseline is success.
    - Trial with equal mean rubric score is failure.
    - Trial with lower mean rubric score is failure even when token cost or behavioral records improve.
    - Empty rubric judgments produce a deterministic mean of 0.0 and do not crash.
    - Token cost and behavioral metric deltas can be recorded but never influence success.
  </behavior>
  <action>
    Rewrite `seers_harness/evolution/uplift.py` so the public reward helper consumes typed `PersonalizedCopyRubricArtifact` inputs for baseline and trial.

    Keep the module small. A suitable output shape is a frozen dataclass carrying `baseline_mean_rubric_score`, `trial_mean_rubric_score`, `score_delta`, `token_cost_delta`, `behavioral_metric_lift`, and `is_positive`. The only field that determines `is_positive` is `trial_mean_rubric_score > baseline_mean_rubric_score`.

    Replace old tests in `tests/test_uplift.py` with typed rubric artifact tests. Reuse the axis-score construction pattern from `tests/test_models_rubric.py`; do not use LLM self-rated quality fields, hand-written uplift heuristics, run success booleans, token deltas, or agent intuition as reward.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_uplift.py -q</automated>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('seers_harness/evolution/uplift.py').read_text()
assert 'PersonalizedCopyRubricArtifact' in text
assert 'trial_mean_rubric_score' in text
assert 'baseline_mean_rubric_score' in text
PY</automated>
  </verify>
  <acceptance_criteria>
    Behavior assertion: `is_positive` changes only when trial mean rubric score is strictly greater than baseline mean rubric score. Source assertion: reward helper imports and uses `PersonalizedCopyRubricArtifact`.
  </acceptance_criteria>
  <done>Task is complete when typed rubric reward tests pass and old behavioral/token reward semantics no longer decide success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist rubric reward evidence and fold alpha/beta/sample_count</name>
  <files>seers_harness/evolution/portfolio_journal.py, seers_harness/validation/runner.py, tests/test_portfolio_journal.py, tests/test_validation_runner.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-02, D9-GATE-02/04)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (journal append/read/fold pattern)
    - seers_harness/evolution/portfolio_journal.py (PortfolioJournalEntry, fold_portfolio_journal)
    - seers_harness/validation/runner.py (_run_one_request trial block and journal append callsite after Plan 09-01 exploration_decision migration)
    - tests/test_portfolio_journal.py (append, missing journal, unknown delta tests)
    - tests/test_validation_runner.py (trial journal tests and artifact/rubric fake setup)
  </read_first>
  <behavior>
    - Journal entry records `baseline_mean_rubric_score`, `trial_mean_rubric_score`, and `score_delta`.
    - Runner trial path loads baseline and patched trial rubric artifacts and appends journal rows using rubric-derived success.
    - Fold increments alpha on success, beta on failure, and sample_count on every trial.
    - Fold ignores token and behavioral records when deciding alpha/beta because success is already rubric-derived.
    - Extra fields remain forbidden by Pydantic validation.
  </behavior>
  <action>
    Extend `PortfolioJournalEntry` minimally with rubric provenance fields: `baseline_mean_rubric_score: float`, `trial_mean_rubric_score: float`, and `score_delta: float`. Preserve `success: bool` as the folded binary reward input because D9-EVO-09 updates Beta counters from success/failure.

    Keep JSONL append/read/fold behavior intact: parent directory creation, UTF-8, one JSON object per line, missing journal returns original portfolio, unknown delta ids skipped. Fold must continue to call `update_after_trial(row, success=entry.success, token_cost_delta=entry.token_cost_delta)` or equivalent, but tests must show alpha/beta/sample_count derive from the rubric-derived `success` field only.

    In `seers_harness/validation/runner.py`, migrate the existing trial journal append callsite in the same plan as the journal/reward schema change per D9-EVO-07..09 and D9-GATE-04. After Plan 09-01's exploration decision chooses `selected_delta_id`, run the existing baseline and patched trial trajectories, load their `personalized_copy_rubric` artifacts, call the Plan 09-02 reward helper, and append `PortfolioJournalEntry` with `success=reward.is_positive`, `baseline_mean_rubric_score`, `trial_mean_rubric_score`, `score_delta`, record-only `token_cost_delta`, and record-only `behavioral_metric_lift` if still available.

    Update `tests/test_validation_runner.py` so the runner-level trial test constructs baseline/trial rubric artifacts and proves journal rows contain the rubric means and success follows `trial_mean_rubric_score > baseline_mean_rubric_score`. Do not leave runner tests expecting old `compute_uplift` success-count or behavioral/token reward proxy semantics.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_portfolio_journal.py tests/test_validation_runner.py -k "journal or trial or rubric" -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: portfolio journal rows carry baseline/trial rubric means and score delta. Behavior assertion: folding updates alpha, beta, and sample_count exactly from success/failure records.
  </acceptance_criteria>
  <done>Task is complete when journal tests prove rubric provenance fields are persisted and fold behavior remains deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove token-cost lifecycle blocking from status transitions</name>
  <files>seers_harness/evolution/status_machine.py, tests/test_status_machine.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-EVO-10, D9-MET-06)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md (Open Question 2 recommendation)
    - seers_harness/evolution/status_machine.py (apply_status_transitions)
    - tests/test_status_machine.py (token-cost blocking test to replace)
  </read_first>
  <behavior>
    - Promotion/rejection decisions depend on sample_count, success_count/failure_count, and lower-bound thresholds.
    - High token_cost_delta history does not block a row that otherwise satisfies rubric win/loss promotion criteria.
    - Insufficient samples still hold an experimental row.
    - Low lower-bound rows still reject after the explicit sample threshold.
  </behavior>
  <action>
    Update `apply_status_transitions` so lifecycle transitions are based on rubric win/loss posterior evidence only: sample count, success/failure count, and `wilson_lcb`.

    Remove `token_cost_p95_max` and `token_cost_deltas_by_delta` from decision logic, or leave token data only as ignored compatibility input if removing it would create unnecessary callsite churn. If retained as an ignored optional parameter, document in the function docstring that token cost is record-only and never gates status. Update tests so high token cost no longer blocks promotion when the win/loss evidence satisfies the explicit promotion rule.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_status_machine.py -q</automated>
  </verify>
  <acceptance_criteria>
    Behavior assertion: status transitions promote/reject/hold from rubric win/loss posterior evidence only. Source assertion: no token-cost threshold controls promotion.
  </acceptance_criteria>
  <done>Task is complete when status tests pass and token cost is no longer a lifecycle blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rubric artifact -> posterior update | Quality evidence crosses from model-generated rubric artifact into durable evolution state. |
| journal JSONL -> folded portfolio | Append-only evidence is replayed into posterior counters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Spoofing | `uplift.py` reward computation | mitigate | Use typed `PersonalizedCopyRubricArtifact.judgments[*].total_score`; no self-rated delta success fields. |
| T-09-02-02 | Tampering | `portfolio_journal.py` | mitigate | Keep Pydantic `extra='forbid'`; persist rubric means and binary success; fold through existing posterior update. |
| T-09-02-03 | Repudiation | reward provenance | mitigate | Journal rows carry baseline mean, trial mean, and score delta so the posterior update is auditable. |
| T-09-02-04 | Tampering | `status_machine.py` | mitigate | Remove token-cost lifecycle blocker so token/cost records cannot silently veto rubric win evidence. |
</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_uplift.py tests/test_portfolio_journal.py tests/test_status_machine.py tests/test_validation_runner.py -q`.

Run source checks that `seers_harness/evolution/uplift.py` imports `PersonalizedCopyRubricArtifact` and that status transition tests no longer expect token-cost promotion blocking.

Run `.venv/bin/python -m pytest -q` before closing this wave; the runner journal callsite migration is included here so the full suite must remain green after the reward/journal API change.
</verification>

<success_criteria>
- D9-EVO-07..10 and D9-GATE-04 are executable in tests.
- Reward success comes only from trial rubric mean greater than baseline rubric mean.
- Journal fold updates alpha/beta/sample_count from the rubric-derived success/failure result.
- Token cost remains recordable but not reward or lifecycle control.
</success_criteria>

<output>
Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-02-SUMMARY.md` when done.
</output>
