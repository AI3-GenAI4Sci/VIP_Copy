---
phase: 09-acceptance-metrics-evolution-algorithm-closure
plan: 05
type: execute
wave: 4
depends_on:
  - 09-04
files_modified:
  - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md
  - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md
  - workflow-skills/current/personalized-copy-generation/SKILL.md
  - tests/test_phase09_skill_contract.py
requirements:
  - D9-MET-04
  - D9-MET-05
  - D9-MERGE-01
  - D9-MERGE-02
  - D9-MERGE-03
  - D9-MERGE-04
  - D9-MERGE-05
  - D9-MERGE-06
  - D9-MERGE-07
  - D9-MERGE-08
  - D9-MERGE-09
  - D9-GATE-05
autonomous: true
must_haves:
  truths:
    - "Merged personalized_copy_generation remains the primary production path per D9-MERGE-01."
    - "Plan execution starts only after `09-ACCEPTANCE-EVIDENCE.md` records `Real run status: COMPLETED` for the 30-request concurrency-5 DeepSeek run per D9-GATE-01."
    - "A bounded 5-8 request evidence read assesses factor distinctness, product grounding, copy source_factor_id linkage, and staged tool use per D9-MET-05 and D9-MERGE-02..04."
    - "Any repair is lightweight SKILL wording only unless a documented diagnostic control is needed per D9-MERGE-05/06."
    - "No hard factor thresholds, enumeration prompting, JSON skeletons, internal examples, or long-term split-node rollback are introduced per D9-MERGE-08."
  artifacts:
    - path: .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md
      provides: "Bounded 5-8 request reading notes and concrete failure modes."
    - path: .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md
      provides: "Verdict on merged node stability, repair decision, and optional diagnostic-control result."
    - path: workflow-skills/current/personalized-copy-generation/SKILL.md
      provides: "Optional lightweight wording repair only if evidence shows recurring instability."
    - path: tests/test_phase09_skill_contract.py
      provides: "Guardrails for SKILL repair: plural contract and no prohibited forcing patterns."
  key_links:
    - from: .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md
      to: .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md
      via: "`Real run status: COMPLETED`, real run id, and sampled request paths"
      pattern: "Real run status: COMPLETED"
---

<objective>
Close the merged-node and bounded-quality reading slice: read 5-8 real requests from completed real-run evidence, decide whether the merged generation node is stable, and apply only evidence-driven lightweight SKILL wording repair if needed.

Purpose: Phase 9 replaces the old hard factor-count gate with bounded case reading. The merged node is judged by real chain evidence, not structure or quantity alone.
Output: Case-reading artifact, merged-node assessment artifact, and optional guarded SKILL wording repair with tests.
</objective>

<execution_context>
@/Users/macbook/.codex/get-shit-done/workflows/execute-plan.md
@/Users/macbook/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md
@workflow-skills/current/personalized-copy-generation/SKILL.md
@workflow-skills/current/personalized-copy-rubric-judge/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bounded case-reading artifact from real run evidence</name>
  <files>.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-04/05, D9-MERGE-02..04, D9-GATE-05)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md (manual-only bounded case-reading instructions)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md (real run id and artifact paths)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (Phase 7 case-analysis analog)
  </read_first>
  <action>
    Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md` and fill it from the completed real run recorded in `09-ACCEPTANCE-EVIDENCE.md`. Before selecting samples, verify that `09-ACCEPTANCE-EVIDENCE.md` contains `Real run status: COMPLETED`; if it contains `Real run status: BLOCKED` or lacks completed real-run paths, stop and report the dependency as blocked instead of creating case-reading findings from incomplete evidence.

    Select 5-8 requests. The sample must include pressure cases likely to reveal single-angle collapse, tool-skipping, weak linkage, copy-first reasoning, duplicate factors, role/label renamed as factor, and at least one trialed request if the real run produced one. Use `index.json` and `batch_summary.json` only for navigation.

    For each sampled request, record exact paths to scenario/input, `evidence/personalized_copy_generation/artifact.json`, generation `tool_calls.jsonl`, `evidence/personalized_copy_rubric/artifact.json`, and `evolution_snapshot.json`. Then record concrete observations for: whether factors stand as independent user-product insights before copy, whether each candidate links to an existing `source_factor_id`, whether copy is a transduction from the factor instead of product-only slogan text, whether factor state appears before copy state in tool evidence, and whether reflection/repair was used when uncertainty or coverage/separation gaps warranted it.

    Do not create a numeric pass threshold from factor count. Record failure modes as prose evidence with request ids and file paths.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md')
text = p.read_text(encoding='utf-8')
for needle in ['sampled request', 'source_factor_id', 'tool_calls.jsonl', 'personalized_copy_generation', 'personalized_copy_rubric', 'Failure modes']:
    assert needle in text, needle
PY</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: 5-8 sampled requests are recorded with concrete file paths and qualitative findings. Behavior assertion: the artifact treats factor count as navigation/record evidence only.
  </acceptance_criteria>
  <done>Task is complete when the case-reading artifact contains 5-8 concrete request readings tied to real evidence paths.</done>
</task>

<task type="auto">
  <name>Task 2: Decide merged-node stability and optional diagnostic control</name>
  <files>.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MERGE-01..06)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md
    - workflow-skills/current/personalized-copy-generation/SKILL.md (current merged path contract)
  </read_first>
  <action>
    Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md`.

    Summarize whether the merged `personalized_copy_generation` node is stable from the bounded case reading. The assessment must explicitly preserve the merged production path per D9-MERGE-01 and must not propose a long-term split-node rollback.

    If the case reading shows recurring instability whose root cause is unclear between merged architecture and SKILL wording, add a small diagnostic-control section. The diagnostic may compare one or two sampled requests against archived split-node reference behavior or an equivalent local control, but it must be marked diagnostic-only and must not change production DAG wiring. If the case reading is stable or the issue clearly maps to SKILL wording, record that no split-node diagnostic was needed and why.

    Include a repair decision: no repair, SKILL wording repair, or diagnostic-only finding. The decision must be grounded in concrete request ids and failure modes from `09-CASE-READING.md`. Do not plan tool implementation changes in Plan 09-05 because no concrete tool implementation files or tests are in scope.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md')
text = p.read_text(encoding='utf-8')
for needle in ['merged production path', 'repair decision', 'diagnostic-only', 'request']:
    assert needle in text, needle
assert 'long-term split-node production architecture' not in text.lower()
PY</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: the assessment records a concrete merged-node verdict and repair decision tied to case evidence. Scope assertion: any split-node control is diagnostic-only and not production architecture.
  </acceptance_criteria>
  <done>Task is complete when the merged-node assessment is explicit, evidence-linked, and does not restore split-node production architecture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply guarded lightweight SKILL repair only if assessment requires it</name>
  <files>workflow-skills/current/personalized-copy-generation/SKILL.md, tests/test_phase09_skill_contract.py, .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MERGE-06..09)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CASE-READING.md
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-MERGED-NODE-ASSESSMENT.md
    - workflow-skills/current/personalized-copy-generation/SKILL.md
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (SKILL prose anti-patterns)
  </read_first>
  <behavior>
    - If assessment says no repair, test records existing SKILL contract and production SKILL file remains unchanged.
    - If assessment says repair, edits are limited to plural/distinctness contract, multi-record tool-state awareness in SKILL prose, and red flags for single-angle collapse, duplicate factors, copy-before-factor, and source_factor_id linkage.
    - Test fails on hard numeric factor thresholds, internal examples, JSON skeletons, ellipsis templates, enumeration-taxonomy prompting, or split-node production language.
  </behavior>
  <action>
    Read `09-MERGED-NODE-ASSESSMENT.md` first. If it says no repair is required, do not edit `workflow-skills/current/personalized-copy-generation/SKILL.md`; create `tests/test_phase09_skill_contract.py` to pin that the current SKILL already contains the merged-path contract, `source_factor_id` linkage, staged factor/copy state, and no prohibited forcing patterns.

    If the assessment says repair is required, make only lightweight wording changes to `workflow-skills/current/personalized-copy-generation/SKILL.md`. The allowed repair vocabulary is: plural distinct user-product tensions/opportunities, multi-record tool-state awareness in SKILL prose, single-angle collapse red flag, duplicate-factor red flag, copy-before-factor red flag, and `source_factor_id` linkage. Do not add hard numeric factor thresholds, internal examples, JSON skeletons, ellipsis templates, enumeration/taxonomy prompting, new tool mechanisms, or production split-node rollback language.

    In both branches, create `tests/test_phase09_skill_contract.py` with source assertions over the SKILL prose. The test should use positive checks for merged path and linkage language, plus negative checks for prohibited forcing patterns. Avoid self-invalidating grep by scanning only the SKILL file.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase09_skill_contract.py -q</automated>
    <automated>python - <<'PY'
from pathlib import Path
skill = Path('workflow-skills/current/personalized-copy-generation/SKILL.md').read_text(encoding='utf-8')
for token in ['source_factor_id', 'merged generation path']:
    assert token in skill, token
for bad in ['ellipsis template', 'JSON skeleton', 'long-term split-node production architecture']:
    assert bad.lower() not in skill.lower(), bad
PY</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: SKILL prose is guarded against forcing patterns and preserves the merged production path. Artifact assertion: the assessment records whether a repair was applied and why.
  </acceptance_criteria>
  <done>Task is complete when the SKILL contract test passes and any SKILL edit is justified by case-reading evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| real run evidence -> human-readable assessment | Agent-written reading can overstate quality or hide failure modes. |
| SKILL prose -> model behavior | Prompt wording can introduce shortcuts, thresholds, or architecture rollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Spoofing | case-reading verdict | mitigate | Require request ids and exact artifact paths for every observation; no aggregate-only verdict. |
| T-09-05-02 | Tampering | `personalized-copy-generation/SKILL.md` | mitigate | Repair only from documented failure modes; test blocks hard thresholds, examples, skeletons, and split-node rollback. |
| T-09-05-03 | Repudiation | optional diagnostic control | mitigate | Assessment labels any split-node control diagnostic-only and records it separately from production architecture. |
| T-09-05-04 | Information Disclosure | real request artifacts | mitigate | Case artifact records local paths and observations; do not paste long raw DeepSeek messages or secrets. |
</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_phase09_skill_contract.py -q`.

Run `.venv/bin/python -m pytest tests/test_phase09_acceptance_gates.py tests/test_phase09_skill_contract.py -q`.

Confirm `09-CASE-READING.md` has 5-8 sampled requests and `09-MERGED-NODE-ASSESSMENT.md` has a repair decision tied to evidence.
</verification>

<success_criteria>
- Bounded case reading replaces hard factor-count acceptance.
- Merged node remains the production path.
- Any repair is lightweight SKILL wording and evidence-driven.
- Optional split-node control remains diagnostic only.
- Prohibited forcing patterns are tested out of the SKILL surface.
</success_criteria>

<output>
Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-05-SUMMARY.md` when done.
</output>
