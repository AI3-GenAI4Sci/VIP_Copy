---
phase: 09-acceptance-metrics-evolution-algorithm-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seers_harness/evolution/delta_portfolio.py
  - seers_harness/validation/evolution_snapshot.py
  - seers_harness/validation/runner.py
  - tests/test_delta_portfolio.py
  - tests/test_validation_runner.py
requirements:
  - D9-EVO-01
  - D9-EVO-02
  - D9-EVO-03
  - D9-EVO-04
  - D9-EVO-05
  - D9-EVO-06
  - D9-GATE-02
  - D9-GATE-03
autonomous: true
must_haves:
  truths:
    - "Every request can produce an explicit exploration_decision per D9-EVO-03."
    - "No selection API or snapshot evidence contains token_budget_pressure, production_pressure, trial_prob, static probability skip, random skip, hardcoded forcing, or artificial priors per D9-EVO-01..05 and D9-GATE-03."
    - "Trial selection uses Thompson sampling over eligible experimental deltas per D9-EVO-06."
  artifacts:
    - path: seers_harness/evolution/delta_portfolio.py
      provides: "ExplorationDecision contract, allowed no_trial_reason values, information-value trigger, and Thompson delta selection."
    - path: seers_harness/validation/evolution_snapshot.py
      provides: "Snapshot reducer branch for exploration_decision evidence."
    - path: seers_harness/validation/runner.py
      provides: "Runner callsite migration from trial_gate/select_trial_delta pressure inputs to exploration_decision events."
    - path: tests/test_delta_portfolio.py
      provides: "Unit coverage for allowed decisions, forbidden fields, information-value trigger, and Thompson selection."
  key_links:
    - from: seers_harness/evolution/delta_portfolio.py
      to: seers_harness/validation/evolution_snapshot.py
      via: "plain dict event with type='exploration_decision'"
      pattern: "exploration_decision"
    - from: seers_harness/validation/runner.py
      to: seers_harness/evolution/delta_portfolio.py
      via: "calls the new exploration decision API without pressure/probability arguments"
      pattern: "exploration_decision"
---

<objective>
Replace the pressure/probability trial gate with a small explicit exploration-decision contract per D9-EVO-01..06 and D9-GATE-02/03.

Purpose: Phase 8 observed real trial wiring, but most requests were suppressed by pressure/probability fields that Phase 9 explicitly rejects.
Output: A deterministic, tested selection surface that records why a request did or did not trial, and a snapshot reducer that exposes that decision without laundering old shortcut fields.
</objective>

<execution_context>
@/Users/macbook/.codex/get-shit-done/workflows/execute-plan.md
@/Users/macbook/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md
@.planning/phases/06-evolution-chain-production-hardening/06-CONTEXT.md
@seers_harness/evolution/delta_portfolio.py
@seers_harness/validation/evolution_snapshot.py
@seers_harness/validation/runner.py
@tests/test_delta_portfolio.py
@tests/test_validation_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exploration decisions and selection tests</name>
  <files>seers_harness/evolution/delta_portfolio.py, tests/test_delta_portfolio.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-EVO-01..06, D9-GATE-02/03)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (selection and test helper patterns)
    - seers_harness/evolution/delta_portfolio.py (DeltaPortfolioRow, select_trial_delta, update_after_trial)
    - tests/test_delta_portfolio.py (_make_row helper and current selector tests)
  </read_first>
  <behavior>
    - Test no eligible delta: decision has should_trial false, selected_delta_id None, no_trial_reason "no_eligible_delta".
    - Test evidence-sufficient no-trial: experimental rows above the explicit minimum-sample/evidence rule do not trial and record "all_eligible_deltas_evidence_sufficient".
    - Test information-value trigger: low-sample or near-boundary eligible experimental rows produce should_trial true.
    - Test Thompson sampling: injected deterministic random source selects the row with the highest sampled posterior among eligible experimental rows.
    - Test anti-cheat: public selection signatures and decision payloads contain none of token_budget_pressure, production_pressure, trial_prob, random skip, static probability miss, or manual prior fields.
  </behavior>
  <action>
    In `seers_harness/evolution/delta_portfolio.py`, replace the old `select_trial_delta(... recent_failure_rate, token_budget_pressure, production_pressure ...) -> Optional[str]` contract with an explicit decision result such as `ExplorationDecision`.

    The decision result must expose these durable fields: `should_trial: bool`, `selected_delta_id: str | None`, `eligible_delta_count: int`, `trigger_reason: str | None`, `no_trial_reason: str | None`, and `posterior_samples: dict[str, float]` or an equivalent record that lets tests prove Thompson selection without adding a dependency.

    Allowed no-trial reasons are exactly structural or evidence-based per D9-EVO-04: no eligible delta, all eligible deltas evidence-sufficient, all eligible deltas non-experimental, target unresolvable, provider/auth/schema blocker. Do not include token pressure, concurrency pressure, random skip, static probability miss, hardcoded trial suppression, or artificial/manual prior reasons per D9-EVO-05.

    Implement the information-value trigger using planner-chosen explicit constants local to the module, grounded only in posterior evidence. Use a small transparent rule: insufficient sample count, posterior mean near a decision boundary, or insufficient lower-bound confidence can trigger exploration. Keep threshold names readable and tests pinned to behavior, not to hidden magic.

    Implement Thompson selection with `rng.betavariate(row.belief_alpha, row.belief_beta)` over eligible rows whose `status == "experimental"` and whose target/applicable surface matches the request surface. Preserve the existing pure-transform style and `random.Random | None` injection pattern.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_delta_portfolio.py -q</automated>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('seers_harness/evolution/delta_portfolio.py').read_text()
for token in ['token_budget_pressure', 'production_pressure', 'trial_prob']:
    assert token not in text, token
PY</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `select_trial_delta` no longer accepts token, concurrency, pressure, or static probability inputs. Behavior assertion: tests prove explicit decisions, allowed no-trial reasons, information-value triggering, and Thompson selection.
  </acceptance_criteria>
  <done>Task is complete when `tests/test_delta_portfolio.py` passes and production selection source has no old pressure/probability tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit exploration_decision snapshots without old gate fields</name>
  <files>seers_harness/validation/evolution_snapshot.py, seers_harness/validation/runner.py, tests/test_delta_portfolio.py, tests/test_validation_runner.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-GATE-02 mechanism evidence)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (event reducer pattern)
    - seers_harness/validation/evolution_snapshot.py (current trial_gate reducer branch)
    - seers_harness/validation/runner.py (_trial_gate_event, select_trial_delta callsite, and _run_one_request trial/no-trial branch)
    - tests/test_delta_portfolio.py (decision payload tests from Task 1)
    - tests/test_validation_runner.py (runner snapshot, no-trial, trial, and journal tests)
  </read_first>
  <behavior>
    - Snapshot reducer accepts an event with type "exploration_decision" and writes top-level `exploration_decision`.
    - Snapshot evidence includes selected delta when trialing and no_trial_reason when not trialing.
    - Runner appends one exploration_decision event for every successful request before any trial/no-trial branch returns.
    - Runner source no longer computes or passes token pressure, production/concurrency pressure, recent-failure probability modifiers, static trial probability, or random skip into selection.
    - Snapshot output contains no `trial_gate`, `token_budget_pressure`, `production_pressure`, or `trial_prob` keys.
  </behavior>
  <action>
    In `seers_harness/validation/evolution_snapshot.py`, replace the `trial_gate` reducer branch and top-level output key with `exploration_decision`.

    Preserve the existing reducer style: tolerate malformed events, ignore unknown event types, redact exception messages, create parent directories, write UTF-8 JSON with indent and trailing newline. Do not move business-rule validation into the snapshot writer; it should serialize the decision event produced by the evolution module.

    In `seers_harness/validation/runner.py`, migrate the `_run_one_request` selection callsite in the same wave as the selector contract change per D9-EVO-01..06 and D9-GATE-02/03. Delete `_trial_gate_event` and replace it with a small exploration-decision event adapter fed by the Plan 09-01 decision object. Pass only request-relevant structure into selection: current portfolio, active applicable surfaces, target skill, structural blocker state if applicable, and injected RNG. Do not compute or pass token pressure, production/concurrency pressure, recent failure rate as a probability modifier, static trial probability, or random skip.

    When `decision.should_trial` is false, append the exploration_decision event and continue without trial workspace or journal writes. When true, use `decision.selected_delta_id` for the existing trial workspace path. Preserve the current baseline/trial workspace isolation and keep the existing reward/journal implementation unchanged until Plan 09-02 migrates rubric reward provenance.

    Add or adjust tests so a plain `exploration_decision` event becomes stable artifact evidence containing `should_trial`, `selected_delta_id`, `eligible_delta_count`, `trigger_reason`, and `no_trial_reason`, with absent old gate fields.

    Update `tests/test_validation_runner.py` in this task because it is the runner callsite test surface. Replace old `trial_gate` snapshot assertions with `exploration_decision` assertions, prove no-trial requests record an allowed `no_trial_reason`, prove trial requests set `record["trial_selected_delta_id"]` from the selected decision delta, and remove tests that expect pressure/probability suppression.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_delta_portfolio.py tests/test_validation_runner.py -k "snapshot or exploration or delta_portfolio" -q</automated>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('seers_harness/validation/evolution_snapshot.py').read_text()
assert 'exploration_decision' in text
for token in ['trial_gate', 'token_budget_pressure', 'production_pressure', 'trial_prob']:
    assert token not in text, token
runner = Path('seers_harness/validation/runner.py').read_text()
assert 'exploration_decision' in runner
for token in ['token_budget_pressure', 'production_pressure', 'trial_prob']:
    assert token not in runner, token
PY</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: new snapshots expose `exploration_decision` as the mechanism evidence field. Source assertion: the snapshot reducer no longer emits the old pressure/probability gate.
  </acceptance_criteria>
  <done>Task is complete when snapshot tests pass and the reducer serializes exploration_decision without old trial_gate fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner event -> evidence JSON | Production evidence can be read as acceptance proof; shortcut fields would launder invalid mechanisms. |
| source selector -> trial workspace | A selected delta controls which trial workspace is patched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | `delta_portfolio.select_trial_delta` | mitigate | Delete token/concurrency/probability inputs; tests assert forbidden fields are absent from source and decision payloads. |
| T-09-01-02 | Spoofing | `evolution_snapshot.json` | mitigate | Emit `exploration_decision` with selected delta or allowed no_trial_reason; no `trial_gate` compatibility alias. |
| T-09-01-03 | Tampering | trial patch workspace selection | mitigate | Select only eligible `experimental` deltas by target/applicable surface and record selected delta id in mechanism evidence. |
| T-09-01-04 | Information Disclosure | evidence JSON | accept | This plan writes only delta ids and decision metadata, not DeepSeek env values or request secrets. |
</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_delta_portfolio.py tests/test_validation_runner.py -k "snapshot or exploration or delta_portfolio" -q`.

Run a source anti-cheat check over production files only: tokens `token_budget_pressure`, `production_pressure`, and `trial_prob` must be absent from `seers_harness/evolution/delta_portfolio.py`, `seers_harness/validation/evolution_snapshot.py`, and `seers_harness/validation/runner.py`.

Run `.venv/bin/python -m pytest -q` before closing Wave 1; the runner callsite migration is included in this plan so the full suite must remain green after the selector/snapshot API change.
</verification>

<success_criteria>
- D9-EVO-01..06 and D9-GATE-02/03 are represented by executable tests and production selection/snapshot code.
- Every selection result is explainable as trial or allowed no-trial.
- Old pressure/probability gate fields are removed from this plan's production surfaces.
</success_criteria>

<output>
Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-01-SUMMARY.md` when done.
</output>
