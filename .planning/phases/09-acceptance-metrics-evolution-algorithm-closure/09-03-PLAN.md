---
phase: 09-acceptance-metrics-evolution-algorithm-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - seers_harness/validation/runner.py
  - seers_harness/validation/batch_summary_writer.py
  - seers_harness/validation/machine_judges.py
  - tests/test_validation_runner.py
  - tests/test_08_07_behavioral_metrics.py
  - tests/test_batch_summary_writer.py
requirements:
  - D9-EVO-03
  - D9-EVO-07
  - D9-EVO-08
  - D9-EVO-09
  - D9-MET-02
  - D9-MET-03
  - D9-MET-06
  - D9-MET-07
  - D9-GATE-01
  - D9-GATE-02
  - D9-GATE-04
autonomous: true
must_haves:
  truths:
    - "Runner records exploration_decision for every request per D9-EVO-03 and D9-GATE-02."
    - "A trial path runs baseline full generation plus rubric and patched full generation plus rubric, then appends a rubric-derived journal row per D9-EVO-07..09."
    - "Journal rows fold before batch_summary.json reads M5, so folded posterior sample_count/alpha/beta reaches behavioral_metrics.trial_belief_update_count per D9-MET-02."
    - "Stage 3 can be run as 30 requests at concurrency 5 without using concurrency as exploration input per D9-MET-07 and D9-EVO-02."
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "Runner fold-before-summary ordering and Phase 9 run shape, preserving exploration decisions and rubric reward provenance from 09-01/09-02."
    - path: seers_harness/validation/batch_summary_writer.py
      provides: "Summary writer path that can read folded posterior state."
    - path: seers_harness/validation/machine_judges.py
      provides: "Behavioral report M5 reading from folded portfolio while keeping factor/cache/token as records."
  key_links:
    - from: seers_harness/validation/runner.py
      to: seers_harness/evolution/uplift.py
      via: "baseline/trial rubric artifacts passed to reward helper"
      pattern: "trial_mean_rubric_score"
    - from: seers_harness/validation/runner.py
      to: seers_harness/validation/batch_summary_writer.py
      via: "folded portfolio before write_batch_summary"
      pattern: "final_portfolio"
---

<objective>
Wire the remaining Phase 09 runner and summary behavior after Plans 09-01/09-02 have migrated the breaking runner callsites: M5 reads folded posterior state and the real validation run shape is 30 requests at concurrency 5.

Purpose: Plans 09-01 and 09-02 make exploration and rubric reward reachable in the runner without breaking earlier waves; this plan closes the M5 folded-summary bug and exposes the Phase 9 run configuration.
Output: Runner/summary code and integration tests proving fold-before-summary M5 and Phase 9 run configuration while preserving the exploration/reward evidence introduced earlier.
</objective>

<execution_context>
@/Users/macbook/.codex/get-shit-done/workflows/execute-plan.md
@/Users/macbook/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-02-SUMMARY.md
@seers_harness/validation/runner.py
@seers_harness/validation/batch_summary_writer.py
@seers_harness/validation/machine_judges.py
@tests/test_validation_runner.py
@tests/test_08_07_behavioral_metrics.py
@tests/test_batch_summary_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fold journal before batch_summary M5 reads posterior state</name>
  <files>seers_harness/validation/runner.py, seers_harness/validation/batch_summary_writer.py, seers_harness/validation/machine_judges.py, tests/test_validation_runner.py, tests/test_08_07_behavioral_metrics.py, tests/test_batch_summary_writer.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-02, D9-GATE-02)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md (M5 fold/write-order finding)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md (real journal row with M5 summary 0)
    - seers_harness/validation/runner.py (_run_stage ordering around write_index, write_batch_summary, fold_portfolio_journal)
    - seers_harness/validation/batch_summary_writer.py (write_batch_summary)
    - seers_harness/validation/machine_judges.py (build_behavioral_report and compute_belief_update_count)
    - tests/test_validation_runner.py (stage fold test)
    - tests/test_08_07_behavioral_metrics.py (M5 behavioral report tests)
    - tests/test_batch_summary_writer.py (summary helper)
  </read_first>
  <behavior>
    - A stage with a portfolio journal row folds the row into `delta_portfolio` before `write_batch_summary` builds behavioral metrics.
    - `batch_summary.json.behavioral_metrics.trial_belief_update_count` is greater than zero when folded portfolio has any row with sample_count greater than zero.
    - `portfolio_journal.jsonl` remains raw event evidence; summary reads folded posterior/status evidence, not raw event count.
    - Factor count, cache miss, and token values remain visible records but are not converted into pass/fail acceptance gates.
  </behavior>
  <action>
    Fix the stage ordering bug by folding `portfolio_journal.jsonl` into the in-memory `delta_portfolio` before calling `write_batch_summary`, then applying status transitions before summary reads status if status evidence is included.

    Choose the smallest stable interface for summary M5: either pass `final_portfolio=delta_portfolio` into `write_batch_summary` and through to `build_behavioral_report`, or write a stage-level folded portfolio artifact before summary and make `build_behavioral_report` read it. The chosen path must keep writer-layer separation: `batch_summary_writer.py` may import `machine_judges`, but must not import capture-layer modules.

    Update tests to reproduce the Phase 8 failure: raw journal row exists, fold occurs, and summary M5 reports the folded posterior update instead of 0. Preserve existing behavioral metrics keys, but ensure tests describe factor count/cache/token as records only, not hard gates.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_validation_runner.py tests/test_08_07_behavioral_metrics.py tests/test_batch_summary_writer.py -q</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: `batch_summary.json.behavioral_metrics.trial_belief_update_count` reads folded posterior evidence. Source assertion: summary code does not infer M5 from raw journal row count alone.
  </acceptance_criteria>
  <done>Task is complete when summary-order tests pass and M5 sees folded alpha/beta/sample_count state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose Phase 09 real run shape without feeding concurrency into exploration</name>
  <files>seers_harness/validation/runner.py, tests/test_validation_runner.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-07, D9-EVO-02)
    - seers_harness/validation/runner.py (CLI args, _STAGE_CONFIG, _run_stage)
    - tests/test_validation_runner.py (CLI and stage config tests)
  </read_first>
  <behavior>
    - Real acceptance can run stage 3 with 30 requests and concurrency 5 from CLI or stage configuration.
    - Concurrency controls execution fan-out only and is not passed to exploration decision logic.
    - Existing stage defaults remain understandable for prior phase evidence; Phase 09 command is explicit.
  </behavior>
  <action>
    Add the smallest runner interface needed for Phase 09 validation: either an explicit `--concurrency 5` CLI option that overrides the stage concurrency for the invocation, or a Phase 09-specific documented command path that runs Stage 3 with N=30 and concurrency=5. Prefer a CLI override if it fits existing argument parsing because it avoids reinterpreting historical Phase 7/8 stage defaults.

    Tests must prove that the command path drives `_run_stage` with `num_requests=30` and `concurrency=5`, and that no concurrency value is passed into the exploration decision or no-trial reason logic. Do not use concurrency as an exploration input per D9-EVO-02.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_validation_runner.py -k "concurrency or num_requests or exploration" -q</automated>
  </verify>
  <acceptance_criteria>
    Command-output assertion: tests prove the Phase 09 run shape is 30 requests at concurrency 5. Source assertion: concurrency remains an execution setting only.
  </acceptance_criteria>
  <done>Task is complete when the runner can execute the Phase 09 Stage 3 shape without passing concurrency into selection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DeepSeek env/logs -> evidence | Real run may include provider errors and env-file markers; secrets must not leak. |
| trial workspace -> live skill root | Patched trials must not mutate production skill files. |
| journal -> summary | Raw event evidence becomes acceptance-facing folded posterior evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | `runner.py` real DeepSeek execution | mitigate | Preserve env-file safe logging and safe exception redaction; do not print resolved keys. |
| T-09-03-02 | Tampering | trial workspace | mitigate | Reuse isolated baseline/patched workspace pattern; selected delta id recorded in exploration decision and journal. |
| T-09-03-03 | Spoofing | reward journal | mitigate | Journal success comes from typed rubric means loaded from baseline and trial artifacts only. |
| T-09-03-04 | Tampering | `batch_summary.json` M5 | mitigate | Fold journal before summary reads posterior; tests assert M5 from folded sample_count. |
</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_validation_runner.py tests/test_08_07_behavioral_metrics.py tests/test_batch_summary_writer.py -q`.

Run `.venv/bin/python -m pytest -q` after this wave because runner, summary, and evolution behavior are now connected.
</verification>

<success_criteria>
- Every request has exploration_decision evidence.
- Trial reward journal rows use baseline/trial rubric mean score comparison.
- Folded posterior reaches batch summary M5.
- Real Stage 3 acceptance can be launched for 30 requests at concurrency 5 without exploration using concurrency as an input.
</success_criteria>

<output>
Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-03-SUMMARY.md` when done.
</output>
