---
phase: 09-acceptance-metrics-evolution-algorithm-closure
plan: 04
type: execute
wave: 3
depends_on:
  - 09-03
files_modified:
  - tests/test_phase09_acceptance_gates.py
  - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md
requirements:
  - D9-MET-01
  - D9-MET-03
  - D9-MET-04
  - D9-MET-06
  - D9-MET-07
  - D9-GATE-01
  - D9-GATE-02
  - D9-GATE-03
  - D9-GATE-04
  - D9-GATE-05
autonomous: true
user_setup:
  - service: deepseek
    why: "Phase 09 acceptance requires a real DeepSeek 30-request concurrency-5 run."
    env_vars:
      - name: DEEPSEEK_API_KEY
        source: ".env.local or operator environment; never print the resolved value."
    dashboard_config:
      - task: "Ensure DeepSeek account balance/quota is sufficient for one Stage 3 run."
        location: "DeepSeek account dashboard"
must_haves:
  truths:
    - "Acceptance is categorized by mechanism evidence and bounded reading, not old fixed factor/trial/cache/token thresholds per D9-MET-01/03/04/06."
    - "A real DeepSeek 30-request concurrency-5 run is required for acceptance per D9-MET-07 and D9-GATE-01."
    - "Provider/auth/quota/balance failure blocks Phase 09 acceptance and cannot complete the real-run task per D9-GATE-01."
    - "Anti-cheat tests prove forbidden shortcut fields and reward proxies are absent from production source per D9-GATE-02..04."
  artifacts:
    - path: tests/test_phase09_acceptance_gates.py
      provides: "Source-level anti-cheat checks for forbidden exploration and reward shortcuts."
    - path: .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md
      provides: "Run-evidence ledger for pytest, real run command, mechanism evidence, and record-only metrics."
  key_links:
    - from: tests/test_phase09_acceptance_gates.py
      to: seers_harness/
      via: "production-source scan excluding tests and planning docs"
      pattern: "exploration_decision"
---

<objective>
Replace the old acceptance shape with executable anti-cheat gates and a real-run evidence ledger per D9-MET-01/03/04/06/07 and D9-GATE-01..05.

Purpose: Phase 8 failed old numeric acceptance metrics. Phase 9 must prove the new mechanism, record-only metrics, and real-evidence requirements without restoring those old gates.
Output: Automated anti-cheat test coverage and a Phase 09 acceptance evidence artifact that records local pytest, the real 30-request concurrency-5 run, mechanism evidence, and record-only metrics.
</objective>

<execution_context>
@/Users/macbook/.codex/get-shit-done/workflows/execute-plan.md
@/Users/macbook/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-RESEARCH.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md
@.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-03-SUMMARY.md
@seers_harness/evolution/delta_portfolio.py
@seers_harness/evolution/uplift.py
@seers_harness/validation/runner.py
@seers_harness/validation/evolution_snapshot.py
@seers_harness/validation/batch_summary_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add production-source anti-cheat gates</name>
  <files>tests/test_phase09_acceptance_gates.py</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-GATE-01..05)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md (Wave 0 anti-cheat targets)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-PATTERNS.md (test anti-cheat patterns)
    - seers_harness/evolution/delta_portfolio.py
    - seers_harness/evolution/uplift.py
    - seers_harness/validation/runner.py
    - seers_harness/validation/evolution_snapshot.py
  </read_first>
  <behavior>
    - Test scans production source under `seers_harness/evolution` and `seers_harness/validation`, excluding tests and planning docs.
    - Test fails if old exploration shortcuts appear as live identifiers: token pressure, production pressure, old trial probability field, static probability skip, random skip, hardcoded trial forcing, or artificial/manual prior.
    - Test fails if reward success can be computed without baseline and trial rubric artifacts.
    - Test requires `exploration_decision`, selected delta evidence, journal evidence, and folded posterior evidence identifiers to exist in production source.
  </behavior>
  <action>
    Create `tests/test_phase09_acceptance_gates.py` with source-level checks over production files only. Avoid self-invalidating grep gates by constructing forbidden tokens from fragments or by scanning only target source files, not the test file.

    Gate 1: forbidden exploration shortcuts are absent from `seers_harness/evolution/delta_portfolio.py`, `seers_harness/validation/runner.py`, and `seers_harness/validation/evolution_snapshot.py`.

    Gate 2: reward provenance requires rubric artifacts by checking `seers_harness/evolution/uplift.py` for `PersonalizedCopyRubricArtifact`, `baseline_mean_rubric_score`, and `trial_mean_rubric_score`, and by checking absence of self-rated reward names such as confidence, probability, quality_score, or agent_judgment in reward code.

    Gate 3: mechanism evidence identifiers exist: `exploration_decision`, selected delta id, trial workspace, portfolio journal append, fold_portfolio_journal, and trial_belief_update_count.

    Gate 4: acceptance code does not define pass/fail gates for factor count, cache miss, token use, or fixed trial count. It may report those fields as records.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase09_acceptance_gates.py -q</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: anti-cheat test fails if the old pressure/probability/reward shortcuts are reintroduced into production code. Behavior assertion: the test passes on the Phase 09 implementation.
  </acceptance_criteria>
  <done>Task is complete when the anti-cheat test passes and does not count its own prose as a violation.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 09 acceptance evidence ledger and run local gates</name>
  <files>.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-01/03/04/06/07, D9-GATE-01..05)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md (manual-only verification instructions)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md (old gaps and why they are reclassified)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-03-SUMMARY.md (runner command shape)
    - tests/test_phase09_acceptance_gates.py
  </read_first>
  <action>
    Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md` as the execution ledger for Phase 09 acceptance.

    The ledger must include sections for: local pytest result, anti-cheat test result, real DeepSeek command, real run status, real run id, index path, batch_summary path, portfolio_journal path, sampled request paths, mechanism evidence checklist, record-only metrics table, and blockers if the real provider cannot run.

    The mechanism checklist must require: `exploration_decision` for every request, selected delta when trialing, trial workspace path, portfolio journal row, folded posterior sample_count/alpha/beta/status evidence, and batch_summary M5 reading folded state. The record-only metrics table must include factor_count_p50, cache miss, and token use as observations only, with no pass/fail threshold language.

    Run local verification first: targeted Phase 09 pytest and then full `.venv/bin/python -m pytest -q`. Record the command outputs in summarized form in the ledger; do not paste secrets or long logs.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_phase09_acceptance_gates.py tests/test_delta_portfolio.py tests/test_uplift.py tests/test_portfolio_journal.py tests/test_validation_runner.py tests/test_08_07_behavioral_metrics.py tests/test_batch_summary_writer.py -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
    <automated>test -f .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    Artifact assertion: the evidence ledger exists and distinguishes local preconditions from real acceptance evidence. Command-output assertion: targeted and full pytest results are recorded.
  </acceptance_criteria>
  <done>Task is complete when local gates are green and the evidence ledger is populated through the local-verification sections.</done>
</task>

<task type="auto">
  <name>Task 3: Run real DeepSeek Stage 3 acceptance shape and record completed evidence</name>
  <files>.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-CONTEXT.md (D9-MET-07 and D9-GATE-01)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-VALIDATION.md (manual-only real run instructions)
    - .planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md
    - seers_harness/validation/runner.py (actual CLI flags from Plan 09-03)
  </read_first>
  <action>
    Launch the real DeepSeek acceptance run using the actual runner CLI from Plan 09-03. The command must run Stage 3 with 30 requests and concurrency 5, loading `.env.local` through the runner's env-file support if present. The expected command shape is `.venv/bin/python -u -m seers_harness.validation.runner --env-file .env.local --stage 3 --num-requests 30 --concurrency 5`; adjust only if Plan 09-03 chose a different exact CLI name, and record the exact command used.

    Do not print or copy the resolved DeepSeek API key into any artifact. If the provider returns auth/quota/balance failure, classify it as `Real run status: BLOCKED` in the ledger with failure_class and safe error text, then stop execution and escalate as blocked. Provider/auth/quota/balance failure is not task completion, not plan completion, and not Phase 09 acceptance evidence per D9-GATE-01.

    On a completed run only, update the ledger with `Real run status: COMPLETED`, run id, index path, batch summary path, journal path, by_failure_class, count of requests with exploration_decision, count of trialed requests explained by decision evidence, folded M5 value, and the record-only factor/cache/token observations. Do not compare trial count to a fixed old target; explain observed trial count through eligible deltas, posterior evidence, and allowed no_trial_reason distribution.
  </action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
p = Path('.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-ACCEPTANCE-EVIDENCE.md')
text = p.read_text(encoding='utf-8')
for needle in ['Real run status: COMPLETED', 'Real run id', 'index.json', 'batch_summary.json', 'portfolio_journal.jsonl', 'exploration_decision', 'record-only']:
    assert needle in text, needle
assert 'Real run status: BLOCKED' not in text
PY</automated>
  </verify>
  <acceptance_criteria>
    Artifact evidence: the ledger records a real completed 30-request concurrency-5 run with mechanism evidence. Provider/auth/quota/balance failure must be recorded as `Real run status: BLOCKED` and escalated without satisfying this task.
  </acceptance_criteria>
  <done>Task is complete only when the real-run section is populated with `Real run status: COMPLETED`, concrete run paths, and mechanism evidence from the 30-request concurrency-5 DeepSeek run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local` -> runner | Real DeepSeek secrets enter process environment. |
| source code -> acceptance evidence | Source can accidentally reintroduce forbidden shortcut gates. |
| real run artifacts -> phase ledger | Local run evidence is summarized into an in-tree artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | DeepSeek env/logs/evidence | mitigate | Use env-file support; never record resolved key values; ledger stores only safe run paths and safe error text. |
| T-09-04-02 | Tampering | anti-cheat source gates | mitigate | Production-source tests fail on forbidden pressure/probability/reward shortcuts and old acceptance gates. |
| T-09-04-03 | Repudiation | `09-ACCEPTANCE-EVIDENCE.md` | mitigate | Ledger records exact commands, run ids, artifact paths, and blocker status. |
| T-09-04-04 | Spoofing | acceptance claim | mitigate | Pytest is recorded as precondition only; real 30-request concurrency-5 evidence is required. |
</threat_model>

<verification>
Run `.venv/bin/python -m pytest tests/test_phase09_acceptance_gates.py -q`.

Run `.venv/bin/python -m pytest -q`.

Long phase acceptance gate, separate from quick automated task verification: run `.venv/bin/python -u -m seers_harness.validation.runner --env-file .env.local --stage 3 --num-requests 30 --concurrency 5`, then verify `09-ACCEPTANCE-EVIDENCE.md` records `Real run status: COMPLETED` and concrete run paths. If the provider returns auth/quota/balance failure, mark the plan blocked/escalated; do not complete the task or phase.
</verification>

<success_criteria>
- Anti-cheat tests prove forbidden shortcut gates are absent.
- Evidence ledger exists and separates local preconditions from real acceptance.
- Real run evidence is recorded for 30 requests at concurrency 5; an external provider blocker is documented only as a blocked/escalation outcome and does not complete the plan.
- Factor count, cache miss, and token use are recorded only.
</success_criteria>

<output>
Create `.planning/phases/09-acceptance-metrics-evolution-algorithm-closure/09-04-SUMMARY.md` when done.
</output>
