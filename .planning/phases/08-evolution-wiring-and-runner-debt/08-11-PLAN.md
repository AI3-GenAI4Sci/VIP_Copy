---
phase: 08-evolution-wiring-and-runner-debt
plan: 11
type: execute
wave: 6
depends_on:
  - 08-10
files_modified:
  - seers_harness/evolution/trial_runner.py
  - tests/test_trial_runner_smoke.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-G-WR-05:trial_runner 的 except Exception 收紧到 (TrialFailure, AssertionError, SchemaError),re-raise (ProviderAuthError, ProviderRateLimitError, ProviderTransientError),让 trial 中的 provider 错误走 runner D-19 fail-fast 而非默默 swallow 成 trial_failed。贡献 D8-ACC-1(trial 中 provider 错误不被掩盖)+ D8-ACC-4(re-raise / catch-narrow pytest)。"
  truths:
    - "ProviderAuthError / ProviderRateLimitError / ProviderTransientError 在 run_request_trial 中直接 re-raise,不写 trial_failed event,让 D-19 路由到 provider_error fail-fast"
    - "TrialFailure / AssertionError / SchemaError(== pydantic.ValidationError)仍 catch,写 trial_failed event(D-19 trial_failure 路径不变)"
    - "其他未列入的 Exception(KeyError、TypeError、IndexError 等 runner bug)上浮,让 D-19 走 infra_error fail-fast"
    - "WR-05 在 F 接线之后落地(plan 08-05 → 08-11 wave 顺序),narrow 后 F 接线的真实 bug 会浮现而不被掩盖 — 反而增强可观测性"
  artifacts:
    - path: seers_harness/evolution/trial_runner.py
      provides: "run_request_trial 异常路径 narrow 到 3 + 3 模式:re-raise 3 个 ProviderError,catch 3 个 TrialFailure/AssertionError/SchemaError"
      contains: "except (ProviderAuthError, ProviderRateLimitError, ProviderTransientError)"
  key_links:
    - from: run_request_trial except branch
      to: D-19 routing
      via: "ProviderError re-raise → runner D-19 provider_error fail-fast"
      pattern: "except \\(ProviderAuthError, ProviderRateLimitError, ProviderTransientError\\)"
    - from: run_request_trial trial_failed event
      to: TrialFailure / AssertionError / SchemaError
      via: "narrow catch tuple"
      pattern: "except \\(TrialFailure, AssertionError, SchemaError\\)"
  forbid_list:
    - "禁止 把 ProviderError 类 catch 后转 trial_failed —— D-19 锁定 provider_error 路径,trial 不能 swallow 它"
    - "禁止 在 narrow tuple 中加 except Exception fallback —— D-19 让其他异常走 infra_error,WR-05 不为它兜底"
    - "禁止 把 SchemaError 直接 import pydantic.ValidationError;用 module-local alias `SchemaError = ValidationError` 让 charter 文本与 code 对齐(08-RESEARCH §2-E + Q1)"
    - "禁止 改 TrialOutcome 数据 shape —— failure_category 字段值仍取 type(exc).__name__,events 仍 emit trial_failed (有效路径不变)"
---

<objective>
落地 D8-G-WR-05:`seers_harness/evolution/trial_runner.py` 的 `except Exception` 收紧到 narrow tuple。Provider errors re-raise 让 runner D-19 走 provider_error fail-fast;TrialFailure / AssertionError / SchemaError 仍 catch 走 trial_failure 路径(D-19);其他 Exception 上浮走 infra_error。

Purpose: D8-G-WR-05 in `07-WRIN-TRIAGE.md` deferred 项;F 接线(plan 08-05)后 trial_runner 才被 runtime 真正调用,在此之前 narrow 无可观测后果。本 plan 在 F + IN-01 + M1-M5 + B + WR-01/02 之后,补上最后一块异常路由防护,让 trial 中的 provider auth/rate/transient 错误正确 fail-fast。
Output: 单一 atomic commit;trial_runner.py except 子句 narrow + 新 import;tests/test_trial_runner_smoke.py 新增 2 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/evolution/trial_runner.py
@seers_harness/core/errors.py
@seers_harness/validation/exception_classifier.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: trial_runner except Exception narrow 到 3+3 模式 + 2 个单测</name>
  <files>seers_harness/evolution/trial_runner.py, tests/test_trial_runner_smoke.py</files>
  <read_first>
    - seers_harness/evolution/trial_runner.py(line 226-242 当前 `except Exception as exc` 块,events trial_failed 写入)
    - seers_harness/core/errors.py(ProviderAuthError / ProviderRateLimitError / ProviderTransientError / ProviderResponseError 完整继承图,确认 ProviderTransientError 与 ProviderResponseError 是否平级)
    - seers_harness/validation/exception_classifier.py(TrialFailure sentinel 类的完整名)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-WR-05 完整伪代码 + §7.3 顺序复核 — "F 之后立即 WR-05 是 *增强* F 可观测性")
    - tests/test_trial_runner_smoke.py(fault injection fixture 范式)
  </read_first>
  <behavior>
    - Test 1 `test_trial_runner_reraises_provider_errors`(parametrize 3 个 ProviderError):
      - fake runtime 在 `run_request` 中 raise ProviderAuthError → 断言 `run_request_trial` re-raise 同一异常类,**不** emit trial_failed event,events 列表中无 trial_failed entry
      - 重复 case 验证 ProviderRateLimitError、ProviderTransientError 同样行为
    - Test 2 `test_trial_runner_catches_schema_violation`:
      - fake runtime raise `pydantic.ValidationError`(SchemaError alias)→ 断言 `outcome.success == False`,`outcome.failure_category == "ValidationError"`,events 含 `trial_failed` entry
      - 重复 case 验证 TrialFailure、AssertionError 同样 catch 行为
    - 可选 Test 3 `test_trial_runner_propagates_unknown_exception`:
      - fake runtime raise `KeyError("foo")` → 断言 raise 浮到 caller,outcome 不被赋失败值,events 无 trial_failed(D-19 infra_error 路由)
  </behavior>
  <action>
    在 `seers_harness/evolution/trial_runner.py` 按 08-RESEARCH §4-WR-05 改写 line 226-242 的 except 块。

    1. **顶部 import** 新增:
       ```
       from seers_harness.core.errors import (
           ProviderAuthError,
           ProviderRateLimitError,
           ProviderTransientError,
       )
       from seers_harness.validation.exception_classifier import TrialFailure
       from pydantic import ValidationError as SchemaError
       ```
       (per RESEARCH §2-E Q1 — SchemaError module-local alias = pydantic.ValidationError,显式对齐 charter 文本与 code。)

    2. **except 块改写**:
       ```
       except (ProviderAuthError, ProviderRateLimitError, ProviderTransientError):
           # D-19:provider errors 必须 fail-fast 走 provider_error,不是 trial failure
           raise
       except (TrialFailure, AssertionError, SchemaError) as exc:
           outcome.success = False
           outcome.failure_category = type(exc).__name__
           if events is not None:
               events.append({"type": "trial_failed", ...verbatim 既有 event shape...})
       ```
       (events.append 的 event shape 与既有 phase-6/7 字段顺序一致 — `trial_id`, `delta_id`, `failure_category`, `scenario_id`, `request_id` 等;具体字段从 read_first line 229-240 读 verbatim。)

    3. **不要** 加 `except Exception` fallback —— 让其他异常上浮走 D-19 infra_error。这是 WR-05 的核心收紧动作。

    4. **不要** 改 `outcome.token_cost_observed`(plan 08-06 IN-01 落地;它在 *try 块尾部成功路径* 赋值,异常路径下保持默认 0)。

    5. 单测追加到 `tests/test_trial_runner_smoke.py`。Test 1 用 `pytest.parametrize("exc_cls", [ProviderAuthError, ProviderRateLimitError, ProviderTransientError])`;Test 2 用 `pytest.parametrize("exc_cls", [TrialFailure, AssertionError, ValidationError])`。fake runtime 注入用 `class FaultRuntime: def run_request(self, ...): raise self.exc_to_raise`。
  </action>
  <verify>
    <automated>pytest -q tests/test_trial_runner_smoke.py -k "reraises_provider_errors or catches_schema_violation or propagates_unknown" -x 2>&1 | tail -25 ; grep -c "except (ProviderAuthError" seers_harness/evolution/trial_runner.py ; grep -c "except (TrialFailure" seers_harness/evolution/trial_runner.py ; grep -c "except Exception as exc" seers_harness/evolution/trial_runner.py ; grep -c "SchemaError" seers_harness/evolution/trial_runner.py</automated>
    <human-check>2+ 个新测全绿(parametrize 展开后实际 5-6 个 case);`except (ProviderAuthError` 出现 == 1;`except (TrialFailure` 出现 == 1;`except Exception as exc` 在 run_request_trial body 中出现 == 0(narrow 完成);`SchemaError` 出现 ≥ 2(import alias + 引用)。</human-check>
  </verify>
  <done>
    - 2+ 个新测全绿(provider error re-raise + schema/trial-failure catch + unknown propagate)
    - `except (ProviderAuthError, ProviderRateLimitError, ProviderTransientError):` re-raise 块
    - `except (TrialFailure, AssertionError, SchemaError) as exc:` catch 块
    - 无 `except Exception` fallback
    - `SchemaError = ValidationError` module-local alias
    - phase-7 baseline 253 测试无回归
    - phase-6 baseline trial_runner_smoke 不回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trial worker ↔ provider | trial 中的 provider 错误此前被 trial_runner swallow 标为 trial_failed → 假装 trial 失败,实则 D-19 应该 provider_error fail-fast(stale env / rate limit 应 fail-fast,不应反复 trial)|

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11-01 | Repudiation | trial 中 401 错误被 swallow 成 trial_failed → batch_summary 显示 trial 部分失败,实则是 env 漂移 | mitigate | WR-05 narrow 让 401 浮到 runner D-19,fail-fast 标 provider_error,batch_summary by_failure_class.auth 非零,operator 可直接定位 stale env |
| T-08-11-02 | Tampering | future patch 把 except 子句加回 `Exception` 退回 phase-7 行为 | mitigate | 单测 `test_trial_runner_propagates_unknown_exception` 在 KeyError 等 case 上强制 raise 浮出,patch 加回 fallback 即测试失败 |
</threat_model>

<verification>
- pytest 全套绿,2+ 个新测 + parametrize 展开 case 入帐
- grep narrow tuple 各 1 次;`except Exception as exc` 在 run_request_trial body 中 0 hits
- 实测证据(post-batch,D8-G-WR-05):IF trial 期间 DeepSeek auth/rate 错误自然发生 → log 显示 `provider_error -> fail-fast`,**无** `trial_failed` 事件携带 provider exception class;若无 provider error → pytest 单测为唯一证据
</verification>

<success_criteria>
- D8-G-WR-05 落地:trial 异常路径 narrow,provider errors 走 D-19 fail-fast
- SchemaError module-local alias 落地(charter 文本与 code 对齐 per Q1)
- 测序复核:WR-05 在 F + IN-01 + M1-M5 + B + WR-01/02 之后落地,narrow 不掩盖 F 接线 bug,反而增强可观测性(per RESEARCH §7.3)
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-11-SUMMARY.md`。
</output>
