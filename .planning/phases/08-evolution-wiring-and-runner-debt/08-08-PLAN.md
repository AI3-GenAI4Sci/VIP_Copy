---
phase: 08-evolution-wiring-and-runner-debt
plan: 08
type: execute
wave: 5
depends_on:
  - 08-07
files_modified:
  - seers_harness/agentic/tool_loop.py
  - tests/test_tool_loop.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-B (重构后):在 seers_harness/agentic/tool_loop.py 已有 transient retry 点 (line 53-65) 加 backoff 序列 (0/5/15s),不引入第二层 wrapper,runner.py 零改动。这是 phase 8 唯一不动 runner.py 的 deliverable。贡献 D8-ACC-1(transient 重试在自然 transient 时 absorb 而非 fail-fast)+ D8-ACC-4(4 路径 pytest 全绿)。"
  truths:
    - "_TRANSIENT_BACKOFF_SECONDS 模块级常量元组 == (0.0, 5.0, 15.0);attempt 0 不 sleep,attempt 1 sleep 5s,attempt 2 sleep 15s"
    - "ProviderTransientError 触发 retry,最多 max_transient_retries_per_turn+1 次 attempts(默认 3)"
    - "ProviderAuthError / ProviderRateLimitError / ProviderResponseError 第一次 attempt 就 fail-fast,无 sleep,无 retry"
    - "transient budget 耗尽时 raise 最后的 ProviderTransientError,让 runner D-19 路由走 provider_error fail-fast"
    - "B 项加 backoff 落在已有 retry 点,不引入第二层 wrapper —— runner.py 零改动,这是 phase 8 唯一不动 runner 的 deliverable"
  artifacts:
    - path: seers_harness/agentic/tool_loop.py
      provides: "已有 turn 内 transient retry 加 backoff 序列 + module-level _TRANSIENT_BACKOFF_SECONDS 常量"
      contains: "_TRANSIENT_BACKOFF_SECONDS"
  key_links:
    - from: tool_loop.run_skill_via_tools (turn loop)
      to: time.sleep
      via: "attempt > 0 时调用 time.sleep(_TRANSIENT_BACKOFF_SECONDS[min(attempt, len(...) - 1)])"
      pattern: "time\\.sleep\\(.*backoff"
    - from: _TRANSIENT_BACKOFF_SECONDS
      to: charter D8-B
      via: "(0.0, 5.0, 15.0) 序列字面值锁定 — charter Q4 + D8-B"
      pattern: "_TRANSIENT_BACKOFF_SECONDS\\s*[:=]\\s*\\("
  forbid_list:
    - "禁止 修改 runner.py —— B 项落在 tool_loop.py 已有 retry 点,不引入第二层 wrapper(避免 finally 重复 flush_evidence)"
    - "禁止 用指数退避(2^attempt * base)—— charter 锁定 5s/15s 字面值,固定值更可测"
    - "禁止 加 print/log 输出 backoff —— 已有 retry 点已经在 LLM trace 中可见(下一个 turn 是 retry),新增 print 是噪声"
    - "禁止 retry ProviderAuthError / ProviderRateLimitError / ProviderResponseError —— 这些是 fail-fast 路由(D-02 + D-19),只有 ProviderTransientError 走 retry"
    - "禁止 把 backoff 序列硬编码到 for 循环字面值里 —— 必须 module-level 常量 _TRANSIENT_BACKOFF_SECONDS 单一来源"
---

<objective>
落地 D8-B(2026-05-27 grep 后重构):在 `seers_harness/agentic/tool_loop.py:31-65` 已有的 turn 内 transient retry 循环加 backoff 序列 (0s/5s/15s),不在 `_run_one_request` 外起 wrapper。这是 phase 8 *唯一* 不动 runner.py 的 deliverable。

Purpose: charter D8-B 的"5s/15s 请求级 retry"既有 retry 点是天然落地位置 — 已有 `max_transient_retries_per_turn=2`(共 3 次 attempts),无 backoff,immediate retry 把 transient budget 浪费在 DeepSeek 端还没恢复的几秒内。本 plan 加 backoff,单层循环,不引入第二层 wrapper,且 evidence flush 完整(原始 finally 块单次执行)。
Output: 单一 atomic commit;tool_loop.py 新增模块级 `_TRANSIENT_BACKOFF_SECONDS = (0.0, 5.0, 15.0)` + 3 行 backoff 注入到既有 for-attempt 循环;tests/test_tool_loop.py 新增 4 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/agentic/tool_loop.py
@seers_harness/core/errors.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 加 _TRANSIENT_BACKOFF_SECONDS 常量 + 在已有 retry 点注入 time.sleep + 4 个单测</name>
  <files>seers_harness/agentic/tool_loop.py, tests/test_tool_loop.py</files>
  <read_first>
    - seers_harness/agentic/tool_loop.py(全文 — 聚焦 line 31-65 `run_skill_via_tools` 与现有 for-attempt 嵌套循环)
    - seers_harness/core/errors.py(确认 ProviderTransientError / ProviderAuthError / ProviderRateLimitError / ProviderResponseError 4 个类的精确名)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-B 完整伪代码 + "为什么这是更好的位置"论证)
    - tests/test_tool_loop.py(若存在;若不存在则按 RESEARCH §5.1 表补建文件,沿用 phase-7 tests/test_validation_runner.py 的 fault-injection provider 范式)
  </read_first>
  <behavior>
    - Test 1 `test_tool_loop_backoff_on_transient`:fake provider 前 2 次 attempt raise `ProviderTransientError`,第 3 次返成功 result;monkeypatch `time.sleep` 记调用列表 → 断言 `sleep_calls == [5.0, 15.0]`(attempt 1/2 各 sleep 一次,attempt 0 不 sleep);最终 result 成功返回。
    - Test 2 `test_tool_loop_does_not_backoff_on_first_attempt`:fake provider 第 1 次就成功;monkeypatch `time.sleep` → 断言 sleep 未被调用(`sleep_calls == []`)。
    - Test 3 `test_tool_loop_does_not_retry_auth_error`:fake provider 第 1 次 raise `ProviderAuthError`;monkeypatch `time.sleep` → 断言 raise 浮到 caller,`sleep_calls == []`(无 retry,无 backoff)。同样 case 验 `ProviderRateLimitError` 与 `ProviderResponseError`(可放在同一 test 用 pytest.parametrize)。
    - Test 4 `test_tool_loop_exhausts_transient_budget`:fake provider 连续 3 次 raise `ProviderTransientError`;monkeypatch `time.sleep` → 断言 raise 最后的 `ProviderTransientError`,`sleep_calls == [5.0, 15.0]`,然后 raise 浮到 caller。
  </behavior>
  <action>
    在 `seers_harness/agentic/tool_loop.py`:

    1. **顶部 import 段**:确保 `import time` 存在(若已 import 跳过)。
    2. **模块级常量**:在 import 之后、`run_skill_via_tools` 之前,新增:
       ```
       _TRANSIENT_BACKOFF_SECONDS: tuple[float, ...] = (0.0, 5.0, 15.0)
       """D8-B: backoff 序列,与 max_transient_retries_per_turn=2(共 3 attempts)对齐.
       attempt 0 → 0.0s, attempt 1 → 5.0s, attempt 2 → 15.0s. 字面值锁定 by charter Q4 + D8-B."""
       ```
    3. **已有 retry 点改写**(line 53-65 附近的 `for attempt in range(max_transient_retries_per_turn + 1):` 块):
       ```
       for attempt in range(max_transient_retries_per_turn + 1):
           if attempt > 0:
               backoff = _TRANSIENT_BACKOFF_SECONDS[min(attempt, len(_TRANSIENT_BACKOFF_SECONDS) - 1)]
               time.sleep(backoff)
           try:
               result = provider.generate_with_tools(...)  # 既有调用,不动 signature
               break
           except ProviderTransientError:
               if attempt == max_transient_retries_per_turn:
                   raise
       ```
       (保留既有 `provider.generate_with_tools` 调用、break 位置、最后 attempt raise 行为不变。**只插入** if-attempt>0 sleep 三行。)
    4. **禁止** 在 try/except 块中再 catch `ProviderAuthError` / `ProviderRateLimitError` / `ProviderResponseError` —— 这些异常不在 except 列表,会自然上浮(fail-fast 第一次 attempt),无 sleep 介入。Test 3 验此行为。
    5. **禁止** 改 `_run_one_request` 或 runner.py 任何文件 —— 验收 grep 包含此项。
    6. 单测:若 `tests/test_tool_loop.py` 不存在,创建之;沿用 phase-7 `tests/test_validation_runner.py` 中 fault-injection provider 范式(class FaultProvider with `generate_with_tools(self, ...): if self.call_count < self.fail_until: ... raise ProviderTransientError(...) else: return ...`)。`monkeypatch.setattr("seers_harness.agentic.tool_loop.time.sleep", lambda s: sleep_calls.append(s))` 记 sleep。
  </action>
  <verify>
    <automated>pytest -q tests/test_tool_loop.py -x 2>&1 | tail -25 ; grep -c "_TRANSIENT_BACKOFF_SECONDS" seers_harness/agentic/tool_loop.py ; grep -c "time.sleep" seers_harness/agentic/tool_loop.py ; git diff --stat seers_harness/validation/runner.py</automated>
    <human-check>4 个新测全绿;`_TRANSIENT_BACKOFF_SECONDS` 出现 ≥ 2(定义 + 引用);`time.sleep` 出现 == 1(只在 backoff 注入处);`git diff --stat seers_harness/validation/runner.py` 显示 0 changed lines(runner.py 零改动 — 关键审美约束)。</human-check>
  </verify>
  <done>
    - 4 个新测在 tests/test_tool_loop.py 入帐并全绿
    - `_TRANSIENT_BACKOFF_SECONDS = (0.0, 5.0, 15.0)` 模块级常量
    - `time.sleep` 在 tool_loop.py 出现 == 1(仅在 backoff 注入处)
    - **runner.py 字节级不变**(关键审美约束;grep / git diff 验证)
    - phase-7 baseline 253 测试无回归
    - dag_runner.py 等其他 `run_skill_via_tools` 调用点无任何 sleep mock 期望(grep `run_skill_via_tools(` callsites 全为常规调用 — 影响面扩大到所有调用点是 *好事*,见 RESEARCH §7.3)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool_loop ↔ provider | retry 仅 catch ProviderTransientError;auth/rate/response 类直接 fail-fast |
| backoff 序列 ↔ Stage 3 c=20 wall-clock | backoff 总 20s/请求;c=20 worker 独立线程,backoff 不阻塞他人,实际 wall-clock 增量 ≈ 20s 而非 400s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | DoS | DeepSeek 故意持续 transient 让 backoff 燃尽 token budget(每次 5+15s 累积 in wall-clock)| accept | charter Q4 锁定 budget 3 attempts;budget 耗尽即 fail-fast,不无限 retry;Stage 3 worst-case 20s/请求是已知接受范围 |
| T-08-08-02 | Tampering | future patch 把 backoff 序列从 (0,5,15) 改成 (0,1,2) silently | mitigate | 模块级常量带 docstring 引用 D8-B + charter Q4;grep 验收锁定 `(0.0, 5.0, 15.0)` 字面 |
| T-08-08-03 | Information Disclosure | print backoff 时间到 stderr 间接泄露 retry 模式给攻击者 | mitigate | forbid_list 禁加 print/log;retry 行为已经在 LLM trace 中可见,不引入二级日志 |
</threat_model>

<verification>
- pytest 4 个新测全绿
- grep:`_TRANSIENT_BACKOFF_SECONDS` 在 tool_loop.py 出现 ≥ 2;`time.sleep` == 1
- runner.py 字节级不变(`git diff --stat seers_harness/validation/runner.py` == empty)
- 实测证据(post-batch,D8-B 用户决定 Q3=B):**不在 batch 中混故障注入**;phase-8 batch 若无自然 transient,pytest 4 路径单测为唯一证据,**禁止伪造实测证据**。若有自然 transient → `.run-logs/runner-<ts>.log` 显示 ProviderTransientError 后下一个 turn 仍成功(隐含 sleep 已生效)
</verification>

<success_criteria>
- D8-B 落地:已有 retry 点加 backoff,单层循环,无 wrapper
- 关键审美约束:runner.py 字节级不变 — phase 8 唯一不动 runner.py 的 deliverable
- evidence flush 完整:原始 finally 块单次执行(B 项不影响 finally)
- 影响面扩大到所有 run_skill_via_tools 调用点(dag_runner + _distill_after_stage1),整个 harness 获 backoff
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-08-SUMMARY.md`,记录 commit SHA + 4 个新测 + grep 验收 + git diff --stat 证明 runner.py 零改动。
</output>
