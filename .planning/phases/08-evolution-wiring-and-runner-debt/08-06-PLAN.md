---
phase: 08-evolution-wiring-and-runner-debt
plan: 06
type: execute
wave: 3
depends_on:
  - 08-05
files_modified:
  - seers_harness/evolution/trial_runner.py
  - tests/test_trial_runner_smoke.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-G-IN-01:把 runtime.trace 中 tool_loop_summary events 的 usage.total_tokens 累加,写入 TrialOutcome.token_cost_observed,让 M4/M5 真阳性度量有可验证数据(死字段复活)"
  truths:
    - "run_request_trial 成功路径下 TrialOutcome.token_cost_observed == sum(int(ev.usage.total_tokens or 0) for ev in runtime.trace if ev.type == 'tool_loop_summary')"
    - "runtime.trace 中无 tool_loop_summary event 或所有 usage 缺失时,token_cost_observed == 0(不 raise)"
    - "tool_call_count 与 token_cost_observed 在同一聚合块中赋值(两者都从 runtime.trace 派生),保持代码近邻易审计"
  artifacts:
    - path: seers_harness/evolution/trial_runner.py
      provides: "run_request_trial 成功路径下 token_cost_observed 字段从 runtime.trace usage 累加(IN-01 复活死字段)"
      contains: "token_cost_observed = sum"
  key_links:
    - from: run_request_trial success branch
      to: runtime.trace[*].usage.total_tokens
      via: "sum(int((ev.get('usage') or {}).get('total_tokens') or 0) for ev in runtime.trace if ev.get('type') == 'tool_loop_summary')"
      pattern: "token_cost_observed\\s*=\\s*sum"
  forbid_list:
    - "禁止给 TrialOutcome 加新字段 —— 只复活既有 token_cost_observed,不扩 schema"
    - "禁止在 trial_runner 中读 evidence 文件计 token —— runtime.trace 是 in-memory 数据源,evidence 是写盘后状态,前者更早可见"
    - "禁止给 token_cost_observed 加 fallback (如 trace 为空时取 len(messages) * 100 估算)—— 启发式禁地,缺数据就是 0"
    - "禁止改 run_request_trial 异常路径下的 token_cost_observed —— 默认 0 由 dataclass 默认值提供,异常路径不赋值"
---

<objective>
落地 D8-G-IN-01:让 `TrialOutcome.token_cost_observed` 死字段复活。`run_request_trial` 成功路径下从 `runtime.trace` 中 `tool_loop_summary` events 累加 `usage.total_tokens`,赋给 outcome。M4/M5 行为度量(plan 08-07)依赖此字段非零。

Purpose: D8-G-IN-01 in `07-WRIN-TRIAGE.md` deferred 项;M4 (delta_diversity) 与 M5 (trial_belief_update_count) 的真阳性都依赖 `token_cost_observed > 0` 作为"trial 真的产生了 LLM 调用"的证据。F 接线(plan 08-05)已把 trial 跑通,本 plan 在同一聚合块把死字段填上。
Output: 单一 atomic commit;trial_runner.py 在既有 `tool_call_count` 聚合块下方加 3-4 行 `token_cost_observed` 累加;新增 2 个单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/evolution/trial_runner.py
@seers_harness/workflow/dag_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: token_cost_observed 从 runtime.trace usage 累加 + 2 个单测</name>
  <files>seers_harness/evolution/trial_runner.py, tests/test_trial_runner_smoke.py</files>
  <read_first>
    - seers_harness/evolution/trial_runner.py(line 155-255 `run_request_trial`,line 217-225 现有 `tool_call_count` 聚合)
    - seers_harness/workflow/dag_runner.py(确认 `WorkflowRuntime.trace` 字段 + `tool_loop_summary` event 的 usage 字段精确 shape:期望 `{"type": "tool_loop_summary", "node_id": ..., "tool_calls_made": int, "usage": {"prompt_tokens": int, "completion_tokens": int, "total_tokens": int}}`)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-IN-01 完整伪代码)
    - tests/test_trial_runner_smoke.py(找 fake runtime + scripted provider fixture 模式)
  </read_first>
  <behavior>
    - Test 1 `test_trial_outcome_token_cost_from_trace_usage`:fake runtime 的 `trace` 含 3 个 tool_loop_summary events,usage.total_tokens 分别为 100, 250, 500 → `outcome.token_cost_observed == 850`。
    - Test 2 `test_trial_outcome_token_cost_zero_when_no_usage`:fake runtime 的 `trace` 为空 *或* 所有 events 的 type != 'tool_loop_summary' *或* events 含 usage=None → `outcome.token_cost_observed == 0`(不 raise,不 fallback)。
  </behavior>
  <action>
    在 `seers_harness/evolution/trial_runner.py` 的 `run_request_trial` 函数中,定位现有 `tool_call_count` 聚合块(line 217-225 附近),在其紧接下方按 08-RESEARCH §4-IN-01 伪代码追加:
    ```
    outcome.tool_call_count = sum(
        int(ev.get("tool_calls_made") or 0)
        for ev in runtime.trace
        if ev.get("type") == "tool_loop_summary"
    )
    # IN-01: token_cost_observed 从 runtime.trace 中 tool_loop_summary events 累加 usage.total_tokens
    outcome.token_cost_observed = sum(
        int((ev.get("usage") or {}).get("total_tokens") or 0)
        for ev in runtime.trace
        if ev.get("type") == "tool_loop_summary"
    )
    ```
    
    **位置严守:** 紧贴 `tool_call_count` 之后,*同一聚合块*。两者都从 `runtime.trace` 派生 —— 代码近邻便于未来 audit"trace 派生字段都来自这里"。
    
    **不要** 改 `run_request_trial` 的异常分支(line 226-242 的 `except` 块)—— 异常路径下 TrialOutcome 的 `token_cost_observed` 保持 dataclass 默认值 0(per forbid_list)。
    
    **不要** 在新逻辑里加 try/except 包 KeyError/TypeError —— `.get("usage") or {}` 已经处理 None 缺失;若 trace event 是非 dict shape(理论不会),`ev.get` 会 AttributeError,这是 runner_bug,应该上浮。
    
    单测追加到 `tests/test_trial_runner_smoke.py`。fake runtime 应是一个最小 stub:`class FakeRuntime: trace = [...]`,trace 列表手工构造。`run_request_trial` 调用需要其他 kwarg(scenario / nodes / live_skill_root / workspace_dir),沿用文件中既有 phase-6 baseline fixture 模式。
  </action>
  <verify>
    <automated>pytest -q tests/test_trial_runner_smoke.py -k "token_cost" -x 2>&1 | tail -20 ; grep -c "token_cost_observed = sum" seers_harness/evolution/trial_runner.py ; grep -c "token_cost_observed" seers_harness/evolution/trial_runner.py</automated>
    <human-check>2 个新测全绿;`token_cost_observed = sum` 出现 1 次;`token_cost_observed` 总出现 ≥ 2(dataclass 默认值定义 + sum 赋值);异常分支不含 `token_cost_observed` 赋值。</human-check>
  </verify>
  <done>
    - 2 个新测在 tests/test_trial_runner_smoke.py 入帐并全绿
    - `token_cost_observed` 累加块紧贴 `tool_call_count` 聚合块,同一 indentation 层级
    - 异常分支字节级不变(WR-05 在 plan 08-11 才动该分支)
    - phase-7 baseline 253 测试无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime.trace → outcome | trace 是 in-memory dag_runner 数据;outcome 写入 evolution_snapshot.json 用于审计。token_cost_observed 只是聚合数字,无 PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Tampering | trace event shape 漂移(future SDK 改 usage 字段名)→ token_cost_observed 默认 0,M5 度量误判 | mitigate | `.get("usage") or {}` 仅在期望 shape 上累加;若漂移,M5 metric 失败 → user review,不静默 swallow |
| T-08-06-02 | Repudiation | trial 跑了但 token_cost 报 0,看似无 LLM 调用实则有 | mitigate | trace event shape 由 dag_runner 控制(phase 6 contract);若 trace 缺 usage,是 dag_runner bug 而非 IN-01 责任,在 6.6 runner_bug 桶可定位 |
</threat_model>

<verification>
- pytest 全套绿,2 个新测入帐
- grep `token_cost_observed = sum` 在 trial_runner.py 出现 == 1
- 实测证据(post-batch,M4/M5 联动):至少一个 `evolution_snapshot.json` 的 `trials[*].token_cost_observed > 0`(F 接线产生的 trial 真有 LLM 调用)
</verification>

<success_criteria>
- D8-G-IN-01 落地:死字段复活,M4/M5 度量数据源就位
- TrialOutcome schema 字节级不变(只填字段,不加字段)
- 算法路径 vs 启发式:sum 累加 + None-safe,无 fallback,无估算
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-06-SUMMARY.md`。
</output>
