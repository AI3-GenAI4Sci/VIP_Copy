---
phase: 08-evolution-wiring-and-runner-debt
plan: 05
type: execute
wave: 3
depends_on:
  - 08-04
files_modified:
  - seers_harness/validation/runner.py
  - tests/test_validation_runner.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-F (C4 路径):distill-skill-deltas agent 在 Stage 1 完成后从真 trace 算法提取 delta,_run_one_request 增加 trial loop 在 portfolio 非空时跑 run_request_trial,完整闭环 distill→portfolio→trial→update_after_trial。单 commit,大改动,无 hardcoded seed delta。"
  truths:
    - "run() 在 Stage 1 passed 后恰好调用 _distill_after_stage1 一次,Stage 1 failed 时不调用 distill"
    - "_distill_after_stage1 调用 run_skill_via_tools 跑 distill-skill-deltas agent,产 DeltaDistillationArtifact,merge 进 portfolio (assemble_portfolio 纯 transform)"
    - "_run_one_request 在 delta_portfolio 非空时对每个 row 跑一次 run_request_trial,events 由 caller 传入并落进 evolution_snapshot.json"
    - "_run_one_request 在 delta_portfolio 为空时(Stage 1 / distill 前)host 请求仍跑通,evolution_snapshot.trials[] 为空,不 raise(D-18)"
    - "_patch_from_portfolio_row 仅支持 change_type=='modify_skill' + target_skill 存在;其他类型返回 None(skip 不 raise)"
    - "trial 中 update_after_trial 把 outcome 折回 portfolio row 的 belief counters(sample_count / success_count / failure_count),供 M5 度量"
    - "distill agent 产 0 proposals 时 portfolio 保持空 — 禁止 fallback 到 hardcoded seed,真阳性边界严守"
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "_distill_after_stage1 + _build_trajectory_payload + _patch_from_portfolio_row + _run_one_request(增 delta_portfolio/live_skill_root kwargs + trial loop) + _run_stage/run 调用点更新"
      contains: "_distill_after_stage1"
    - path: tests/test_validation_runner.py
      provides: "至少 5 个新 F-接线单测(见 RESEARCH §3.6)"
      contains: "test_run_one_request_fires_trial_when_portfolio_nonempty"
  key_links:
    - from: runner.py run()
      to: _distill_after_stage1
      via: "stage1.passed 后唯一调用点,把 stage1_result + provider_factory + current portfolio 传入"
      pattern: "_distill_after_stage1\\("
    - from: _distill_after_stage1
      to: run_skill_via_tools
      via: "skill='distill-skill-deltas' + EVOLUTION_TOOLS_SPEC + EVOLUTION_TOOL_HANDLERS"
      pattern: "run_skill_via_tools\\("
    - from: _distill_after_stage1
      to: assemble_portfolio
      via: "proposals (from DeltaDistillationArtifact) 合并进 current_portfolio"
      pattern: "assemble_portfolio\\("
    - from: _run_one_request trial loop
      to: run_request_trial
      via: "for portfolio_row in delta_portfolio: run_request_trial(events=events, patch=_patch_from_portfolio_row(row))"
      pattern: "run_request_trial\\("
    - from: _run_one_request trial loop
      to: update_after_trial
      via: "把 TrialOutcome 折回 portfolio belief — M5 数据源"
      pattern: "update_after_trial\\("
  forbid_list:
    - "禁止 hardcoded seed delta —— delta_portfolio 起始空,只由 distill agent 在 Stage 1 后从真 trace 算法提取填充(D8-F2 + Q2=C4 用户决定 2026-05-27)"
    - "禁止 fallback 到人工预定义 patch —— distill agent 产 0 proposals 时 portfolio 保持空,Stage 2/3 不跑 trial,acceptance 失败就失败(真阳性边界)"
    - "禁止启发式 if/elif 选 delta —— assemble_portfolio 是 set-merge by delta_id,update_after_trial 是 bandit counter 算术,全 pure transform"
    - "禁止给 _run_one_request 加 retry wrapper —— B 项落 tool_loop.py(plan 08-08),不在此处嵌套"
    - "禁止在 distill 失败时 catch ValidationError 把 distill 当成 trial_failure —— ValidationError 必须上浮到 run(),让 D-19 走 schema_violation fail-fast"
    - "禁止给 portfolio_row 加除 modify_skill 之外的 change_type 处理 —— phase 8 范围内只支持 textual modify_skill,其他类型 _patch_from_portfolio_row 返回 None 让 trial loop skip"
---

<objective>
落地 D8-F(C4 路径):runner.py ↔ evolution 全链路接线。Stage 1 完成后跑一次 `distill-skill-deltas` agent,把真 trace 算法提取的 delta 合并进 `delta_portfolio`;Stage 2/3 的 `_run_one_request` 在 host 请求成功后,对 portfolio 中每个 `modify_skill` 类 row 跑一次 `run_request_trial`,outcome 折回 portfolio belief counters。完整闭环 distill→portfolio→trial→update_after_trial,零 hardcoded seed,零启发式。

Purpose: D8-ACC-2 + D8-F1/F2/F3 + M4/M5 真阳性边界全部依赖本 plan。F 接线是 phase 8 的 *primary deliverable*,RESEARCH §7.2 锁定它为单 commit 大改动(IN-01 紧跟在 plan 08-06,M1-M5 紧跟在 plan 08-07)。
Output: 单一 atomic commit;runner.py 新增 3 个 helper(`_distill_after_stage1` / `_build_trajectory_payload` / `_patch_from_portfolio_row`)+ `_run_one_request` 签名扩展 + trial loop + `_run_stage` + `run()` 调用点更新;tests/test_validation_runner.py 新增 ≥5 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/runner.py
@seers_harness/evolution/trial_runner.py
@seers_harness/evolution/delta_portfolio.py
@seers_harness/validation/evolution_snapshot.py
@seers_harness/agentic/tool_loop.py
@seers_harness/tools/evolution_tools.py
@workflow-skills/evolution/distill-skill-deltas/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 _build_trajectory_payload + _distill_after_stage1 + _patch_from_portfolio_row 三个 helper</name>
  <files>seers_harness/validation/runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(全文 + 特别 line 425-535 `_run_one_request`、line 780-790 `_delta_portfolio_empty` D-18 anchor、`run()` 函数 Stage 循环)
    - seers_harness/agentic/tool_loop.py(`run_skill_via_tools` 签名与返回 shape)
    - seers_harness/tools/evolution_tools.py(`EVOLUTION_TOOLS_SPEC` 字典结构 + `EVOLUTION_TOOL_HANDLERS` 字典结构 + `submit_delta_distillation_final` 入口)
    - seers_harness/evolution/delta_portfolio.py(`DeltaDistillationArtifact`、`DeltaProposal`、`DeltaPortfolioRow`、`SkillDeltaPatch`、`assemble_portfolio`、`update_after_trial`、`sha256_of_text`)
    - workflow-skills/evolution/distill-skill-deltas/SKILL.md(确认 skill bundle path 与名字)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§3.4 完整伪代码 + §3.5 _patch_from_portfolio_row 伪代码)
  </read_first>
  <behavior>
    - 单测无,纯 helper 实现;行为由 Task 2/3 的集成测试覆盖。
    - `_build_trajectory_payload(stage1_request_dir: Path) -> dict`:读 evidence/<node>/{artifact.json, tool_calls.jsonl, usage.json},返回固定 shape 的 dict(per RESEARCH §3.4 伪代码)。Pure file-read + dict-assembly,无 LLM,无 filtering。
    - `_distill_after_stage1(*, stage1_result, provider_factory, current_portfolio) -> list[DeltaPortfolioRow]`:跑 distill agent,merge 进 portfolio,返回 new portfolio。
    - `_patch_from_portfolio_row(row: DeltaPortfolioRow, live_skill_root: Path) -> SkillDeltaPatch | None`:row.change_type != "modify_skill" → None;target file 不存在 → None;否则构造 SkillDeltaPatch(target_path, sha256_of_text(live_text), replacement_text=row.proposed_change)。
  </behavior>
  <action>
    在 `seers_harness/validation/runner.py` 顶层 helper 段(其他 helper 附近,如 `_safe_request_dirname` 周围),新增三个函数,实现 verbatim 取自 08-RESEARCH §3.4 + §3.5 伪代码:

    **`_build_trajectory_payload(stage1_request_dir: Path) -> dict`**(纯 transform,per §3.4):
    - 读 `evidence/factor_discovery/artifact.json`、`evidence/copy_generation/artifact.json`、`evidence/personalized_copy_rubric/artifact.json`
    - 对三 node 各读 `tool_calls.jsonl`(逐行 json.loads,skip 空行)
    - 对三 node 各读 `usage.json`
    - 返回 dict 含 keys: `request_id`, `factor_discovery`, `copy_generation`, `personalized_copy_rubric`, `tool_calls_per_node`, `usage_per_node`
    - **禁止** 在 payload 里加 filtering / scoring / weighting —— 启发式禁地

    **`_distill_after_stage1(*, stage1_result, provider_factory, current_portfolio: list[DeltaPortfolioRow]) -> list[DeltaPortfolioRow]`**(per §3.4):
    - 取 `stage1_result.records[0]["request_id"]`,组装 `stage1_request_dir = stage1_result.stage_dir / _safe_request_dirname(rid)`
    - 调 `_build_trajectory_payload(stage1_request_dir)` 拿 payload
    - import:`from seers_harness.agentic.tool_loop import run_skill_via_tools`、`from seers_harness.tools.evolution_tools import EVOLUTION_TOOLS_SPEC, EVOLUTION_TOOL_HANDLERS`、`from seers_harness.evolution.delta_portfolio import DeltaDistillationArtifact, assemble_portfolio`、`from seers_harness.validation.recording_provider import RecordingProvider`
    - 用 `provider_factory()` 构造 inner provider,`RecordingProvider(inner, [])` 包装(distill agent trace 作为审计证据,虽不写盘但保持一致 wrapping)
    - 读 distill skill bundle:`skill_bundle = (LIVE_SKILL_ROOT / "evolution/distill-skill-deltas/SKILL.md").read_text(encoding="utf-8")`。**`LIVE_SKILL_ROOT` LOCKED:** 在 runner.py 顶部 import 区下方(line 145 附近 `_DEFAULT_RUNS_ROOT` 常量旁)新增模块级常量 `LIVE_SKILL_ROOT: Path = Path(__file__).resolve().parents[2] / "workflow-skills"`(grep-verified 2026-05-27 runner.py 当前未含此常量;workspace skill 根在 `workflow-skills/` 而非 `workspace-skills/`,与 RESEARCH §3.4 校正一致)
    - **Distill firing 实测证据(ISSUE-06 修复):** 在调用 `run_skill_via_tools` *之前* 写一行 `print(f"[runner] distill_after_stage1: starting agent, stage1_request_id={rid}", file=sys.stderr)`,*之后* 写一行 `print(f"[runner] distill_after_stage1: produced {len(artifact.proposals)} proposals (delta_ids={[p.delta_id for p in artifact.proposals]})", file=sys.stderr)`。这两行使 `.run-logs/runner-<ts>.log` 可被 grep 验证 distill agent 真的运行了(per RESEARCH §3.7 firing 证据 + D8-VAL-REAL)。**绝不** 把 proposal observation / proposed_change 文本写日志(可能含 trace 摘要)—— 只展示 delta_id 列表。
    - 调 `run_skill_via_tools(skill_name="distill-skill-deltas", skill_bundle=skill_bundle, payload=trajectory_payload, tools_spec=EVOLUTION_TOOLS_SPEC["distill-skill-deltas"], tool_handlers=EVOLUTION_TOOL_HANDLERS, provider=distill_provider, node_id="distill_after_stage1")`
    - `artifact = DeltaDistillationArtifact.model_validate(result.artifact)` —— ValidationError 浮到 caller(forbid_list 第 5 条)
    - `return assemble_portfolio(current_portfolio, artifact.proposals, events=None)` —— phase 8 不在 batch 级 emit `portfolio_assembled` event(per §3.2 推荐),避免每请求 noise

    **`_patch_from_portfolio_row(row: DeltaPortfolioRow, live_skill_root: Path) -> SkillDeltaPatch | None`**(per §3.5):
    - `if row.change_type != "modify_skill": return None` —— phase 8 范围严守
    - `live_target = live_skill_root / row.target_skill`
    - `if not live_target.exists(): return None` —— D8-VAL-ROOTCAUSE "不掩盖",skip 而非 raise
    - `live_text = live_target.read_text(encoding="utf-8")`
    - `return SkillDeltaPatch(target_path=row.target_skill, original_text_sha256=sha256_of_text(live_text), replacement_text=row.proposed_change)`

    **禁止** 在三个 helper 中加 try/except 包 ValidationError 把它转成 None 或默认值 —— 见 forbid_list。**禁止** 在 _distill_after_stage1 里 catch 0-proposal case 然后塞 hardcoded seed —— 见 forbid_list。
  </action>
  <verify>
    <automated>grep -c "def _build_trajectory_payload" seers_harness/validation/runner.py ; grep -c "def _distill_after_stage1" seers_harness/validation/runner.py ; grep -c "def _patch_from_portfolio_row" seers_harness/validation/runner.py ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10</automated>
    <human-check>三个 helper grep 命中 == 1;现有套件不回归(单测覆盖在 Task 3 加,此处只保护 baseline)。</human-check>
  </verify>
  <done>
    - 三个 helper 函数在 runner.py 出现各 1 次定义
    - 现有 253 baseline 测试无回归
    - 无 try/except 包 ValidationError;无 hardcoded seed 字面量
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _run_one_request 签名扩展 delta_portfolio + live_skill_root + trial loop 调用</name>
  <files>seers_harness/validation/runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 425-535 `_run_one_request` 完整体)
    - seers_harness/evolution/trial_runner.py(line 155-255 `run_request_trial` 签名与 events emission)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§3.5 完整伪代码)
  </read_first>
  <behavior>
    - 行为由 Task 3 的单测覆盖;此 task 是签名 + body 改动,无独立单测。
    - `_run_one_request(*, request_id, scenario, nodes, provider_factory, request_dir, events, delta_portfolio, live_skill_root)`:host 请求成功后,for portfolio_row in delta_portfolio: patch=_patch_from_portfolio_row(...); 若 patch is None → continue;否则调 run_request_trial(events=events, patch=patch, live_skill_root=live_skill_root, workspace_dir=request_dir / "trial_workspace" / row.delta_id);若 outcome.trial_delta_id 非 None,写 record["trial_selected_delta_id"];调 update_after_trial(row, outcome) 折回 belief。
  </behavior>
  <action>
    在 `seers_harness/validation/runner.py` `_run_one_request` 函数(line 425-535)按 RESEARCH §3.5 伪代码改写:

    1. **签名加 2 个 kwarg**:`delta_portfolio: list[DeltaPortfolioRow]` 与 `live_skill_root: Path`。kwarg-only(`*,` 之后),无 default —— caller 必须显式传,避免误用。
    2. **顶部 import**(若 runner.py 尚未 import):`from seers_harness.evolution.delta_portfolio import DeltaPortfolioRow, update_after_trial`、`from seers_harness.evolution.trial_runner import run_request_trial`、`from seers_harness.workflow.dag_runner import WorkflowRuntime`(若已 import 复用)。
    3. **host 请求 try 块尾部**(在现有 artifact 解析后、`finally` 之前),插入 trial loop:
       ```
       for portfolio_row in delta_portfolio:
           patch = _patch_from_portfolio_row(portfolio_row, live_skill_root)
           if patch is None:
               continue   # change_type 非 modify_skill 或 target 漂移,skip
           trial_workspace = request_dir / "trial_workspace" / portfolio_row.delta_id
           trial_outcome = run_request_trial(
               runtime=WorkflowRuntime(provider=proxy, output_dir=trial_workspace / "_artifacts"),
               scenario=scenario,
               nodes=list(nodes),
               live_skill_root=live_skill_root,
               workspace_dir=trial_workspace,
               patch=patch,
               request_id=request_id,
               scenario_id=str(scenario.get("scenario_id", "")),
               events=events,
           )
           if trial_outcome.trial_delta_id is not None:
               record["trial_selected_delta_id"] = trial_outcome.trial_delta_id
           # 把 outcome 折回 portfolio belief counters(算法,非启发式 — M5 数据源)
           portfolio_row = update_after_trial(portfolio_row, trial_outcome)
       ```
    4. **不要** 把 trial 异常 catch 转成 record.exception —— `run_request_trial` 已经 emit `trial_failed` event(D-19 trial_failure 路径);本 plan 落地的是 *已经存在的* 异常路由,只把 trial 跨进来。
    5. **保留** finally 块的 `_cv.reset(token)`、`flush_evidence`、`write_evolution_snapshot(events, ...)`(WR-02 在 plan 08-10 才动 finally;此处 events 已含 trial events,write 后落进 evolution_snapshot.json)。
    6. **不要** 给 `_run_one_request` 加 transient retry wrapper —— B 项落在 tool_loop.py(plan 08-08)。
  </action>
  <verify>
    <automated>grep -c "delta_portfolio" seers_harness/validation/runner.py ; grep -c "run_request_trial" seers_harness/validation/runner.py ; grep -c "update_after_trial" seers_harness/validation/runner.py ; grep -c "live_skill_root" seers_harness/validation/runner.py ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10</automated>
    <human-check>delta_portfolio 在 runner.py 出现 ≥ 4(签名 + 循环 + run_stage 传参 + run() 装配);run_request_trial 出现 ≥ 1;update_after_trial 出现 ≥ 1。</human-check>
  </verify>
  <done>
    - `_run_one_request` 签名扩展,kwarg-only,无 default
    - trial loop 在 host 成功路径尾部,_patch_from_portfolio_row 返 None 时 continue
    - update_after_trial 折回 belief
    - 既有 baseline 253 测试无回归(empty portfolio 走 for 空循环,host 行为不变)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _run_stage + run() 调用点更新 + 5+ 个 F-接线单测</name>
  <files>seers_harness/validation/runner.py, tests/test_validation_runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(`_run_stage` 与 `run()` 调用 _run_one_request 的两处 + 现有 stage 循环)
    - tests/test_validation_runner.py(找 ScriptedProvider / FaultProvider fixture + monkeypatch 模式)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§3.6 测试锚点 9 个 + §3.4-3.5 行为 spec)
  </read_first>
  <behavior>
    - `test_run_one_request_fires_trial_when_portfolio_nonempty`:inject DeltaPortfolioRow(`change_type="modify_skill"`, `target_skill="<existing live path>"`, `proposed_change="..."`) + fake provider(返回 valid host artifacts + valid trial artifacts)→ 跑 1 个 _run_one_request → `evolution_snapshot.json` 解析后 `trials[]` 非空,outcome.success=True。
    - `test_run_one_request_skips_trial_when_portfolio_empty`:empty portfolio → host 仍成功,evolution_snapshot.trials 为空(D-18 默认行为)。
    - `test_run_one_request_trial_failure_does_not_abort_host`:inject portfolio + fake provider 在 trial 中 raise TrialFailure → host record.exception is None,evolution_snapshot 含 trial_failed event(`run_request_trial` 内部已 catch TrialFailure 并 emit)。
    - `test_run_one_request_skips_non_modify_skill_delta`:portfolio row `change_type="add_skill"` → trial loop continue,evolution_snapshot.trials 为空,host 成功。
    - `test_run_one_request_skips_drifted_target_path`:portfolio row `target_skill="nonexistent/path"` → _patch_from_portfolio_row 返 None,trial loop continue,host 成功。
    - `test_distill_after_stage1_with_recording_provider`:fake provider 返回 valid distill artifact(含 2 DeltaProposal)+ fake stage1 evidence dir → `_distill_after_stage1` 返回 portfolio 长度 == 2,delta_id 来自 artifact。
    - `test_distill_after_stage1_empty_proposals_yields_empty_portfolio`:provider 返回 `proposals=[]` → portfolio 长度 == 0,**不 raise,不 fallback 到 hardcoded seed**。
    - `test_distill_after_stage1_invalid_artifact_raises`:provider 返回 schema-invalid artifact dict → `pydantic.ValidationError` 浮到 caller(forbid_list 第 5 条)。
    - `test_run_drives_distill_only_after_stage1_passes`:inject failing Stage 1(host raise)→ distill 不被调用;passing Stage 1 → distill 被调用恰一次(monkeypatch `_distill_after_stage1` 计 call 次数)。
  </behavior>
  <action>
    **runner.py 改动**:
    1. `_run_stage` 签名加 `delta_portfolio: list[DeltaPortfolioRow]` + `live_skill_root: Path` kwarg-only,并把它们透传给 `_run_one_request`(在 submit / direct 调用两处都改)。
    2. `run()` 函数(stage 循环装配点):
       a. 删除 `_delta_portfolio_empty: list[Any] = []` 一行(D-18 anchor),改为 `delta_portfolio: list[DeltaPortfolioRow] = []  # D-18:起始空,distill 后填充`
       b. 在 stage 循环内 stage 1 完成后(`if result.passed and stage_number == 1:`),调用 `delta_portfolio = _distill_after_stage1(stage1_result=result, provider_factory=provider_factory, current_portfolio=delta_portfolio)`
       c. 把 `delta_portfolio` 和 `live_skill_root`(从 runner 模块级常量或 args 取)透传给 `_run_stage`
       d. **禁止** 在 Stage 1 之外的 stage 调 distill —— per RESEARCH §3.4,distill 是 Stage 1 后唯一一次

    **tests/test_validation_runner.py 新增 9 个测试**:沿用文件中既有 ScriptedProvider / monkeypatch.setenv / tmp_path fixture 模式。fake provider 应实现 `generate_with_tools` 接口,按 (call_index, node_id) 返回 scripted response。distill agent 测试需要构造 fake stage1_result(`StageResult(stage_dir=tmp_path/'stage1', records=[{"request_id": "req-1"}])`)+ pre-populate tmp_path/'stage1/req-1/evidence/<3-nodes>/{artifact.json, tool_calls.jsonl, usage.json}`)。
  </action>
  <verify>
    <automated>pytest -q tests/test_validation_runner.py -k "trial or distill" -x 2>&1 | tail -30 ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10 ; grep -c "_distill_after_stage1" seers_harness/validation/runner.py</automated>
    <human-check>9 个新测全绿;`_distill_after_stage1` 出现 ≥ 2(定义 + run() 调用);phase-7 baseline 253 不回归。</human-check>
  </verify>
  <done>
    - 9 个 F-接线单测在 tests/test_validation_runner.py 入帐并全绿
    - `_run_stage` + `run()` 调用点透传 delta_portfolio + live_skill_root
    - 删除 `_delta_portfolio_empty` D-18 anchor;新 `delta_portfolio: list[DeltaPortfolioRow] = []` 起始空
    - pytest 全套绿,无 phase-7 回归
    - 单测中无 hardcoded seed delta;`proposed_change` 字段在测试 fixture 里来自显式 DeltaProposal,**禁止** test fixture 复用 production hardcoded constant
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stage1 trace → distill agent | trace 含 model output(可能含 PII / prompt leakage);distill agent prompt 把它直接送回 DeepSeek。已有 evidence_writer 的 mirror redaction(CR-03)在 capture 层处理,_build_trajectory_payload 不再做二次 redaction(不引入新 mutation 点) |
| distill agent output → portfolio | DeltaProposal.proposed_change 是 raw text 将被写到 live skill replacement 路径;但 trial 在 temp 副本上跑,live SKILL.md 不被修改 |
| portfolio_row → SkillDeltaPatch | sha256 验证 in run_request_trial(已有契约);若 live SKILL 在 distill 与 trial 之间被改,patch.original_text_sha256 mismatch → run_request_trial 内部 raise(已有路径) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | distill agent 产恶意 `replacement_text` 删 SKILL 关键行 | accept | trial 在 temp 副本上跑,live SKILL 不动;phase 8 不 propagate trial 结果回 live —— D-22d 边界保护 |
| T-08-05-02 | Information Disclosure | distill agent prompt 把整 trace(含 API 响应)送回 provider,潜在 prompt-injection 把 trace 内容回流 | accept | trace 已经过 capture 层 redaction(CR-03);LLM-side prompt injection 是 phase 6 evolution 设计的已知边界,phase 8 不重新设计 |
| T-08-05-03 | Repudiation | trial 跑了但 portfolio 没更新 (update_after_trial 未调用) | mitigate | M5 metric 强制阻塞:`trial_belief_update_count > 0`(plan 08-07);单测 `test_run_one_request_fires_trial_when_portfolio_nonempty` 间接覆盖 sample_count > 0 |
| T-08-05-04 | DoS | distill agent 产 1000 proposals,trial loop 在每请求跑 1000 次 trial → token 燃尽 | accept | distill skill 自身约束 proposal 数量(SKILL.md prompt 端);phase 8 不强制 portfolio 上限(scope creep);Stage 1 单 trace 难以产出爆炸式 proposals |
| T-08-05-05 | Tampering | 测试 fixture 复用 production hardcoded seed delta 绕开 forbid_list | mitigate | task 3 done 项显式禁止;code review 检查 test fixture 的 DeltaProposal 实例化字面值,确认来自测试本地变量 |
</threat_model>

<verification>
- pytest 全套绿,新增 9 个 F-接线测试入帐
- grep 完整性:`_distill_after_stage1`、`_build_trajectory_payload`、`_patch_from_portfolio_row` 各定义 1 次;`update_after_trial` 调用 ≥ 1;`delta_portfolio` 在 runner.py 出现 ≥ 4 处
- 无 hardcoded seed delta:grep `phase-8-seed`、`seed-001`、`DeltaProposal\(.*delta_id="seed` 在 runner.py 内 == 0
- 实测证据(post-batch,D8-ACC-2):
  - Stage 1 后 log 含 `node=distill_after_stage1` 的 tool_loop_summary 行
  - `delta_portfolio` 在 Stage 1 结束时 ≥ 1 row(由 agent 决定数量)
  - Stage 2/3 至少一个 `evolution_snapshot.json` 含 `trials[]` 非空,`delta_id` 来源于 distill artifact 而非 hardcoded
  - portfolio rows 的 `sample_count > 0`(M5 数据源)
</verification>

<success_criteria>
- D8-F1/F2/F3 全部落地:_run_one_request 在非空 portfolio 上跑 trial;Stage 1 后 distill agent 算法提取 delta(C4 路径);events 流到 evolution_snapshot.json
- D8-ACC-2:`trials[]` 至少一个非空(假阳性下限;真阳性靠 plan 08-07 M4/M5)
- 真阳性边界严守:distill agent 0 proposals 时 portfolio 保持空,无 fallback,**不为 acceptance 造假**
- 算法路径 vs 启发式:assemble_portfolio (set-merge)、update_after_trial (counter)、_build_trajectory_payload (file-read)、_patch_from_portfolio_row (sha256 + dispatch by change_type),全 pure transform
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-05-SUMMARY.md`,记录 commit SHA、3 个 helper 函数 + _run_one_request 签名扩展 + 9 个新测试列表 + grep 验收。
</output>
