---
phase: 08-evolution-wiring-and-runner-debt
plan: 09
type: execute
wave: 6
depends_on:
  - 08-08
files_modified:
  - seers_harness/validation/runner.py
  - tests/test_validation_runner.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-G-WR-01:Stage 3 fail-fast 在退出前 drain 已在跑的 in-flight futures,确保 disk artifacts 与 index.json 在 c=20 并发场景下数量一致。贡献 D8-ACC-1(Stage 3 fail-fast 时 batch artifact 完整性)+ D8-ACC-4(drain 路径单测)。"
  truths:
    - "Stage 3 fail-fast 触发后,already-running 的 future 跑完(或 raise)再 break,完成的 record 入 records 列表"
    - "尚未开始的 future 走 future.cancel(),cancelled future 不产生 record(disk 上也无 artifact)"
    - "drain 出的失败 record 也填 failure_class(exc) per plan 08-03 的 7-enum 路由"
    - "原始 fail-fast exception (failure_exc) 不被 drain 出的 in-flight exception 覆盖(因果链保留)"
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "Stage 3 fail-fast 路径下 drain 逻辑替换原 cancel + break"
      contains: "as_completed"
  key_links:
    - from: _run_stage Stage 3 fail-fast branch
      to: concurrent.futures.as_completed
      via: "drain remaining = [f for f in future_to_rid if not f.done()]; for f in as_completed(remaining): ..."
      pattern: "as_completed\\(remaining\\)"
    - from: drain branch failure record
      to: failure_class
      via: "drain 出的 exception 也走 failure_class(drain_exc) per E"
      pattern: "failure_class\\(drain_exc\\)"
  forbid_list:
    - "禁止 把 already-running future 强制 kill —— concurrent.futures.Future.cancel 文档原话 '仅取消未开始',强 kill 没有标准接口"
    - "禁止 用 future.cancel() 然后 future.result(timeout=N) 等 N 秒就放弃 —— 简化 timeout 行为,要 cleanly drain"
    - "禁止 在 drain 中覆盖 failure_exc —— 第一个失败原因是 fail-fast 触发因,drain 出的失败是次因(虽都入 records 但 failure_exc 不变)"
    - "禁止 在 drain 中 catch 完异常然后转 trial_failure —— D-19 路由禁止;drain 的 exception 走 failure_class 7 enum 路由"
---

<objective>
落地 D8-G-WR-01:Stage 3 fail-fast 在 cancel 未开始的 futures 后,等待已经在跑的 future 跑完(或 raise),收集 drain record 再 break。解决 phase-7 0526 "disk-vs-index 不一致"问题(`index.json` n=20 但 disk 上可能 19 或 21 个 request_dir)。

Purpose: D8-G-WR-01 in `07-WRIN-TRIAGE.md` deferred 项;F 接线之后 Stage 3 c=20 真跑 trial,fail-fast 路径更易触发,disk-vs-index 不一致会污染 batch 证据。本 plan 让 drain 干净。
Output: 单一 atomic commit;runner.py `_run_stage` Stage 3 fail-fast 分支替换 cancel+break 为 drain 逻辑;tests/test_validation_runner.py 新增 1 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stage 3 fail-fast 路径 drain in-flight futures + 1 个单测</name>
  <files>seers_harness/validation/runner.py, tests/test_validation_runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 670-690 `_run_stage` 的 Stage 3 fail-fast 路径;特别 line 679-683 当前 cancel + break 块;同文件 `_run_stage` 顶部确认 `as_completed` 是否已 import)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-WR-01 完整伪代码)
    - seers_harness/validation/exception_classifier.py(plan 08-03 新增的 failure_class —— drain 出失败 record 调它)
    - tests/test_validation_runner.py(找 ScriptedProvider + ThreadPoolExecutor mock 模式)
  </read_first>
  <behavior>
    - Test `test_stage3_fail_fast_drains_inflight`:
      - 构造 stage 3 c=20 / n=20 fixture,inject 一个 ScriptedProvider:第 5 个请求 raise ProviderAuthError(触发 fail-fast),其他 19 个 sleep 短暂后正常完成
      - 当 fail-fast 触发时,5 之外的 future 可能已经 done(完成态)或 still running
      - 调用 `_run_stage` → 断言:
        - records 长度 == 20(包括 fail-fast 自身的失败 + 已 done 的成功 + drain 出的 in-flight 完成)
        - records 里有一条 `failure_class=="auth"` 的 record(原始 fail-fast)
        - failure_exc 是 ProviderAuthError(不被 drain 出的其他 exception 覆盖)
        - cancelled future 不产生 record(disk 上也无 artifact)
      - 用 monkeypatch / mocked ThreadPoolExecutor 让测试可控;或 ScriptedProvider + `time.sleep` 配合让顺序可预测
  </behavior>
  <action>
    在 `seers_harness/validation/runner.py` `_run_stage` 函数的 Stage 3 fail-fast 路径(line 679-683 附近,搜 `for other in future_to_rid: other.cancel(); break`):

    1. 确认 `from concurrent.futures import as_completed` 已 import;若未 import 在文件顶部添加。
    2. 替换 cancel + break 块为 RESEARCH §4-WR-01 伪代码:
       ```
       records.append(fail_record)
       failure_exc = exc
       print(... "fail-fast", file=sys.stderr)
       traceback.print_exc(file=sys.stderr)
       # WR-01: drain in-flight futures so disk and index.json agree.
       remaining = [f for f in future_to_rid if not f.done()]
       for f in remaining:
           f.cancel()
       for f in as_completed(remaining):
           rid_drain = future_to_rid[f]
           if f.cancelled():
               continue  # 未开始的,disk 上也没有 artifact
           try:
               record = f.result()
               records.append(record)
           except BaseException as drain_exc:
               records.append({
                   "node_id": _safe_request_dirname(rid_drain),
                   "request_id": rid_drain,
                   "artifact": None,
                   "reflow_triggered": False,
                   "trial_selected_delta_id": None,
                   "exception": safe_exc(drain_exc),
                   "failure_class": failure_class(drain_exc),  # E + plan 08-03 接线
               })
       break
       ```
    3. **failure_class 必须 import**(若 plan 08-03 已加在文件顶部 from exception_classifier,复用;否则 grep 确认 import 路径)。
    4. **禁止** 覆盖 `failure_exc` —— 它保留原始 fail-fast 触发因,即使 drain 出更多 exception 都不动 `failure_exc`。
    5. **禁止** 在 drain 路径加 `flush_evidence` / `write_evolution_snapshot` —— 这些已在 `_run_one_request` 的 finally 块跑过(被 drain 的 future 各自完成时自动 finally),WR-01 不重复调。
    6. 单测追加到 `tests/test_validation_runner.py`。测试需要构造能让 fail-fast 早于全部完成时触发的 scripted provider —— 用 `threading.Event` 配合 ScriptedProvider 可控 future 完成顺序。
  </action>
  <verify>
    <automated>pytest -q tests/test_validation_runner.py -k "stage3_fail_fast_drains" -x 2>&1 | tail -15 ; grep -c "as_completed(remaining)" seers_harness/validation/runner.py ; grep -c "failure_class(drain_exc)" seers_harness/validation/runner.py</automated>
    <human-check>新测全绿;`as_completed(remaining)` 出现 == 1;`failure_class(drain_exc)` 出现 == 1。</human-check>
  </verify>
  <done>
    - 新测 `test_stage3_fail_fast_drains_inflight` 入帐并全绿
    - cancel + as_completed drain 替换原 cancel + break
    - drain record 含 failure_class(per plan 08-03 路由)
    - failure_exc 不被 drain 覆盖(因果链保留)
    - phase-7 baseline 253 测试无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThreadPoolExecutor ↔ runner state | already-running future 持有自己 _run_one_request 上下文;drain 时只读 future.result(),不动 worker 内部 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | DoS | 一个 in-flight future 永不完成(死锁 / 远端超时无返回)→ drain 永久挂起 | accept | provider 已有 timeout(180s per A);drain 等待时间上限 = 单请求 timeout × 剩余并发数,Stage 3 c=20 worst-case 180s,可接受。若日后需 timeout drain,follow-up phase |
| T-08-09-02 | Tampering | drain 出的 exception 被误标 failure_class="auth" 实则非 auth | mitigate | failure_class 是 plan 08-03 7-enum 路由,基于 isinstance + cause-chain walk,不依赖人工分类 |
</threat_model>

<verification>
- pytest 全套绿,新测入帐
- grep `as_completed(remaining)` 在 runner.py 出现 == 1
- 实测证据(post-batch,D8-G-WR-01):IF Stage 3 fail-fast 自然触发 → `len(index.json["requests"]) == 20` AND disk `stage3/<rid>/` 目录数 == 20;若无自然 fail-fast → pytest 是唯一证据
</verification>

<success_criteria>
- D8-G-WR-01 落地:Stage 3 fail-fast 后 disk-vs-index 一致
- drain 路径走 failure_class 7-enum 路由,与 plan 08-03 一致
- 原始 fail-fast 因果链保留
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-09-SUMMARY.md`。
</output>
