---
phase: 08-evolution-wiring-and-runner-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - seers_harness/provider_runtime/openai_compatible.py
  - tests/test_provider_openai_compatible.py
autonomous: true
requirements: []
must_haves:
  goal: "把 DEEPSEEK_TIMEOUT_SECONDS 默认值 60→180s,使 deepseek-v4-pro reasoning model 在 TTFB 60-180s 之间不再误超时,贡献 D8-ACC-1(零请求因 60s 超时被丢)"
  truths:
    - "无 env 覆盖时 deepseek_provider_from_env() 构造的 client.timeout == 180.0"
    - "deepseek_runtime_facts() 返回 default_timeout_seconds == 180"
    - "180 不是散落 magic number,而是模块级常量(_DEFAULT_TIMEOUT_SECONDS)"
  artifacts:
    - path: seers_harness/provider_runtime/openai_compatible.py
      provides: "180s timeout 默认值 + 单一来源常量"
      contains: "_DEFAULT_TIMEOUT_SECONDS"
  key_links:
    - from: deepseek_provider_from_env
      to: _DEFAULT_TIMEOUT_SECONDS
      via: "fallback when DEEPSEEK_TIMEOUT_SECONDS env unset"
      pattern: "_DEFAULT_TIMEOUT_SECONDS"
    - from: deepseek_runtime_facts
      to: _DEFAULT_TIMEOUT_SECONDS
      via: "PROD-02 探针读同一常量"
      pattern: "default_timeout_seconds.*_DEFAULT_TIMEOUT_SECONDS"
  forbid_list:
    - "禁止把 180 写成两处独立 literal —— 必须用单一模块级常量,避免 future drift"
    - "禁止改 SDK max_retries(D-03 锁定为 0)"
    - "禁止改 connect 超时(DeepSeek beta TLS < 5s 从未观察到瓶颈)"
---

<objective>
落地 D8-A:把 `seers_harness/provider_runtime/openai_compatible.py` 中 DeepSeek HTTP 超时默认值从 60s 提到 180s,以承载 deepseek-v4-pro + reasoning_effort=max 的 TTFB(0526 trace 观测最坏 ≈110s,180s 给 ~60% headroom)。

Purpose: D8-ACC-1 要求实测 batch 零请求被 60s 默认值误杀 —— 当前默认是 phase-7 reopen 的根因 RC-01。
Output: 一个 atomic commit,改动 `openai_compatible.py:131, 141` + 新增 1 个模块级常量 `_DEFAULT_TIMEOUT_SECONDS = 180`,新增 2 个单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/provider_runtime/openai_compatible.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 抽 _DEFAULT_TIMEOUT_SECONDS=180 单一来源常量,改默认值 60→180,加 2 个单测</name>
  <files>seers_harness/provider_runtime/openai_compatible.py, tests/test_provider_openai_compatible.py</files>
  <read_first>
    - seers_harness/provider_runtime/openai_compatible.py(整文件,聚焦 line 120-148:`deepseek_runtime_facts` 与 `deepseek_provider_from_env`)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md(D8-A 决策、D-03 SDK max_retries 不动)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-A 修改点 + 陷阱清单)
    - tests/test_provider_openai_compatible.py(找现有 timeout / runtime_facts 测试位点)
  </read_first>
  <behavior>
    - Test 1 `test_deepseek_provider_default_timeout_180s`:`monkeypatch.delenv("DEEPSEEK_TIMEOUT_SECONDS", raising=False)` + `monkeypatch.setenv("DEEPSEEK_API_KEY", "...")`,调用 `deepseek_provider_from_env()`,断言返回 provider 内部 `client.timeout == 180.0`(或对应 SDK 字段)。
    - Test 2 `test_deepseek_runtime_facts_default_timeout_180s`:断言 `deepseek_runtime_facts()["default_timeout_seconds"] == 180`(整数 180,不是 60)。
    - Test 3(可选辅助)`test_deepseek_provider_env_override_still_works`:`monkeypatch.setenv("DEEPSEEK_TIMEOUT_SECONDS", "240")` → `client.timeout == 240.0`(env override 仍生效)。
  </behavior>
  <action>
    在 `seers_harness/provider_runtime/openai_compatible.py` 顶部(import 之后、`deepseek_runtime_facts` 之前)新增一个模块级常量 `_DEFAULT_TIMEOUT_SECONDS: int = 180`,作为 D8-A per 08-RESEARCH.md §2-A 的单一来源。改写两处:(1) `deepseek_runtime_facts` 中 `"default_timeout_seconds": 60` → `"default_timeout_seconds": _DEFAULT_TIMEOUT_SECONDS`(line 131);(2) `deepseek_provider_from_env` 中 `os.environ.get("DEEPSEEK_TIMEOUT_SECONDS", "60")` → `os.environ.get("DEEPSEEK_TIMEOUT_SECONDS", str(_DEFAULT_TIMEOUT_SECONDS))`(line 141)。**严禁** 把 180 字面值在两处分别硬编码 —— 见 RESEARCH §2-A "陷阱"。SDK `max_retries` 路径(D-03)零改动,签名 `deepseek_provider_from_env(*, model, timeout_seconds, max_retries)` 保持不变。在 `tests/test_provider_openai_compatible.py` 新增 Test 1/2/3,沿用该文件的 monkeypatch fixture 模式。
  </action>
  <verify>
    <automated>pytest -q tests/test_provider_openai_compatible.py -x 2>&1 | tail -20 ; grep -c "_DEFAULT_TIMEOUT_SECONDS" seers_harness/provider_runtime/openai_compatible.py ; grep -c '"60"' seers_harness/provider_runtime/openai_compatible.py</automated>
    <human-check>
      grep `_DEFAULT_TIMEOUT_SECONDS` 出现次数 ≥ 3(1 次定义 + 2 次引用);grep `"60"` 在 openai_compatible.py 中出现次数 == 0(原 60 字面值已全部替换)。
    </human-check>
  </verify>
  <done>
    - pytest 退出码 == 0 且新增 2-3 个单测在 `tests/test_provider_openai_compatible.py` 中绿
    - `grep -c "_DEFAULT_TIMEOUT_SECONDS" seers_harness/provider_runtime/openai_compatible.py` ≥ 3
    - `grep -c '"60"' seers_harness/provider_runtime/openai_compatible.py` == 0
    - `grep -E "default_timeout_seconds.*60" seers_harness/provider_runtime/openai_compatible.py` 0 hits
    - 无 SDK max_retries / classify_exception / connect 超时相关改动(D-03 + D-22 锁定)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→DeepSeek HTTPS | TTFB 在 60-180s 间是 reasoning model 正常工作,不是攻击向量;改默认值不引入新的信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Denial of Service | DeepSeek 端故意拖慢响应使 client 资源占用 | accept | 180s 是单请求超时上限;Stage 3 c=20 worker 独立线程,慢请求不阻塞他人;deferred 章节明确 circuit-breaker 不在 phase 8 范围 |
| T-08-01-02 | Tampering | env DEEPSEEK_TIMEOUT_SECONDS 被恶意置极大值 | accept | env override 行为既有契约(D-22),非本 plan 引入 |
</threat_model>

<verification>
- pytest 全套绿(集成层):`pytest -q` 退出码 0,新增测试入帐
- grep 完整性:`grep -c "_DEFAULT_TIMEOUT_SECONDS" seers_harness/provider_runtime/openai_compatible.py` ≥ 3
- 实测证据(post-batch,D8-VAL-REAL):phase-8 final batch 中至少一个请求 `usage.json` 总耗时 60s < t < 180s 且 `index.json` 该 row `exception is null`
</verification>

<success_criteria>
- D8-A 决策落地:默认 timeout = 180s,可被 env 覆盖,SDK max_retries 不变
- 单元层:3 个新测全绿;phase-7 baseline 253 测试无回归
- 真阳性边界:不只是 "180 出现在代码",而是构造的 client 实例 timeout 字段 == 180.0
- M1-M5 中无依赖此 plan 的 metric;此 plan 仅服务 D8-ACC-1 的"零请求被 60s 误杀"条件
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-01-SUMMARY.md`,记录 commit SHA、改动文件、新增测试名称、grep verify 结果。
</output>
