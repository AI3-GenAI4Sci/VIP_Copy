---
phase: 08-evolution-wiring-and-runner-debt
plan: 10
type: execute
wave: 6
depends_on:
  - 08-09
files_modified:
  - seers_harness/validation/runner.py
  - tests/test_validation_runner.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-G-WR-02:_run_one_request 的 finally 块 best-effort 包装 flush_evidence / write_evolution_snapshot,使清理失败不掩盖原始 try 块的异常(Python finally 语义反模式修复)。贡献 D8-ACC-4(finally writer 失败时原始 exc 仍 propagate 的 pytest 单测)。"
  truths:
    - "finally 块中 flush_evidence raise 时,原始 try 块的异常仍 propagate 到 caller(因 cleanup 被 try/except 包住)"
    - "finally 块中 write_evolution_snapshot raise 时,原始 try 块的异常仍 propagate 到 caller"
    - "cleanup 失败被 print 到 stderr(可观测)但不阻塞"
    - "_cv.reset(token) 的 try/except 兜底 *不变*(plan 08-04 已迁移到 reset_current_node_id helper,其 try/except 兜底语义保留)"
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "_run_one_request finally 块的 flush_evidence + write_evolution_snapshot 各裹 try/except,print 错误到 stderr 后继续"
      contains: "flush_evidence failed for"
  key_links:
    - from: _run_one_request finally
      to: flush_evidence
      via: "try/except 包,except 内 print 错误"
      pattern: "flush_evidence failed for"
    - from: _run_one_request finally
      to: write_evolution_snapshot
      via: "try/except 包,except 内 print 错误"
      pattern: "write_evolution_snapshot failed for"
  forbid_list:
    - "禁止 把原始 try 块的异常 swallow —— finally 中 cleanup 失败只 print 到 stderr,原始异常照常 propagate"
    - "禁止 把 cleanup 失败转 fail-fast —— cleanup 是 best-effort,phase 7 IN-04 已 defensive 化 writer 层,本 plan 只补 runner 端兜底"
    - "禁止 改 _cv.reset / reset_current_node_id 的 try/except 兜底语义(plan 08-04 已迁移)"
    - "禁止 在 cleanup 失败时调 traceback.print_exc —— safe_exc 提供 redacted message,不暴露完整 stack(可能含 secret)"
---

<objective>
落地 D8-G-WR-02:`_run_one_request` 的 finally 块把 `flush_evidence` / `write_evolution_snapshot` 各裹 try/except + print 错误到 stderr,使 cleanup 失败不掩盖原始 try 块的异常(修复 Python finally 反模式)。

Purpose: D8-G-WR-02 in `07-WRIN-TRIAGE.md` deferred 项;phase-7 IN-04 已在 writer 层加 None-guard,但 runner finally 端还可能因 disk full / permission denied 等环境问题 raise,需 best-effort 兜底。F 接线(plan 08-05)后 finally 还跑 `write_evolution_snapshot(events)` 把 trial events 落盘,失败概率更高。
Output: 单一 atomic commit;runner.py `_run_one_request` finally 块包 try/except;tests/test_validation_runner.py 新增 1 个测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: finally 块包 flush_evidence + write_evolution_snapshot 各 try/except + 1 个单测</name>
  <files>seers_harness/validation/runner.py, tests/test_validation_runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 515-535 `_run_one_request` finally 块;plan 08-04 已把 _cv.reset 迁移到 reset_current_node_id,plan 08-05 已扩 _run_one_request 签名;确认当前 finally 完整 shape)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-WR-02 完整伪代码)
    - tests/test_validation_runner.py(找 monkeypatch flush_evidence / write_evolution_snapshot fault-injection 范式)
  </read_first>
  <behavior>
    - Test `test_finally_writer_failure_does_not_mask_original`:
      - inject fake provider 让 _run_one_request 的 try 块 raise ProviderAuthError
      - monkeypatch `seers_harness.validation.runner.flush_evidence` 让其 raise PermissionError("test cleanup failure")
      - 调 _run_one_request → 断言:最终浮的异常是 ProviderAuthError(原始 try 块异常),capsys 捕获 stderr 含 `flush_evidence failed for` 行
      - 第二个 case:flush_evidence 成功但 write_evolution_snapshot 失败 → 原始 ProviderAuthError 仍 propagate,stderr 含 `write_evolution_snapshot failed for`
      - 第三个 case (happy path 验证):无 cleanup failure 时,原始 try 块成功 → cleanup 默静,无 failed 行
  </behavior>
  <action>
    在 `seers_harness/validation/runner.py` `_run_one_request` 的 finally 块,按 08-RESEARCH §4-WR-02 改写。

    当前(plan 08-04 落地后)形态:reset_current_node_id 部分已 try/except 兜底,flush_evidence + write_evolution_snapshot 是裸调用。

    目标形态:在 reset_current_node_id 兜底块之后,把 flush_evidence 和 write_evolution_snapshot 各包一层 try/except Exception 兜底,except 内 `print(f"[runner] {writer_name} failed for {request_id}: {safe_exc(cleanup_exc)}", file=sys.stderr)`,然后 continue(不 raise,不 traceback.print_exc)。

    **关键约束**:
    - `safe_exc(cleanup_exc)` 提供 redacted message;严禁 `traceback.print_exc()`(可能含 secret per forbid_list)
    - except 子句捕获 `Exception` 不是 `BaseException` — 不挡 KeyboardInterrupt
    - cleanup print 用 `request_id` 而非 full path
    - **保留** reset_current_node_id 的现有 try/except — plan 08-04 已迁移,不动其语义
    - log 文本与 RESEARCH §4-WR-02 给的字面值一致(`[runner] flush_evidence failed for ...`、`[runner] write_evolution_snapshot failed for ...`)— 单测 grep 这两个串

    单测追加到 `tests/test_validation_runner.py`,fault injection 通过 `monkeypatch.setattr("seers_harness.validation.runner.flush_evidence", _raise_perm_error)` 形式;辅助函数 `def _raise_perm_error(*a, **kw): raise PermissionError("test")`。
  </action>
  <verify>
    <automated>pytest -q tests/test_validation_runner.py -k "finally_writer_failure" -x 2>&1 | tail -15 ; grep -c "flush_evidence failed for" seers_harness/validation/runner.py ; grep -c "write_evolution_snapshot failed for" seers_harness/validation/runner.py ; grep -c "safe_exc(cleanup_exc)" seers_harness/validation/runner.py ; grep -c "traceback.print_exc" seers_harness/validation/runner.py</automated>
    <human-check>新测全绿;`flush_evidence failed for` 字面 grep == 1;`write_evolution_snapshot failed for` 字面 grep == 1;`safe_exc(cleanup_exc)` 出现 ≥ 2;runner.py 中 `traceback.print_exc` 出现次数与 plan 08-09 落地后一致(WR-02 不引入新 print_exc;plan 08-09 fail-fast 已有的不动)。</human-check>
  </verify>
  <done>
    - 新测 `test_finally_writer_failure_does_not_mask_original` 入帐并全绿(三个 case)
    - flush_evidence + write_evolution_snapshot 各包 try/except;cleanup 失败 print 到 stderr 后继续
    - 原始 try 块异常 propagate 不被覆盖
    - phase-7 baseline 253 测试无回归
    - reset_current_node_id 兜底语义不变
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleanup writer ↔ disk | disk full / permission denied 在生产中可能发生 → cleanup 失败应可观测但不阻塞 main flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10-01 | Information Disclosure | cleanup_exc 含 secret 间接 print 到 stderr | mitigate | 用 safe_exc 而非 traceback.print_exc;forbid_list 显式禁 print_exc |
| T-08-10-02 | Repudiation | cleanup 失败被 swallow 后无 audit | mitigate | print 含 request_id + redacted exc message,可后续 root-cause |
</threat_model>

<verification>
- pytest 全套绿,新测 3 个 case 入帐
- grep:`flush_evidence failed for` 与 `write_evolution_snapshot failed for` 各 1 次
- 实测证据(post-batch,D8-G-WR-02):pytest 是唯一证据(IN-04 后 writer 层 defensive,自然 batch 中罕见 cleanup 失败);若实测 batch 中出现 `flush_evidence failed` 行 = 反 IN-04 失效信号,作为新 finding 记录
</verification>

<success_criteria>
- D8-G-WR-02 落地:finally 块 best-effort 包装,原始异常 propagate 不被掩盖
- safe_exc redaction 保护;无 traceback.print_exc 引入
- reset_current_node_id 兜底语义不变(plan 08-04 锁定)
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-10-SUMMARY.md`。
</output>
