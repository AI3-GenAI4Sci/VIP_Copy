---
phase: 08-evolution-wiring-and-runner-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - seers_harness/validation/runner.py
  - tests/test_validation_runner.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-D:runner.py 增加 `--env-file <path>` CLI 标志,消除 shell ENV stale-key 故障模式(phase-7 RC-03),贡献 D8-ACC-1 的'零请求因 shell-env 漂移'条件"
  truths:
    - "runner 通过 --env-file 加载 .env.local 后,os.environ 含文件中所有 KEY=VALUE 对(文件优先于已有 env)"
    - "启动日志恰好 2 行 env-file 相关:'loaded N keys from <path>' + 'DEEPSEEK_API_KEY suffix=****<4chars>'"
    - "value 字符串绝对不出现在任何 stderr / stdout / index.json / batch_summary.json"
    - "文件不存在 → RuntimeError 非 0 退出码"
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "_load_env_file(path) helper + main() 中的 --env-file argparse + env merge 块"
      contains: "def _load_env_file"
  key_links:
    - from: main argparse
      to: _load_env_file
      via: "args.env_file is not None → _load_env_file(args.env_file)"
      pattern: "args\\.env_file"
    - from: _load_env_file
      to: os.environ
      via: "os.environ[key] = value(文件优先)"
      pattern: "os\\.environ\\[key\\]"
  forbid_list:
    - "禁止把 DEEPSEEK_API_KEY 或任何 value 写日志 —— 只写 suffix=****<last4>"
    - "禁止做 shell expansion($VAR、quote 剥除、`export FOO=bar` 语法都不支持)"
    - "禁止把 --env-file path 写到 index.json / batch_summary.json(可能含 PII)"
    - "禁止支持 nested env files(SOURCE_OTHER_ENV=other.env)—— scope creep"
---

<objective>
落地 D8-D:为 `seers_harness/validation/runner.py` 添加 `--env-file <path>` CLI 标志,在构造 provider 之前把文件 KEY=VALUE 合并进 `os.environ`。消除 phase-7 0526 RC-03(shell stale-key 401)。

Purpose: D8-ACC-1 要求 phase-8 final batch 用 `python -m seers_harness.validation.runner --env-file .env.local` 启动(无 shell `export`),且日志可见两行 env-file marker。安全要求:绝不泄露 key value。
Output: 一个 atomic commit,在 runner.py 中新增 `_load_env_file` helper + main argparse 块 + 启动日志两行,新增 5 个单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 _load_env_file + --env-file CLI + 启动日志 + 5 个单测</name>
  <files>seers_harness/validation/runner.py, tests/test_validation_runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(全文,聚焦 line 223-280 的 CLI argparse 块、line 800+ 的 main() 入口、line 244 附近 _default_deepseek_factory)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-D 完整伪代码 + 安全清单)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md(D8-D + D8-VAL-REAL)
    - tests/test_validation_runner.py(找现有 CLI / monkeypatch 测试位点)
  </read_first>
  <behavior>
    - Test 1 `test_env_file_overrides_existing_env`:monkeypatch.setenv("FOO","old"),tmp_path 写文件 `FOO=new`,调用 `_load_env_file(path)` → 返回 1 且 `os.environ["FOO"] == "new"`。
    - Test 2 `test_env_file_does_not_log_values`:tmp_path 写 `DEEPSEEK_API_KEY=sk-secret-1234abcd`,通过 main() 入口启动,capsys 捕获 stderr;断言 stderr 不含子串 "sk-secret-1234abcd" 且不含完整 value,只含 `loaded 1 keys from` 和 `suffix=****abcd`。
    - Test 3 `test_env_file_handles_comments_and_blank`:文件含 `#comment` 行、空行、`FOO=bar` → `_load_env_file` 返回 1,只 merge 1 key。
    - Test 4 `test_env_file_missing_raises`:不存在的 path → RuntimeError。
    - Test 5 `test_env_file_no_shell_expansion`:文件 `BAR=$FOO` → `os.environ["BAR"] == "$FOO"` 字面值(无展开)。
  </behavior>
  <action>
    实现 per 08-RESEARCH.md §2-D 伪代码:
    1. 在 runner.py 顶层 helper 段(`_safe_request_dirname` 附近)新增 `def _load_env_file(path: Path) -> int`,行为 verbatim 取自 RESEARCH §2-D 伪代码:`path.read_text(encoding="utf-8").splitlines()` 逐行处理,strip 后 skip 空 / 以 `#` 起头 / 不含 `=` 的行,`line.partition("=")` 取 KEY=VALUE,无 shell expansion,无 quote 剥除,`os.environ[key] = value`(覆盖既有),返回 merged 计数。文件不存在 raise `RuntimeError(f"--env-file path not found: {path}")`。
    2. 在 main argparse 块(line 823 附近)新增 `parser.add_argument("--env-file", type=Path, default=None, help="Path to KEY=VALUE env file (no shell expansion)")`。注意:**不要** 在 argparse help 里举例 `.env.local`(防止 path-leak 安全联想)。
    3. 在 main() 内 argparse 解析之后、`run()` 调用之前(在构造任何 provider 之前)插入:`if args.env_file is not None: count = _load_env_file(args.env_file); suffix = (os.environ.get("DEEPSEEK_API_KEY") or "<unset>")[-4:]; print(f"[runner] env-file: loaded {count} keys from {args.env_file}", file=sys.stderr); print(f"[runner] env-file: DEEPSEEK_API_KEY suffix=****{suffix}", file=sys.stderr)`。**严格只 2 行 print,不要 INFO / DEBUG / structlog**。
    4. 测试位置:新增到 `tests/test_validation_runner.py`(沿用其现有 monkeypatch + tmp_path fixture 模式);test 2 用 `argparse.Namespace` 或直接 invoke main 的 fixture,避免真起 provider(因为 D8-A 之后 timeout 改了,但此 plan 不依赖 provider 实际构造,只验证 env merge + log 行为)。
    
    **安全 checklist**(逐项 review):value 不进任何 print / log / artifact;path 只到 stderr 不到 JSON;不解析 `export X=Y` shell 语法;不剥 quote;不展开 `$VAR`;不支持 nested。**禁止** 把 value 当 PII 处理后留在 trace 里(`safe_exc` 已处理 provider error 中的 key,但此路径下我们直接不写 value 到任何地方)。
  </action>
  <verify>
    <automated>pytest -q tests/test_validation_runner.py -k "env_file" -x 2>&1 | tail -25 ; grep -c "_load_env_file" seers_harness/validation/runner.py ; grep -c "args.env_file" seers_harness/validation/runner.py</automated>
    <human-check>
      5 个新测全绿;`_load_env_file` 在 runner.py 出现 ≥ 2 次(定义 + 调用);`args.env_file` 出现 ≥ 1 次。手 grep `print.*value` / `print.*api_key` 应为 0 hits(value 不写日志)。
    </human-check>
  </verify>
  <done>
    - pytest 退出码 == 0,5 个新测在 tests/test_validation_runner.py 中入帐
    - `grep -c "_load_env_file" seers_harness/validation/runner.py` ≥ 2
    - `grep -c "args.env_file" seers_harness/validation/runner.py` ≥ 1
    - `grep -c -E '(loaded.*keys from|suffix=\*\*\*\*)' seers_harness/validation/runner.py` >= 2(两行 log)
    - 手 review:no string literal 写完整 key value 到 print / json
    - phase-7 baseline 253 测试无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→process env | env-file 内容跨越文件→进程边界;value 可能含 secret |
| process→stderr/stdout | 日志可能被 nohup/journalctl 持久化 → key value 一旦泄露则持久泄露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | DEEPSEEK_API_KEY value 写入 stderr / artifact | **mitigate** | log 只输出 `suffix=****<4>`;_load_env_file 内不 print 任何 value;5 个单测有专测 `test_env_file_does_not_log_values` 强制 capsys 断言;手 review checklist 入 `<action>` |
| T-08-02-02 | Information Disclosure | env-file path 写入 index.json / batch_summary.json(path 可能含 user home,亦视为 PII) | **mitigate** | args.env_file 不进 json artifact,只到 stderr;writer 层(index_writer / batch_summary_writer)零改动,无 path 入口 |
| T-08-02-03 | Tampering | 文件路径被 trick 为 symlink 指向 /etc/shadow 之类 | accept | 读 KEY=VALUE 格式;不含 `=` 行被 skip;读非 env-shape 文件最多产生 0 merged keys(无副作用) |
| T-08-02-04 | Repudiation | 操作员声称"我加载了正确的 key" | mitigate | suffix=****<4> log 提供事后审计锚点 |
</threat_model>

<verification>
- pytest 5 个新测全绿
- grep 完整性如上
- 实测证据(post-batch):phase-8 final batch 命令 `python -m seers_harness.validation.runner --env-file .env.local`,`.run-logs/runner-<ts>.log` 前几行可见两条 env-file marker;DEEPSEEK_API_KEY suffix 与 .env.local 当前值一致
</verification>

<success_criteria>
- D8-D 决策落地:`--env-file` flag 工作,文件覆盖既有 env,no shell expansion
- 安全:value 不泄露(单测强制 + 手 review)
- D8-ACC-1 unblock 的关键一环:RC-03 (stale shell env) 不再可能发生
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-02-SUMMARY.md`。
</output>
