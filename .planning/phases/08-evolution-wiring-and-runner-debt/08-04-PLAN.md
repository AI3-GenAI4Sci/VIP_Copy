---
phase: 08-evolution-wiring-and-runner-debt
plan: 04
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - seers_harness/validation/runner.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 G 组三件 runner.py 纯清理(WR-03 + WR-04 callsite + IN-08),让 runner.py 在 F 接线之前先扫干净 dead code / forbid-list 绕道。贡献 D8-ACC-1(为 wave 3 F 接线奠基)+ D8-ACC-4(grep verify + pytest baseline 不回归)。"
  truths:
    - "runner.py 中 _detect_delimiter 出现次数 == 0(canonical 来自 intake.request_preprocessor.detect_delimiter)"
    - "runner.py 中 `_current_node_id as _cv` 出现次数 == 0(改用 reset_current_node_id 公开 helper,WR-04 callsite 完成)"
    - "runner.py 中 `_PROVIDER_BUDGET_KEY` 与 `\"max_\" + \"retries\"` 出现次数都 == 0;`deepseek_provider_from_env(max_retries=3)` 出现次数 == 1(IN-08 反 forbid-list scan-evasion)"
  artifacts:
    - path: seers_harness/validation/runner.py
      provides: "三件清理后的 runner,无 dup helper / 无私有 ContextVar 越界 / 无 forbid-list 字符串拼接"
  key_links:
    - from: runner.py _build_scratch_csv
      to: detect_delimiter
      via: "from seers_harness.intake.request_preprocessor import detect_delimiter"
      pattern: "detect_delimiter\\(csv_path\\)"
    - from: runner.py _run_one_request finally
      to: reset_current_node_id
      via: "from seers_harness.validation.recording_provider import reset_current_node_id; reset_current_node_id(token)"
      pattern: "reset_current_node_id\\(token\\)"
    - from: runner.py _default_deepseek_factory
      to: deepseek_provider_from_env
      via: "kwarg max_retries=_RUNNER_PROVIDER_MAX_RETRIES"
      pattern: "deepseek_provider_from_env\\(max_retries"
  forbid_list:
    - "禁止保留 `_PROVIDER_BUDGET_KEY` 或任何 `\"max_\" + \"retries\"` 形式的字符串拼接(IN-08 反 forbid-list scan-evasion)"
    - "禁止再 import 私有 `_current_node_id` ContextVar(WR-04)"
    - "禁止保留 runner.py 中重复实现的 `_detect_delimiter`(WR-03)"
---

<objective>
落地 G 组三件 runner.py 纯清理(WR-03、WR-04 callsite、IN-08),作为 F 接线前的清理 sweep。三者都只触 `runner.py`,且都 grep-verifiable —— 单 commit 一次落地。

Purpose: F 接线(plan 08-05)将对 `_run_one_request` 大改;先扫掉 dead code 和 forbid-list 绕道,降低 F 的改动复杂度。这三件无功能性逻辑,只是回归到既定 contract。
Output: 单一 atomic commit;runner.py 三处替换 + grep 验收。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/runner.py
@seers_harness/intake/request_preprocessor.py
@seers_harness/validation/recording_provider.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-03 — 删除重复 _detect_delimiter,改用 intake 中 canonical detect_delimiter</name>
  <files>seers_harness/validation/runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 270-360 — `_build_scratch_csv` + line 350 dup `_detect_delimiter`,以及 line 273 既有但未用的 import)
    - seers_harness/intake/request_preprocessor.py(确认 `detect_delimiter` 公开签名 + 行为)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-WR-03)
  </read_first>
  <action>
    在 `seers_harness/validation/runner.py`:
    1. 删除 line 350 附近的 `def _detect_delimiter(...)` 整函数(8-9 行)。
    2. 在 `_build_scratch_csv` 函数体中找到 `delimiter = _detect_delimiter(csv_path)` 一行,改为 `delimiter = detect_delimiter(csv_path)`(去掉 `_` 前缀)。
    3. 确认 line 273 的 `from seers_harness.intake.request_preprocessor import detect_delimiter, preprocess_request_from_csv` import 仍然存在(此前是 dead import,现在被使用)。
    4. **不要** 改 `preprocess_request_from_csv` 用法或其他 `intake` 模块的依赖关系。
  </action>
  <verify>
    <automated>grep -c "_detect_delimiter" seers_harness/validation/runner.py ; grep -c "detect_delimiter(csv_path)" seers_harness/validation/runner.py ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "_detect_delimiter" seers_harness/validation/runner.py` == 0
    - `grep -c "detect_delimiter(csv_path)" seers_harness/validation/runner.py` == 1
    - `tests/test_validation_runner.py` 全绿(既有 `_build_scratch_csv` 路径单测无回归)
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-04 callsite — 改用 reset_current_node_id 公开 helper</name>
  <files>seers_harness/validation/runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 515-535 — `_run_one_request` finally 块的 `_cv.reset(token)` 调用)
    - seers_harness/validation/recording_provider.py(确认 `reset_current_node_id` 在 aa49f06 commit 中作为公开 API 落地)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-WR-04)
  </read_first>
  <action>
    在 `seers_harness/validation/runner.py` `_run_one_request` 的 finally 块,把当前形态:
    ```
    try:
        from seers_harness.validation.recording_provider import _current_node_id as _cv
        _cv.reset(token)
    except Exception:
        pass
    ```
    替换为:
    ```
    try:
        from seers_harness.validation.recording_provider import reset_current_node_id
        reset_current_node_id(token)
    except Exception:
        pass
    ```
    保留 `try/except Exception: pass` 的兜底语义不变(节流 finally 失败的既有契约)。**禁止** 删除该 try/except —— phase 8 plan 08-10 (WR-02) 才动 finally 的 wrap 策略。
  </action>
  <verify>
    <automated>grep -c "_current_node_id as _cv" seers_harness/validation/runner.py ; grep -c "reset_current_node_id(token)" seers_harness/validation/runner.py ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `grep -c "_current_node_id as _cv" seers_harness/validation/runner.py` == 0
    - `grep -c "reset_current_node_id" seers_harness/validation/runner.py` ≥ 1
    - `pytest -q` 既有套件无回归
  </done>
</task>

<task type="auto">
  <name>Task 3: IN-08 — 把 max_retries=3 提到 deepseek_provider_from_env kwarg,删除 forbid-list scan-evasion</name>
  <files>seers_harness/validation/runner.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 238-260 — `_PROVIDER_BUDGET_KEY = "max_" + "retries"` + `_PROVIDER_CTOR_KWARGS` + `_default_deepseek_factory`)
    - seers_harness/provider_runtime/openai_compatible.py(line 137 — `deepseek_provider_from_env(*, model, timeout_seconds, max_retries)` 既有 kwarg)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§4-IN-08)
  </read_first>
  <action>
    在 `seers_harness/validation/runner.py`:
    1. 删除 `_PROVIDER_BUDGET_KEY` 与 `_PROVIDER_CTOR_KWARGS` 两个模块级常量(line 238-250 附近)。
    2. 引入新模块级常量 `_RUNNER_PROVIDER_MAX_RETRIES: int = 3  # D-03 budget`。
    3. 改写 `_default_deepseek_factory`:
       ```
       def _default_deepseek_factory() -> Any:
           from seers_harness.provider_runtime.openai_compatible import deepseek_provider_from_env
           return deepseek_provider_from_env(max_retries=_RUNNER_PROVIDER_MAX_RETRIES)
       ```
    4. 检查 module-level docstring 的 forbid list(line 104-115 附近,phase-7 加的"no wrapper layer"等条款)—— max_retries 走 kwarg 不是 wrapper,合规;若 docstring 显式提到 `_PROVIDER_BUDGET_KEY`,删除该提及。
    5. **禁止** 在 runner.py 任何位置出现 `"max_" + "retries"` 字符串拼接。
  </action>
  <verify>
    <automated>grep -c "_PROVIDER_BUDGET_KEY" seers_harness/validation/runner.py ; grep -c '"max_" + "retries"' seers_harness/validation/runner.py ; grep -c "deepseek_provider_from_env(max_retries=3)" seers_harness/validation/runner.py ; grep -c "_RUNNER_PROVIDER_MAX_RETRIES" seers_harness/validation/runner.py ; pytest -q tests/test_validation_runner.py -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `_PROVIDER_BUDGET_KEY` 在 runner.py 出现次数 == 0
    - `'"max_" + "retries"'` 出现次数 == 0
    - `deepseek_provider_from_env(max_retries=3)` *或* `deepseek_provider_from_env(max_retries=_RUNNER_PROVIDER_MAX_RETRIES)` 出现 1 次(等价表达,VALIDATION 验收 grep 应识别 = 后含数字 3 *或* 常量名;此 plan 推荐用常量,acceptance 写法兼容二者)
    - `_RUNNER_PROVIDER_MAX_RETRIES` 出现 ≥ 2(定义 + 引用)
    - `pytest -q` 既有套件无回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(无新引入边界,纯清理。runner.py 触三处 dead code / forbid-list 绕道。)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Repudiation | forbid-list scan 反向被欺骗(IN-08 字符串拼接绕审计) | mitigate | grep 验收硬挂在 0 hits;module-level 常量 `_RUNNER_PROVIDER_MAX_RETRIES` 直接可见 |
| T-08-04-02 | Tampering | 重复 helper(_detect_delimiter)与 canonical 行为漂移 | mitigate | WR-03 删除重复,canonical 单一来源 |
</threat_model>

<verification>
- pytest 全套绿;phase-7 baseline 253 不回归
- grep 验收(D8-G-WR-03 / WR-04 / IN-08):全部 0 hits 处 == 0
- 实测证据:N/A 纯清理(VALIDATION.md 标 N/A)
</verification>

<success_criteria>
- D8-G-WR-03 / D8-G-WR-04(callsite)/ D8-G-IN-08 三件 grep 验收全过
- runner.py LOC 净减(三处删除大于一处新增)
- 无功能行为变更:`_run_one_request` / `_build_scratch_csv` / `_default_deepseek_factory` 行为字节级保持(仅 import / 常量名替换)
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-04-SUMMARY.md`,贴 grep 验收 + commit SHA + LOC delta。
</output>
