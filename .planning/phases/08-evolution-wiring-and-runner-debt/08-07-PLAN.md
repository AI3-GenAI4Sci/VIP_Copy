---
phase: 08-evolution-wiring-and-runner-debt
plan: 07
type: execute
wave: 4
depends_on:
  - 08-06
files_modified:
  - seers_harness/validation/machine_judges.py
  - seers_harness/validation/batch_summary_writer.py
  - tests/test_machine_judges.py
  - tests/test_batch_summary_writer.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 5 个行为度量(M1-M5)真阳性验收边界(用户 2026-05-27 锁定):新增 5 个 compute_* judge 函数 + build_behavioral_report aggregator,batch_summary.json.behavioral_metrics 含 6 个 key(factor_count_p50 / factor_diversity_score / copy_candidate_count_p50 / reflection_triggered_when_underspec_rate / delta_diversity_score (含 count/unique_targets/unique_change_types 三子键) / trial_belief_update_count)。所有计算用算法(median/Jaccard/计数),零启发式 if/elif。"
  truths:
    - "M1 factor_count_p50 = statistics.median([len(d.factors) for d in all_factor_artifacts]) ≥ 3 (阻塞)"
    - "M2 factor_diversity_score = mean(Jaccard distance of covers_product_ids pairs + Jaccard distance of transferable_disposition token pairs) / 2 ≥ 0.5 (阻塞)"
    - "M3a copy_candidate_count_p50 = statistics.median([len(d.considered_drafts) for d in all_copy_artifacts]) ≥ 2 (阻塞)"
    - "M3b reflection_triggered_when_underspec_rate = (#underspec requests where reflect_* tool called) / (#underspec requests);underspec = factor_count<3;若 0 underspec 则 trivially 1.0 (阻塞 ≥ 0.8)"
    - "M4 delta_diversity_score = {count: len(proposals), unique_targets: len(set targets), unique_change_types: len(set change_types)};count ≥ 1 阻塞,unique_* 在 count ≥ 2 时 ≥ 2 软告警"
    - "M5 trial_belief_update_count = sum(1 for row in final_portfolio if row.sample_count > 0);> 0 阻塞"
    - "batch_summary.json.behavioral_metrics 恰好 6 个 key,值都是 numeric 或 dict;build_behavioral_report 是 pure transform 读 index.json + evidence/<node>/{artifact.json, tool_calls.jsonl}"
    - "5 个 judge 函数都用算法(median, Jaccard, 计数);禁止 if/elif 分类 factor 是否多样"
  artifacts:
    - path: seers_harness/validation/machine_judges.py
      provides: "compute_factor_count_p50 / compute_factor_diversity / compute_copy_candidate_count_p50 / compute_reflection_trigger_rate / compute_delta_diversity / compute_belief_update_count + build_behavioral_report aggregator"
      contains: "build_behavioral_report"
    - path: seers_harness/validation/batch_summary_writer.py
      provides: "behavioral_metrics 字段在 summary dict 顶层"
      contains: "behavioral_metrics"
    - path: tests/test_machine_judges.py
      provides: "5 个 metric × ≥1 测试 = 5+ 新测试(每个 metric 至少 happy + edge)"
      contains: "test_machine_judges_factor_diversity"
  key_links:
    - from: batch_summary_writer
      to: build_behavioral_report
      via: "summary['behavioral_metrics'] = build_behavioral_report(stage_dir)"
      pattern: "build_behavioral_report\\("
    - from: build_behavioral_report
      to: compute_factor_diversity
      via: "调用 5 个 compute_* 函数聚合"
      pattern: "compute_factor_diversity"
    - from: compute_factor_diversity
      to: itertools.combinations + set ops
      via: "Jaccard 距离 = 1 - |A∩B|/|A∪B|"
      pattern: "len\\(a & b\\)/len\\(a \\| b\\)"
  forbid_list:
    - "禁止用 if/elif 链对 factor 'diversity' 分桶(如 'high' / 'medium' / 'low')—— 返回 numeric float,阈值比较在 verification 时做"
    - "禁止给 metric 加权(如 'diversity = 0.7*ids + 0.3*text')—— 用均值,系数硬编码 = 启发式"
    - "禁止在 compute_* 内调 LLM —— 全 pure 算术 (median/Jaccard/计数)"
    - "禁止在 metric 缺数据时 raise —— 缺数据返回 0 或 trivially-pass 值(M3b 当 0 underspec 时返 1.0),让 verification 层判断是否可信"
    - "禁止把 behavioral_metrics 写到 capture 层(recording_provider / evidence_writer)—— D-22d 隔离,writer 层独占"
    - "禁止把 6 个 key 之一塞 None / 缺省 —— 缺数据用 0 / 1.0 / {count: 0} 显式表达"
---

<objective>
落地用户 2026-05-27 锁定的真阳性边界:5 个行为度量 (M1-M5) 在 `seers_harness/validation/machine_judges.py` 新增 5 个 `compute_*` judge 函数 + 1 个 `build_behavioral_report(stage_dir)` aggregator,`batch_summary_writer.py` 把结果写到 `summary.behavioral_metrics`。任一阻塞 metric 未达 = phase 8 触发 user review,不自动 pass。

Purpose: phase 8 acceptance 不只是 "trials[] 非空"(假阳性下限);真阳性要求挖掘真的多角度 (M1/M2)、文案真的 multi-draft (M3a)、模型偷懒时反思真的触发 (M3b)、distill 真的产 delta (M4)、trial outcome 真的折回 portfolio (M5)。RESEARCH §5.4b 锁定阈值与算法,本 plan 落地。
Output: 单一 atomic commit;machine_judges.py 新增 5 + 1 = 6 个函数;batch_summary_writer.py 顶层加一行 `"behavioral_metrics": build_behavioral_report(stage_dir)`;tests/test_machine_judges.py 新增 5+ 测试;tests/test_batch_summary_writer.py 新增 1 测试断言 6 个 key 存在。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/machine_judges.py
@seers_harness/validation/batch_summary_writer.py
@seers_harness/domain/models.py
@seers_harness/evolution/delta_portfolio.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_factor_count_p50 + compute_factor_diversity + compute_copy_candidate_count_p50 + compute_reflection_trigger_rate(M1 + M2 + M3)+ 4 个测试</name>
  <files>seers_harness/validation/machine_judges.py, tests/test_machine_judges.py</files>
  <read_first>
    - seers_harness/validation/machine_judges.py(全文 — 看 VAL-01/02/04 既有 compute_* 风格 + extract_literal_overlap Jaccard 实现)
    - seers_harness/domain/models.py(PersonalizationFactor / CopyGenerationArtifact 字段精确名:`factors`, `covers_product_ids`, `transferable_disposition`, `considered_drafts`)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§5.4b M1 / M2 / M3 完整伪代码)
    - tests/test_machine_judges.py(找既有 compute_* 测试 fixture 模式)
  </read_first>
  <behavior>
    - Test 1 `test_machine_judges_factor_count_p50`:6 artifacts,各 factors 数量 [1,2,3,4,5,6] → median == 3.5(若 statistics.median 返 float 取 3.5,断言 == 3.5)。再加一个 [2,2,3,3,3,3] case → median == 3.0。
    - Test 2 `test_machine_judges_factor_diversity_jaccard`:构造 2 artifacts,covers_product_ids 集合分别 {1,2}, {3,4} → ids_distance = 1.0(全不重叠);transferable_disposition tokens 完全不重叠 → text_distance = 1.0 → score == 1.0。再构造完全相同 factors → score == 0.0。
    - Test 3 `test_machine_judges_factor_diversity_handles_empty`:0 或 1 个 factors 总数 → 返 0.0,不 raise(避免 combinations 空集)。
    - Test 4 `test_machine_judges_copy_candidate_count_p50`:5 copy_artifacts 各 considered_drafts 长度 [1,2,2,3,4] → median == 2.0。
    - Test 5 `test_machine_judges_reflection_trigger_rate`:per_request 列表 [(factor_count, [tool_names])]:`[(2, ["reflect_on_coverage"]), (2, []), (5, [])]` → underspec = 2 个 (factor_count<3),其中 1 个 triggered → rate = 0.5。再 case `[(5, []), (10, [])]` → 0 underspec → rate = 1.0 trivially pass。
  </behavior>
  <action>
    在 `seers_harness/validation/machine_judges.py` 新增 4 个 `compute_*` 函数,实现 verbatim 取自 08-RESEARCH §5.4b 伪代码:

    **`compute_factor_count_p50(all_factor_artifacts: list[dict]) -> float`** (M1):
    ```
    import statistics
    counts = [len(d.get("factors", [])) for d in all_factor_artifacts]
    return float(statistics.median(counts)) if counts else 0.0
    ```

    **`compute_factor_diversity(all_factor_artifacts: list[dict]) -> float`** (M2):
    - 提取所有 factor:`all_factors = [f for d in all_factor_artifacts for f in d.get("factors", [])]`
    - `ids_sets = [frozenset(f.get("covers_product_ids", [])) for f in all_factors]`
    - `texts = [frozenset(_tokenize(f.get("transferable_disposition", ""))) for f in all_factors]`
    - `if len(all_factors) < 2: return 0.0`
    - 用 `itertools.combinations` 算 Jaccard 距离对均值:
      ```
      from itertools import combinations
      ids_distance = mean(1 - len(a & b)/len(a | b) for a, b in combinations(ids_sets, 2) if (a or b))
      text_distance = mean(1 - len(a & b)/len(a | b) for a, b in combinations(texts, 2) if (a or b))
      return (ids_distance + text_distance) / 2
      ```
    - `_tokenize` 沿用既有 codepoint-Jaccard 风格(IN-03 fix 已落:`9e57edf`)—— grep `extract_literal_overlap` 找 helper 或新增 module-level `_tokenize(text: str) -> set[str]` 用 character-level set(中文兼容)。**禁止** 用 `text.split()`(IN-03 教训)。

    **`compute_copy_candidate_count_p50(all_copy_artifacts: list[dict]) -> float`** (M3a):
    ```
    counts = [len(d.get("considered_drafts", [])) for d in all_copy_artifacts]
    return float(statistics.median(counts)) if counts else 0.0
    ```

    **`compute_reflection_trigger_rate(per_request: list[tuple[int, list[str]]]) -> float`** (M3b):
    - `underspec = [tools for fc, tools in per_request if fc < 3]`
    - `if not underspec: return 1.0`(trivially pass — 0 underspec 时本 metric 不适用)
    - `triggered = sum(1 for tools in underspec if any(t.startswith("reflect_") for t in tools))`
    - `return triggered / len(underspec)`

    每个 compute_* 函数顶部加 docstring 说明算法 + 阈值 + 失败语义(per §5.4b)。

    **禁止** 在 4 个函数中:加权 / if-elif 分类 / fallback estimation / LLM 调用。

    4 个测试追加到 `tests/test_machine_judges.py`,沿用文件既有 fixture 模式。
  </action>
  <verify>
    <automated>pytest -q tests/test_machine_judges.py -k "factor_count or factor_diversity or copy_candidate or reflection_trigger" -x 2>&1 | tail -25 ; grep -c "def compute_factor_count_p50\|def compute_factor_diversity\|def compute_copy_candidate_count_p50\|def compute_reflection_trigger_rate" seers_harness/validation/machine_judges.py</automated>
    <human-check>4 个函数 grep 命中 == 4;5 个新测全绿;baseline 不回归。</human-check>
  </verify>
  <done>
    - 4 个 compute_* 函数定义齐全
    - 5 个新测在 tests/test_machine_judges.py 入帐并全绿
    - 无 if/elif 分类逻辑;_tokenize 用 codepoint-level set(中文兼容)
    - phase-7 baseline 253 测试无回归
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute_delta_diversity + compute_belief_update_count(M4 + M5)+ 2 个测试</name>
  <files>seers_harness/validation/machine_judges.py, tests/test_machine_judges.py</files>
  <read_first>
    - seers_harness/validation/machine_judges.py(Task 1 已新增的 4 个函数,确认命名风格)
    - seers_harness/evolution/delta_portfolio.py(DeltaProposal 字段 `target_skill`, `change_type`, `delta_id`;DeltaPortfolioRow 字段 `sample_count`, `success_count`, `failure_count`)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§5.4b M4 / M5 完整伪代码)
  </read_first>
  <behavior>
    - Test 1 `test_machine_judges_delta_diversity`:proposals = [DeltaProposal(target_skill="A", change_type="modify_skill", ...), DeltaProposal(target_skill="B", change_type="modify_skill", ...)] → `{count: 2, unique_targets: 2, unique_change_types: 1}`。再加 0 proposals case → `{count: 0, unique_targets: 0, unique_change_types: 0}`(不 raise)。
    - Test 2 `test_machine_judges_belief_update_count`:portfolio = [Row(sample_count=0), Row(sample_count=3), Row(sample_count=1)] → 2。空 portfolio → 0。
  </behavior>
  <action>
    在 `seers_harness/validation/machine_judges.py` 接 Task 1 之后新增 2 个 `compute_*` 函数:

    **`compute_delta_diversity(proposals: list) -> dict`** (M4):
    - 接受 list of DeltaProposal-like dict 或 model object;用 `.get(...)` 或 attribute access(用 `getattr` + fallback "" 保持 dict / model 双兼容)
    ```
    return {
        "count": len(proposals),
        "unique_targets": len({getattr(p, "target_skill", None) or p.get("target_skill") for p in proposals}) if proposals else 0,
        "unique_change_types": len({getattr(p, "change_type", None) or p.get("change_type") for p in proposals}) if proposals else 0,
    }
    ```
    (实现细节按 read_first 中 DeltaProposal 的实际类型选最干净路径 — 若 phase-6 已定型为 pydantic model,统一用 attribute access。)

    **`compute_belief_update_count(final_portfolio: list) -> int`** (M5):
    ```
    return sum(1 for row in final_portfolio if (getattr(row, "sample_count", 0) or 0) > 0)
    ```

    2 个测试追加到 `tests/test_machine_judges.py`。fixture 中 DeltaProposal / DeltaPortfolioRow 实例化沿用 phase-6 单测模式(grep 既有用法)。

    **禁止** 给 M4 加 "weighted_diversity_score"(unique_targets * 0.5 + unique_change_types * 0.5)—— forbid_list。M4 返三子键 dict,阈值在 build_behavioral_report 之外比较。
  </action>
  <verify>
    <automated>pytest -q tests/test_machine_judges.py -k "delta_diversity or belief_update" -x 2>&1 | tail -15 ; grep -c "def compute_delta_diversity\|def compute_belief_update_count" seers_harness/validation/machine_judges.py</automated>
    <human-check>2 个函数 grep 命中 == 2;2 个新测全绿。</human-check>
  </verify>
  <done>
    - compute_delta_diversity 返三子键 dict,unique_* 在 proposals 为空时为 0
    - compute_belief_update_count 返 int
    - 2 个新测全绿,baseline 不回归
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: build_behavioral_report aggregator + batch_summary_writer 接入 + 集成测试</name>
  <files>seers_harness/validation/machine_judges.py, seers_harness/validation/batch_summary_writer.py, tests/test_machine_judges.py, tests/test_batch_summary_writer.py</files>
  <read_first>
    - seers_harness/validation/machine_judges.py(Task 1+2 已新增的 6 个函数,确认调用顺序)
    - seers_harness/validation/batch_summary_writer.py(找 summary dict 顶层装配点,plan 08-03 已在此加 by_failure_class —— 行邻位置)
    - seers_harness/validation/index_writer.py(确认 index.json 字段 + evolution_snapshot.json 路径 schema —— stage_dir/<rid>/evolution_snapshot.json 是否标准位)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§5.4b M1-M5 算法 + 数据源描述 + §5.4 实测证据矩阵)
  </read_first>
  <behavior>
    - Test 1 `test_build_behavioral_report_happy_path`:tmp stage_dir 含 3 个 request_dirs,各 evidence/<node>/artifact.json 含 factors / considered_drafts,evolution_snapshot.json 含 trials + portfolio → `build_behavioral_report(stage_dir)` 返 dict 含 6 个 key (`factor_count_p50`, `factor_diversity_score`, `copy_candidate_count_p50`, `reflection_triggered_when_underspec_rate`, `delta_diversity_score`, `trial_belief_update_count`)。
    - Test 2 `test_build_behavioral_report_handles_missing_evidence`:tmp stage_dir 含 1 个 request_dir 但 evidence 文件缺失 → 不 raise,各 metric 返默认值 (0 / 0.0 / {count:0,...} / 1.0)。
    - Test 3 `test_batch_summary_includes_behavioral_metrics`:mock batch_summary_writer 入参 → summary dict 顶层含 `"behavioral_metrics"` key,且值是 dict 含 6 个 sub-key。
  </behavior>
  <action>
    **machine_judges.py 新增 build_behavioral_report aggregator**:
    ```
    def build_behavioral_report(stage_dir: Path) -> dict:
        """Aggregate M1-M5 from a stage_dir (e.g., tests/smoke/.runs/<ts>/stage2/).
        
        Pure transform: reads index.json + evidence/<node>/{artifact.json, tool_calls.jsonl} + evolution_snapshot.json.
        No LLM calls. No heuristics. Returns 6-key dict for batch_summary.behavioral_metrics.
        """
        # 1. 列举所有 request_dirs (从 stage_dir/index.json 读 request_id list)
        # 2. 收集 factor_artifacts / copy_artifacts:逐 request 读 evidence/factor_discovery/artifact.json + evidence/copy_generation/artifact.json,缺失文件 skip
        # 3. 收集 per_request_reflection_data:逐 request 读 evidence/factor_discovery/tool_calls.jsonl,提取 tool_name list;配对 factor_count = len(factor_artifact.factors)
        # 4. 收集 all_proposals (M4 数据源,LOCKED 反推路径):从 stage_dir/evolution_snapshot.json 的 portfolio rows 反推。每个 portfolio row 对应一个 distill agent 起始 proposal;phase 8 portfolio 起始空且 distill 是唯一增长源(plan 08-05 forbid_list 第 1 条),因此 portfolio rows ≡ proposals 集合。映射:`proposals = [{"delta_id": row.delta_id, "target_skill": row.target_skill, "change_type": row.change_type} for row in final_portfolio]`。**不读** stage_dir/distill_proposals.json(plan 08-05 不写此文件)。
        # 5. 收集 final_portfolio:从最末 stage 的 evolution_snapshot.json `portfolio` 字段读 (`portfolio` reducer 已 phase-7 落地 per RESEARCH §3.3)
        # 6. 调 5 个 compute_*,装配 6-key dict
        ...
    ```
    
    **关键实施细节 — M4 proposals 数据源 LOCKED:** 本 task 通过 `evolution_snapshot.json` 的 portfolio rows 反推 proposals 集合。因 phase 8 distill agent 是 portfolio 唯一增长源(plan 08-05 forbid_list 第 1 条 + truths 第 1 条共同保证),portfolio rows 与 distill 产出的 proposals 是同构集合。映射只用 (`delta_id`, `target_skill`, `change_type`) 三字段计算 M4 三子键(count / unique_targets / unique_change_types)—— observation / proposed_change 等 raw 字段对 M4 metric 不是必需。**禁止** 在本 plan 改 plan 08-05 让 runner 写第二份 audit 文件 — 反推路径在算法上等价。
    
    **batch_summary_writer.py 改动**:在 plan 08-03 已加的 `by_failure_class` 行附近,追加一行:
    ```
    from seers_harness.validation.machine_judges import build_behavioral_report
    # ...在 summary dict 装配处:
    "behavioral_metrics": build_behavioral_report(stage_dir),
    ```
    `stage_dir` 应是 batch_summary_writer 当前可访问的路径(grep 既有签名;若 writer 当前不接 stage_dir,加 kwarg `stage_dir: Path`)。**不要** 改 batch_summary_writer 的现有 sig 顺序 / 字段顺序 — 只追加 kwarg + key。
    
    **测试**:test_machine_judges.py 加 2 个 build_behavioral_report 测;test_batch_summary_writer.py 加 1 个集成测断言 6-key 存在 + 类型正确(`isinstance(summary["behavioral_metrics"]["factor_count_p50"], (int, float))` 等)。
    
    **禁止** 在 build_behavioral_report 内 raise on 缺数据 — per forbid_list,缺数据返默认值,verification 层判断可信度。
  </action>
  <verify>
    <automated>pytest -q tests/test_machine_judges.py tests/test_batch_summary_writer.py -x 2>&1 | tail -30 ; grep -c "build_behavioral_report" seers_harness/validation/machine_judges.py ; grep -c "behavioral_metrics" seers_harness/validation/batch_summary_writer.py</automated>
    <human-check>build_behavioral_report 定义 1 次;batch_summary_writer 中 behavioral_metrics 出现 ≥ 1;3 个新测全绿。手 inspect summary dict 是否含 6 个 sub-key。</human-check>
  </verify>
  <done>
    - build_behavioral_report 返 dict 含 6 个 sub-key (`factor_count_p50`, `factor_diversity_score`, `copy_candidate_count_p50`, `reflection_triggered_when_underspec_rate`, `delta_diversity_score`, `trial_belief_update_count`)
    - delta_diversity_score 是 dict 含三子键 (count / unique_targets / unique_change_types)
    - batch_summary_writer summary dict 顶层含 behavioral_metrics
    - 3 个新测全绿;phase-7 baseline 不回归
    - 缺数据路径不 raise(verified by test 2)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence files → behavioral_metrics | machine_judges 读 evidence 中已 redacted artifact,无 leak;Jaccard tokenize 不读 raw message,只读 transferable_disposition 与 covers_product_ids |
| metric → user review | 阻塞 metric 未达 → user 看 batch_summary 决定接受 / 重跑;metric 是 phase 8 acceptance 唯一 *算法* 真阳性边界,造假代价高(需手改 metric 函数) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | metric 阈值被 silently 降低以让 phase 8 自动 pass | mitigate | 阈值不在代码里硬编码 — 阈值比较在 08-VERIFICATION.md acceptance gate 中显式列出;代码只返 numeric,阈值改动 require doc change with audit |
| T-08-07-02 | Repudiation | M3b underspec=0 时 trivially 返 1.0 → metric 看似 pass 实则不适用 | accept | trivially pass 由 docstring 显式说明;若 batch 全是 factor>=3,反思路径本就不该触发;batch_summary 中可加 metadata `underspec_request_count` 帮判断(scope creep,本 plan 不加) |
| T-08-07-03 | Information Disclosure | _tokenize 把 transferable_disposition 切碎写到 metric 计算中 → 间接 leak | accept | 计算结果是数值;intermediate set 仅在内存,不写盘 |
</threat_model>

<verification>
- pytest 全套绿,7 个新测入帐(M1-M5 各 1+ 测试 + 2 个 aggregator 测试 + 1 个 batch_summary 集成测试)
- grep:5 个 `compute_*` + 1 `build_behavioral_report` 定义齐全
- 实测证据(post-batch,M1-M5 阻塞):
  - `jq '.behavioral_metrics' tests/smoke/.runs/<phase-8-ts>/stage2/batch_summary.json` 返 dict 含 6 sub-key
  - M1 factor_count_p50 ≥ 3
  - M2 factor_diversity_score ≥ 0.5
  - M3a copy_candidate_count_p50 ≥ 2
  - M3b reflection_triggered_when_underspec_rate ≥ 0.8
  - M4 delta_diversity_score.count ≥ 1
  - M5 trial_belief_update_count > 0
- 任一阻塞 metric 未达 → phase 8 状态 gaps_found,user review 决定接受软告警 / 重跑 / 修代码后重跑
</verification>

<success_criteria>
- M1-M5 全部以 *算法* 落地(median / Jaccard / 计数);无 if/elif 分桶;无加权系数
- batch_summary.json.behavioral_metrics 在 phase-8 batch 上 emit 6 个 key
- 缺数据路径不 raise,返默认值
- D-22d 隔离:capture 层零 import 增量
- 真阳性边界落地:用户 2026-05-27 锁定的 5 个行为度量在 acceptance gate 阻塞 phase 8
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-07-SUMMARY.md`,记录 commit SHA + 6 个函数 + 7 个新测试 + grep 验收。
</output>
