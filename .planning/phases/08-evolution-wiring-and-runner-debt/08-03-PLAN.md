---
phase: 08-evolution-wiring-and-runner-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - seers_harness/validation/exception_classifier.py
  - seers_harness/validation/runner.py
  - seers_harness/validation/index_writer.py
  - seers_harness/validation/batch_summary_writer.py
  - tests/test_exception_classifier.py
  - tests/test_index_writer.py
  - tests/test_batch_summary_writer.py
autonomous: true
requirements: []
must_haves:
  goal: "落地 D8-E:新增 failure_class(exc) 7 标签函数(与 D-19 三标签 classify 并存),让 index.json 每行带 failure_class、batch_summary.json 含 by_failure_class 聚合,贡献 D8-ACC-3"
  truths:
    - "failure_class(None) == 'ok';failure_class(ProviderAuthError(...)) == 'auth';failure_class(RuntimeError(ProviderAuthError(...))) == 'auth'(cause-chain walk)"
    - "SchemaError 别名 = pydantic.ValidationError(per 08-RESEARCH §2-E + CONTEXT D8-VAL-ROOTCAUSE Q1)"
    - "index.json 每行有 'failure_class' key 取自 7 标签 enum;batch_summary.json.by_failure_class 各 value 之和 == totals.requests"
    - "用 dispatch table(isinstance allowlist tuple)而非 if/elif 链,符合用户审美约束 D8-VAL-ROOTCAUSE"
  artifacts:
    - path: seers_harness/validation/exception_classifier.py
      provides: "failure_class(exc) 7 标签函数(与 classify 并存)"
      contains: "def failure_class"
    - path: seers_harness/validation/index_writer.py
      provides: "row 含 failure_class 列"
      contains: "failure_class"
    - path: seers_harness/validation/batch_summary_writer.py
      provides: "summary 含 by_failure_class dict"
      contains: "by_failure_class"
  key_links:
    - from: runner.py _run_one_request / _run_stage 失败路径
      to: failure_class()
      via: "record['failure_class'] = failure_class(exc) 或 'ok'"
      pattern: "failure_class\\("
    - from: index_writer.write_index
      to: record['failure_class']
      via: "row['failure_class'] = record.get('failure_class', 'ok')"
      pattern: 'failure_class.*record\\.get'
    - from: batch_summary_writer
      to: rows[i]['failure_class']
      via: "by_failure_class[cls] += 1 dispatch counter"
      pattern: "by_failure_class"
  forbid_list:
    - "禁止改 classify(exc) 的 3 标签 contract(D-19 锁定)"
    - "禁止把 failure_class 加到 capture 层(recording_provider / evidence_writer)—— D-22d 隔离"
    - "禁止用 if/elif 链做 type 分发 —— 用 isinstance + tuple 或 dispatch dict(用户审美 D8-VAL-ROOTCAUSE)"
    - "禁止把任何 7 标签外的字符串塞进 failure_class(SchemaError 别名严格 == pydantic.ValidationError)"
---

<objective>
落地 D8-E + 真阳性边界(用户 2026-05-27):新增 `failure_class(exc) -> Literal[7 enums]` 函数,与 D-19 三标签 `classify(exc)` 并列共存,把可读的 7 标签 failure 类别写进 `index.json` 每行,并由 `batch_summary_writer` 聚合到 `by_failure_class` dict。

Purpose: D8-ACC-3 要求 batch_summary 能告诉操作员"12/20 失败:8 transient + 3 timeout + 1 auth"而非 undifferentiated `requests_failed=12`。
Output: 4 个生产文件 + 3 个测试文件改动,1 个 atomic commit;`failure_class` 函数 + 4 处 wiring + 3+ 个新测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/validation/exception_classifier.py
@seers_harness/core/errors.py
@seers_harness/validation/index_writer.py
@seers_harness/validation/batch_summary_writer.py
@seers_harness/validation/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: failure_class() 函数 + 单测(dispatch table 风格)</name>
  <files>seers_harness/validation/exception_classifier.py, tests/test_exception_classifier.py</files>
  <read_first>
    - seers_harness/validation/exception_classifier.py(全文 — 看 classify 既有 cause-chain walk 范式)
    - seers_harness/core/errors.py(确认 4 个 ProviderError 类的精确名 + 继承关系)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-E 完整伪代码 + SchemaError 别名说明)
    - tests/test_exception_classifier.py(找现有 cause-chain 单测的 monkeypatch 模式)
  </read_first>
  <behavior>
    - Test `failure_class(None) == "ok"`
    - Test 每个 enum 一个 isinstance case:`ProviderAuthError → "auth"`、`ProviderRateLimitError → "rate_limit"`、`ProviderTransientError → "transient"`、`ProviderResponseError → "malformed_tool_args"`、`pydantic.ValidationError → "schema_violation"`
    - Test cause-chain walk:`raise RuntimeError("wrapped") from ProviderAuthError("...")` → `failure_class` 仍返回 `"auth"`
    - Test default 桶:`KeyError("foo") → "runner_bug"`、`ValueError("bar") → "runner_bug"`
    - Test sentinel `TrialFailure` 不属于 7 enums 中任何一个 isinstance match,落 `runner_bug`(因为 TrialFailure 是 D-19 的 trial_failure 路由,不该出现在 fail-fast index 列;若日后改语义,在此点扩 enum)
  </behavior>
  <action>
    在 `seers_harness/validation/exception_classifier.py` 现有 `classify` 函数 *下方* 新增 `failure_class(exc: BaseException | None) -> Literal[...]`,**完全独立** 不复用 classify 内部状态(D-19 锁定 contract)。实现 verbatim 取自 08-RESEARCH §2-E 伪代码:
    - 顶部新 import:`from pydantic import ValidationError as _PydanticValidationError`(本地别名 = SchemaError per 08-RESEARCH Q1)
    - 用 `_FAILURE_CLASS_DISPATCH: tuple[tuple[type[BaseException], str], ...]` 一个 tuple-of-tuples 把 isinstance 表落字面化:
      ```
      _FAILURE_CLASS_DISPATCH = (
          (ProviderAuthError, "auth"),
          (ProviderRateLimitError, "rate_limit"),
          (ProviderTransientError, "transient"),
          (ProviderResponseError, "malformed_tool_args"),
          (_PydanticValidationError, "schema_violation"),
      )
      ```
      (这是 dispatch table,而不是 if/elif 链,满足 forbid_list "禁止 if/elif 链"。)
    - 函数体:`if exc is None: return "ok"`;然后 cause-chain walk(同 classify 范式 — `cur` + `seen: set[id]`);每步对 `_FAILURE_CLASS_DISPATCH` 用 `for exc_type, label in _FAILURE_CLASS_DISPATCH: if isinstance(cur, exc_type): return label`;走完链返回 `"runner_bug"`。
    - 模块级 Literal 别名:`FailureClass = Literal["auth", "rate_limit", "transient", "malformed_tool_args", "schema_violation", "runner_bug", "ok"]`,函数返回值标注此别名,供下游 import。
    
    单测追加到 `tests/test_exception_classifier.py`,凑齐 7 enum case + cause-chain case + None case。**禁止** 修改既有 `classify(exc)` 行为或签名(D-19 锁定)。
  </action>
  <verify>
    <automated>pytest -q tests/test_exception_classifier.py -x 2>&1 | tail -20 ; grep -c "def failure_class" seers_harness/validation/exception_classifier.py ; grep -c "_FAILURE_CLASS_DISPATCH" seers_harness/validation/exception_classifier.py</automated>
  </verify>
  <done>
    - 新测全绿,phase-7 baseline 不回归
    - `def failure_class` 出现 1 次;`_FAILURE_CLASS_DISPATCH` 出现 ≥ 2 次(定义 + 引用)
    - `def classify(` 仅 1 次出现,签名 / 返回类型 verbatim 与 phase-7 一致(D-19)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 failure_class 写进 record + index.json + batch_summary.json,单测覆盖三处</name>
  <files>seers_harness/validation/runner.py, seers_harness/validation/index_writer.py, seers_harness/validation/batch_summary_writer.py, tests/test_index_writer.py, tests/test_batch_summary_writer.py</files>
  <read_first>
    - seers_harness/validation/runner.py(line 425-535 `_run_one_request` 成功路径 record dict;line 600+ `_run_stage` 失败路径 fail_record dict)
    - seers_harness/validation/index_writer.py(全文,看 row 构造点)
    - seers_harness/validation/batch_summary_writer.py(全文,看 totals / fail_lists 聚合块)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-E 修改点 2/3/4)
    - tests/test_index_writer.py + tests/test_batch_summary_writer.py(找既有 row / summary 断言模式)
  </read_first>
  <behavior>
    - Test `test_index_writer_includes_failure_class`:构造 records 含 `failure_class="ok"` 与 `failure_class="auth"` → write_index 后 row 含此 key。
    - Test `test_index_writer_defaults_to_ok_when_missing`:旧 record 不含 failure_class → row 默认 `"ok"`。
    - Test `test_batch_summary_by_failure_class_aggregation`:6 行 records,3 ok / 2 auth / 1 transient → summary["by_failure_class"] == {"ok":3,"auth":2,"transient":1};`sum(values()) == totals.requests`。
    - Test `test_batch_summary_by_failure_class_completeness`:summary["by_failure_class"] dict 之和 ≡ totals.requests(完整性自校验)。
    - 单测可选 `test_run_one_request_success_record_has_ok` / `test_run_stage_fail_record_has_correct_class`:走 `_run_one_request` mocked 路径,断言成功 record["failure_class"] == "ok";构造 inject 失败 (`ProviderAuthError`) → fail_record["failure_class"] == "auth"。
  </behavior>
  <action>
    **runner.py 改动**(per 08-RESEARCH §2-E 修改点 2):
    1. 顶部 import:`from seers_harness.validation.exception_classifier import classify, failure_class`(failure_class 新加,classify 既有)。
    2. `_run_one_request` 成功返回的 record dict 末尾追加 `"failure_class": "ok"`;若代码路径中提前 return failure record,也要附 `"failure_class": failure_class(exc)`。
    3. `_run_stage` 中构造 `fail_record` 的 dict literal,在末尾加一行 `"failure_class": failure_class(exc),  # E`。注意 phase 8 后续 plan 的 WR-01 drain 路径也要写此字段(在 plan 08-09 中显式实现);此 plan 仅负责 _run_one_request 的成功 + _run_stage 主失败两处。
    
    **index_writer.py 改动**(§2-E 修改点 3):
    在构造 row dict 的位置加一行:`row["failure_class"] = record.get("failure_class", "ok") if isinstance(record, dict) else "ok"`。位置:在现有 row dict 完成所有 phase-7 字段后,**最后一行** 追加,保持 column ordering。
    
    **batch_summary_writer.py 改动**(§2-E 修改点 4):
    在 totals 聚合块附近(可能命名 `_aggregate_totals` 或在 main writer 函数体内),新增:
    ```
    by_failure_class: dict[str, int] = {}
    for row in rows:
        cls = row.get("failure_class", "ok")
        by_failure_class[cls] = by_failure_class.get(cls, 0) + 1
    ```
    在 summary dict 顶层附加 `"by_failure_class": by_failure_class`。**禁止** 用 if/elif 链统计每类 —— 用 dict 累加(算法,非启发式)。
    
    **测试**:加到 `tests/test_index_writer.py` 和 `tests/test_batch_summary_writer.py`,沿用各文件 existing fixture(records/rows 列表)。可选:`tests/test_validation_runner.py` 加 1 个 record-shape 断言(已在 phase-7 单测里跑 _run_one_request 路径的话,改 fixture 让 mocked 失败注入 ProviderAuthError 并断言 fail_record["failure_class"] == "auth")。
    
    **完整性自校验**:在 batch_summary writer 内部不要 assert `sum == totals` —— 这是单测责任,生产代码不引入运行时 assert。
  </action>
  <verify>
    <automated>pytest -q tests/test_exception_classifier.py tests/test_index_writer.py tests/test_batch_summary_writer.py tests/test_validation_runner.py -x 2>&1 | tail -30 ; grep -c "failure_class" seers_harness/validation/runner.py ; grep -c "failure_class" seers_harness/validation/index_writer.py ; grep -c "by_failure_class" seers_harness/validation/batch_summary_writer.py</automated>
  </verify>
  <done>
    - 4 套测试全绿
    - `failure_class` 在 runner.py 出现次数 ≥ 3(import + 成功 record + 失败 record)
    - `failure_class` 在 index_writer.py 出现次数 ≥ 1
    - `by_failure_class` 在 batch_summary_writer.py 出现次数 ≥ 2
    - 既有 `classify` 在 exception_classifier.py 行为字节级不变(D-19)
    - 既有 capture 层(recording_provider / evidence_writer / machine_judges)零 import 增量(D-22d 隔离)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime exception → analytic 列 | 异常对象(可能含 secret)→ 7 标签字符串。failure_class 函数只读 type,不读 message,不引入 leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | exception message 通过 `"failure_class": str(exc)` 漏到 index.json | mitigate | failure_class 只返回 7 enum 字面量,不取 str(exc);单测覆盖每个 enum 返回值固定 |
| T-08-03-02 | Repudiation | 操作员看 `requests_failed=12` 但无类别分布 | mitigate | by_failure_class 完整性单测 sum == totals;phase-8 batch acceptance D8-ACC-3 强制 |
| T-08-03-03 | Tampering | 第三方异常类(future SDK)被 default 桶 swallow | accept | runner_bug 是显式 default;§6.6 根因诊断剧本要求 root-cause 后给具体 sub-cause |
</threat_model>

<verification>
- pytest 全套绿,无 phase-7 baseline 回归
- grep 完整性:failure_class 在 4 个生产文件中按预期分布;classify 行为不变
- 实测证据(post-batch,D8-ACC-3):batch 结束后,
  - `find tests/smoke/.runs/<phase-8-ts>/ -name index.json -exec jq '.requests[].failure_class' {} \;` 每行非 null,值 ∈ 7 enum
  - `jq '.by_failure_class | to_entries | map(.value) | add' batch_summary.json` == `jq '.totals.requests' batch_summary.json`
</verification>

<success_criteria>
- D8-E 落地:7 标签可读分类 + 聚合
- D-19 隔离:classify 三标签未变更
- D-22d 隔离:capture 层零改动
- 算法路径 vs 启发式:dispatch table + dict 累加,符合用户审美约束
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-03-SUMMARY.md`,记录 commit SHA + 7 个文件改动 + grep 计数 + 单测列表。
</output>
