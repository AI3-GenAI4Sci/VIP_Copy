---
phase: 08-evolution-wiring-and-runner-debt
plan: 13
type: execute
wave: 7
depends_on:
  - 08-12
files_modified:
  - .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md
  - .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md
autonomous: false
requirements: []
must_haves:
  goal: "Phase 8 closeout:(1) 更新 07-WRIN-TRIAGE.md 7 个 scheduled 项目状态为 phase-8 commit refs(D8-ACC-5);(2) 按 08-VALIDATION.md sign-off 模板写 08-VERIFICATION.md,含 M1-M5 实测结果 quote + audit 状态 + 全部 D8-ACC-1..6 acceptance gate"
  truths:
    - "07-WRIN-TRIAGE.md 表格中 WR-01 / WR-02 / WR-03 / WR-04 callsite / WR-05 / IN-01 / IN-08 共 7 项:scheduled (phase 8) → phase-8 commit SHA"
    - "07-WRIN-TRIAGE.md frontmatter items_fixed 从 7 升至 14,items_scheduled 从 7 降至 0"
    - "08-VERIFICATION.md 按 08-VALIDATION.md sign-off 模板填齐(Wave 0 / sampling / manual / approval pending)"
    - "08-VERIFICATION.md 含 M1-M5 实测结果(quote batch_summary.json.behavioral_metrics 字段值)"
    - "08-VERIFICATION.md 含 D8-C audit 状态 quote(从 08-12-SUMMARY.md 引用)"
    - "08-VERIFICATION.md 状态字段(frontmatter)= passed | gaps_found | conditional 三选一,基于 M1-M5 实测与 audit 综合判定"
  artifacts:
    - path: .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md
      provides: "7 个 scheduled 项 → 各自 phase-8 commit SHA"
      contains: "phase 8"
    - path: .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md
      provides: "phase-8 acceptance sign-off doc 含 D8-ACC-1..6 + M1-M5 实测 + C audit 状态"
      contains: "D8-ACC-"
  key_links:
    - from: 07-WRIN-TRIAGE.md disposition table
      to: phase-8 commits
      via: "每个 scheduled → 对应 phase-8 plan 落地 commit SHA(plans 04 / 05 / 06 / 08 / 09 / 10 / 11)"
      pattern: "phase[- ]?8.*commit"
    - from: 08-VERIFICATION.md acceptance gates
      to: 08-VALIDATION.md sign-off template
      via: "verbatim 模板字段填值"
      pattern: "D8-ACC-[1-6]"
    - from: 08-VERIFICATION.md M1-M5 status
      to: batch_summary.json.behavioral_metrics
      via: "quote 数值 + 阈值比较"
      pattern: "behavioral_metrics"
  forbid_list:
    - "禁止 把 phase 8 状态标 passed 当 M1-M5 任一阻塞 metric 未达且 user 未显式接受软告警 —— 按 RESEARCH §5.4b 阻塞规则严守"
    - "禁止 编辑除 07-WRIN-TRIAGE.md 与 08-VERIFICATION.md 之外的任何其他文件 —— closeout 只动 doc,无代码"
    - "禁止 reopens phase 7 acceptance gate —— 它的关闭是 phase 7 follow-up 步骤,phase 8 closeout 只 unblock 它"
    - "禁止 在 08-VERIFICATION.md 列任何未在 batch_summary.json 中实际出现的 metric 值 —— 不伪造,实测证据严守 D8-VAL-REAL"
---

<objective>
Phase 8 closeout 文档同步。两个动作:(1) 更新 07-WRIN-TRIAGE.md 7 个 scheduled 项目 → phase-8 commit refs;(2) 按 08-VALIDATION.md sign-off 模板写 08-VERIFICATION.md,含 D8-ACC-1..6 acceptance gate + M1-M5 实测 quote + D8-C audit 状态。

Purpose: D8-ACC-5 + D8-ACC-6 — 两个 final acceptance gate 在本 plan 完成。无代码改动,只文档同步。
Output: 单一 atomic commit;两个 .md 文件改动。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-VALIDATION.md
@.planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 07-WRIN-TRIAGE.md 7 个 scheduled 项 → phase-8 commit SHA</name>
  <files>.planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md</files>
  <read_first>
    - .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md(整文件 — frontmatter + disposition register table)
    - 08-04 / 08-05 / 08-06 / 08-09 / 08-10 / 08-11 已落地的 commit SHA(可从 `git log --oneline` 或各 SUMMARY.md 取)
  </read_first>
  <action>
    Edit `.planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md`:

    **1. frontmatter 更新:**
    - `last_updated: 2026-05-27T02:55:00Z` → 改为本 closeout 时间
    - `status: closed (commits landed; remainder scheduled to phase 8)` → 改为 `closed (all items landed via phase 8 commits)`
    - `items_fixed: 7` → `14`
    - `items_scheduled: 7` → `0`

    **2. Disposition register table 更新 7 行(以下行的 Decision / Commit / Reference 列):**

    | ID | 当前 Decision | → 新 Decision | 新 Commit / Reference |
    |---|---|---|---|
    | WR-01 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-09 commit SHA — Stage 3 fail-fast drain in-flight |
    | WR-02 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-10 commit SHA — finally best-effort wrap |
    | WR-03 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-04 commit SHA — remove dup _detect_delimiter |
    | WR-04(callsite) | scheduled (phase 8) | **fixed (phase 8)** | plan 08-04 commit SHA — migrate to reset_current_node_id helper |
    | WR-05 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-11 commit SHA — narrow trial_runner except |
    | IN-01 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-06 commit SHA — token_cost_observed from runtime.trace |
    | IN-08 | scheduled (phase 8) | **fixed (phase 8)** | plan 08-04 commit SHA — max_retries kwarg |

    具体 commit SHA 从各 plan 的 SUMMARY.md 取(或 `git log --oneline --all` 找)。若 SHA 暂不可知(本 plan 在最末,前 11 个 plan 已 land),用 `git log --oneline -n 30` 找。

    **3. Net 段更新:**
    ```
    - fixed-now/phase-8: 14 commits total
    - scheduled (phase 8): 0
    - waived: 0
    ```

    **4. Status 段追加一行:**
    ```
    - <closeout-timestamp> — phase 8 closeout. All 7 scheduled items landed in phase-8 commits (see plan references above). 07-WRIN-TRIAGE.md now fully resolved.
    ```

    **禁止** 改其他行或语义。
  </action>
  <verify>
    <automated>grep -c "scheduled (phase 8)" .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md ; grep -c "fixed (phase 8)" .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md ; grep -c "items_scheduled: 0" .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md ; grep -c "items_fixed: 14" .planning/phases/07-real-llm-validation/07-WRIN-TRIAGE.md</automated>
    <human-check>`scheduled (phase 8)` 在文件中出现 == 0(全部迁出);`fixed (phase 8)` 出现 == 7;`items_scheduled: 0` 出现 == 1;`items_fixed: 14` 出现 == 1。</human-check>
  </verify>
  <done>
    - 07-WRIN-TRIAGE.md frontmatter items_fixed = 14, items_scheduled = 0
    - Disposition register table 7 行 Decision 列从 scheduled → fixed (phase 8) + commit SHA
    - Net 段更新
    - Status 段追加 closeout 行
  </done>
</task>

<task type="auto">
  <name>Task 2: 写 08-VERIFICATION.md(按 08-VALIDATION.md sign-off 模板填齐)</name>
  <files>.planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-VALIDATION.md(完整 sign-off 模板 — 特别 Per-Task Verification Map + Validation Sign-Off check list)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§5.4 实测证据矩阵 + §5.4b M1-M5 阻塞规则 + §5.5 acceptance gate)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md(D8-ACC-1..6 完整 spec)
    - tests/smoke/.runs/<phase-8-ts>/stage{N}/batch_summary.json(quote M1-M5 实测值)
    - tests/smoke/.runs/<phase-8-ts>/stage{N}/index.json(verify failure_class per row)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-12-SUMMARY.md(quote C audit 状态)
  </read_first>
  <action>
    创建新文件 `.planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md`。结构按 08-VALIDATION.md sign-off 模板:

    **Frontmatter:**
    ```
    ---
    phase: 8
    slug: evolution-wiring-and-runner-debt
    status: <passed | gaps_found | conditional>   # 三选一,基于 M1-M5 + audit 综合判定
    nyquist_compliant: true                        # 设为 true 当 D8-ACC-1..6 全过
    closeout_date: <YYYY-MM-DD>
    batch_ts: <phase-8 batch timestamp from tests/smoke/.runs/>
    ---
    ```

    **正文段落顺序:**

    1. **Summary** — 一段 prose(中文)说明 phase 8 落地状态。

    2. **D8-ACC-1..6 acceptance gate(checklist,逐项 quote 证据):**

       - **D8-ACC-1** 一次 Stage 1+2+3 batch 在单一 phase-8 commit 上 end-to-end 完成,零请求因 60s/stale-env/未处理 transient 而 fail-fast。
         - quote `tests/smoke/.runs/<ts>/.run-logs/runner-*.log` 启动 commit SHA
         - quote `index.json.requests | length` 与各 stage 计数(stage1=1, stage2=20, stage3=20)
         - 列出 `by_failure_class` 字典原文,确认无 unhandled transient

       - **D8-ACC-2** `evolution_snapshot.json` 含 ≥ 1 个非空 `trials[]`(F 接线 firing)。
         - quote `find tests/smoke/.runs/<ts>/ -name evolution_snapshot.json` 中 trials 数 ≥ 1 的样本
         - 列出 trial.delta_id 来源:distill agent 产出而非 hardcoded(per plan 08-05 forbid_list)

       - **D8-ACC-3** `index.json` 每行有 `failure_class`;`batch_summary.json.by_failure_class` 完整。
         - 运行 `jq '.requests[].failure_class' index.json` 验证 100% rows 非 null
         - quote `by_failure_class` 字典 + `sum(values) == totals.requests` 自校验

       - **D8-ACC-4** pytest 全套通过 + M1-M5 新单测全绿。
         - quote pytest exit code + 测试计数(目标 268+ tests, 0 fail, 0 skip)

       - **D8-ACC-5** `07-WRIN-TRIAGE.md` 7 个 scheduled 项目 → phase-8 commit refs。
         - quote 本 plan task 1 的 grep 结果

       - **D8-ACC-6** 本 08-VERIFICATION.md status = passed —— 综合判定栏位

    3. **M1-M5 行为度量实测**(从 batch_summary.json.behavioral_metrics quote 逐项):

       | Metric | 实测值 | 阈值 | 是否达阈值 | 阻塞? |
       |---|---|---|---|---|
       | M1 factor_count_p50 | `<quote>` | ≥ 3 | yes/no | 阻塞 |
       | M2 factor_diversity_score | `<quote>` | ≥ 0.5 | yes/no | 阻塞 |
       | M3a copy_candidate_count_p50 | `<quote>` | ≥ 2 | yes/no | 阻塞 |
       | M3b reflection_triggered_when_underspec_rate | `<quote>` | ≥ 0.8 | yes/no | 阻塞 |
       | M4 delta_diversity_score.count | `<quote>` | ≥ 1 | yes/no | 阻塞 |
       | M4 delta_diversity_score.unique_targets | `<quote>` | count≥2 时 ≥2 | yes/no | 软告警 |
       | M5 trial_belief_update_count | `<quote>` | > 0 | yes/no | 阻塞 |

       任一阻塞 metric 未达 + user 未显式接受软告警 → status = `gaps_found`,列具体 metric 未达项 + 建议路径(重跑 / 修代码后重跑 / user 显式接受)。

    4. **D8-C audit 状态** — quote 08-12-SUMMARY.md 的 audit 判定:
       - pass:列具体 request_id 与 (a)(b)(c) 三条件
       - fail:列 (a)(b)(c) 哪一条不成立 + 新 WR 项 草案
       - 条件未触发:声明 batch 中无截断自然发生,pytest 是唯一证据

    5. **Findings**(若有):任何未达阈值 metric + audit fail + 实测发现的新 WR 项,列在此段。

    6. **Approval**:
       - 若 status=passed:`Approved <date> — phase 8 acceptance gate D8-ACC-1..6 全过,M1-M5 全达阈值,07-WRIN-TRIAGE.md 全部 14 项关闭。Phase 7 acceptance gate 可由后续 follow-up 步骤关闭(per scope_fence)。`
       - 若 status=gaps_found:`Pending user review — <list of gaps>。`

    **禁止** 伪造证据(D8-VAL-REAL)。**禁止** 列任何未在 batch_summary.json 中实际出现的 metric 值。
  </action>
  <verify>
    <automated>test -f .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md && echo "exists" ; grep -c "D8-ACC-" .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md ; grep -c "behavioral_metrics" .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md ; grep -c "status:" .planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md</automated>
    <human-check>08-VERIFICATION.md 存在;`D8-ACC-` 出现 ≥ 6(每个 gate 至少 1 次);`behavioral_metrics` 出现 ≥ 1(M1-M5 段);`status:` 出现 1(frontmatter)。手 verify M1-M5 表格中实测值是真从 batch_summary.json quote 而非编造。</human-check>
  </verify>
  <done>
    - 08-VERIFICATION.md 按 08-VALIDATION.md sign-off 模板填齐
    - D8-ACC-1..6 6 个 gate 各有 quote 证据
    - M1-M5 表格含真实测值(从 batch_summary.json quote)+ 阈值 + 判定
    - D8-C audit 状态从 08-12-SUMMARY.md quote
    - status 字段 = passed | gaps_found | conditional 三选一,有显式理由
    - 若 gaps_found:Findings 段含具体未达项 + 建议路径
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User review — 确认 08-VERIFICATION.md status 与 M1-M5 实测一致</name>
  <what-built>
    Task 1 已更新 07-WRIN-TRIAGE.md;Task 2 已写 08-VERIFICATION.md 含 M1-M5 实测 + D8-C audit + D8-ACC-1..6 acceptance gate。
  </what-built>
  <how-to-verify>
    1. 读 08-VERIFICATION.md M1-M5 表格,与 `jq '.behavioral_metrics' tests/smoke/.runs/<ts>/stage2/batch_summary.json` 实测对照,确认无伪造
    2. 检查 status 字段:
       - 若 M1-M5 全达阈值 + audit pass + 所有 D8-ACC-1..6 过 → status 应为 `passed`
       - 若任一阻塞 metric 未达 → status 应为 `gaps_found`,Findings 含具体项
       - 若 M4 软告警 (unique_*) 触发 → user 决定接受 / reopen
    3. 确认 07-WRIN-TRIAGE.md 7 个项目都有 phase-8 commit SHA(不是 placeholder)
    4. 若 user 接受 → Type "approved";若需重跑 batch / 修代码 → Type "reopen <reason>"
  </how-to-verify>
  <resume-signal>Type "approved" 或 "reopen <reason>"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance doc ↔ batch evidence | VERIFICATION.md 是 phase 8 sign-off doc;若 quote 失真,phase 8 acceptance 失真 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13-01 | Repudiation | acceptance status 标 passed 实则 M1 未达 | mitigate | Task 3 user review checkpoint blocking;forbid_list 禁伪造 |
| T-08-13-02 | Tampering | 偷偷把阈值在 doc 中下调以让 metric 达标 | mitigate | 阈值在 RESEARCH §5.4b 锁定,doc 复述阈值时引用 RESEARCH;改阈值 require 改 RESEARCH 触发 audit |
</threat_model>

<verification>
- 07-WRIN-TRIAGE.md 7 个 scheduled → fixed (phase 8) 全部更新
- 08-VERIFICATION.md 存在且按 08-VALIDATION.md 模板填齐
- 实测证据(D8-ACC-5 + D8-ACC-6):本 plan 落地即 D8-ACC-5 / D8-ACC-6 满足
- user review checkpoint 通过(Task 3)
</verification>

<success_criteria>
- D8-ACC-5 落地:07-WRIN-TRIAGE.md 7 项移到 phase-8 commit refs
- D8-ACC-6 落地:08-VERIFICATION.md 状态确定 + 含 M1-M5 实测 + audit 状态
- 无代码改动,只文档同步
- 无伪造证据(D8-VAL-REAL 严守)
- Phase 8 closeout 完成,phase 7 acceptance gate 可由后续 follow-up 步骤关闭(per scope_fence)
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-13-SUMMARY.md`,记录:
- 07-WRIN-TRIAGE.md frontmatter delta + 7 项 commit refs 表
- 08-VERIFICATION.md status (passed / gaps_found / conditional)
- M1-M5 实测值 + 阈值判定
- D8-C audit 状态
- user review 结果(approved / reopen)
- Phase 8 整体 closeout 状态:complete / needs-rerun / needs-fix
</output>
