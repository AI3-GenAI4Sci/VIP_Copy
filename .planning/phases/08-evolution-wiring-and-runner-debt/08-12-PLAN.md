---
phase: 08-evolution-wiring-and-runner-debt
plan: 12
type: execute
wave: 7
depends_on:
  - 08-11
files_modified:
  - seers_harness/provider_runtime/openai_compatible.py
autonomous: false
requirements: []
must_haves:
  goal: "落地 D8-C:CR-05 parse-retry 路径 verify-only audit。读 phase-8 final batch 的 .run-logs/runner-*.log,确认在 ≥1 tool_call.arguments 截断事件中 parse-retry 行为符合 openai_compatible.py:73-103 的契约(retry budget 内成功 OR raise ProviderResponseError 走 fail-fast)。若 audit 发现 parse-retry 路径无 log 行可观测,补 1 行 audit log。贡献 D8-ACC-1(audit 通过 = 截断事件不会让请求挂在 budget 边界)。"
  truths:
    - "C 是 audit task,不是 implementation —— 大部分内容是读 batch log + 分类 outcome,可能产生 0-line code commit"
    - "若 batch 中确实出现截断但 log 无 parse_retry 行 → 补 1 行 print 到 openai_compatible.py:_parse_args except 分支(单行,审计性质,非业务变更)"
    - "audit pass 条件:(a) batch ≥1 请求 trace 显示 tool_call.arguments 截断;(b) 该请求最终返回 valid tool_calls_out OR raise ProviderResponseError;(c) D-19 路由把 ProviderResponseError 标 provider_error 走 fail-fast"
    - "audit fail 时不在 phase 8 修补,记录到 08-VERIFICATION.md Findings 作为新 WR 项归档,phase 8 仍以 audit 完成 收尾"
    - "前置:phase-8 batch 必须至少包含 1 个 tool_call.arguments 截断事件 — 若 Stage 1+2+3 自然 batch 中未出现截断,等待自然出现,绝不主动注入截断(注入证明不了真 DeepSeek 截断面前的行为)"
  artifacts:
    - path: seers_harness/provider_runtime/openai_compatible.py
      provides: "(可选,仅当 audit 发现 parse-retry 路径无 log 行)在 except ProviderResponseError as exc 分支补 1 行 `print(f'[provider] parse_retry node={node_id} attempt={_attempt+1}/{_parse_max_retries()+1}', file=sys.stderr)`"
      contains: "parse_retry"
  key_links:
    - from: batch .run-logs/runner-<ts>.log
      to: openai_compatible.py:73-103 parse_retry loop
      via: "grep parse_retry 文本 验证 CR-05 行为"
      pattern: "parse_retry"
  forbid_list:
    - "禁止 主动注入截断 — CR-05 路径只对真实 DeepSeek 截断有意义"
    - "禁止 把 _parse_max_retries 默认值从 3 改大 — D-22 / CR-05 已定型,audit 不动 contract"
    - "禁止 在 _parse_args 中尝试修复 JSON(prepend `{`、补 `}` 等)— 引入未审计的 LLM-output mutation,违反 D-22"
    - "禁止 audit 失败时在 phase 8 直接补代码 — 记录到 08-VERIFICATION.md 的 Findings 作为新 WR 项,phase 8 仍以 audit 完成 收尾"
    - "禁止 在 audit log 行打出 _parse_args 的 raw exc message — print 只含 node_id + attempt 数字"
---

<objective>
落地 D8-C:CR-05 parse-retry 路径 verify-only audit。本 plan 是 phase 8 *唯一* 的 audit task(非 implementation)。读 phase-8 final batch 的 .run-logs,确认 CR-05 在真实 DeepSeek tool_call.arguments 截断面前行为符合契约。**大部分 plan 内容是读 log + classify outcome;可能产生 0-line code commit。**

Purpose: D8-C 决策锁定 verify-only;CR-05 路径(`openai_compatible.py:73-103`)在 phase 7 已落地,但从未在真实 batch 中通过 audit。phase-8 batch(F 接线 + B backoff 后)是首次有机会观察。若 audit 发现 parse-retry 路径无 log 可观测,补 1 行 print 让审计本身可观测。
Output: 大概率 0-line code commit(audit pass);若 audit 发现 log 缺失,1-line print 加到 openai_compatible.py。无论哪种情况,都产生 08-12-SUMMARY.md 记录 audit 判定 + log grep 结果。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-CONTEXT.md
@.planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md
@seers_harness/provider_runtime/openai_compatible.py
@.planning/phases/07-real-llm-validation/07-REVIEW.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 等待 phase-8 final batch 完成且其中 ≥1 截断事件出现</name>
  <what-built>
    Plan 08-01 ~ 08-11 全部落地后,operator 跑 phase-8 final batch:`python -m seers_harness.validation.runner --env-file .env.local`。
    本 task 是 audit 的 *前置等待*:batch 必须包含至少 1 个 `tool_call.arguments` 截断事件(自然出现,禁止主动注入)。
  </what-built>
  <how-to-verify>
    1. Phase-8 final batch 完成,batch 输出位于 `tests/smoke/.runs/<phase-8-ts>/`
    2. 运行 grep 探测截断信号:
       ```
       grep -E "Failed to parse tool_call.arguments|truncat|JSONDecodeError" tests/smoke/.runs/<phase-8-ts>/.run-logs/runner-*.log
       ```
    3. 若 grep 命中 ≥1 行 → audit 可进行,Type "audit-ready" 进入 Task 2
    4. 若 0 命中 → batch 中无截断事件,本 phase 不强制硬等;Type "no-truncation-observed" 进入 Task 3(标 audit 为 "条件未触发 — 待后续 batch 验证")
  </how-to-verify>
  <resume-signal>Type "audit-ready"(已观察截断)或 "no-truncation-observed"(本 batch 无截断)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: audit 步骤 — 读 log + classify outcome + 决定是否补 audit log 行</name>
  <files>seers_harness/provider_runtime/openai_compatible.py</files>
  <read_first>
    - seers_harness/provider_runtime/openai_compatible.py(line 66-103 `_parse_args` + parse_retry 循环;特别 line 88-103 except ProviderResponseError 分支)
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§2-C 完整审计步骤 1-4 + 陷阱清单)
    - .planning/phases/07-real-llm-validation/07-REVIEW.md(CR-05 定义)
    - tests/smoke/.runs/<phase-8-ts>/.run-logs/runner-*.log(grep 截断与 parse_retry 相关行)
    - tests/smoke/.runs/<phase-8-ts>/stage{N}/<rid>/evidence/<node>/messages.jsonl(raw model output,看截断详情)
  </read_first>
  <action>
    按 RESEARCH §2-C 审计步骤 1-4 执行:

    **步骤 1 — 搜索 parse_retry 文本:**
    ```
    grep -E "parse_retry" tests/smoke/.runs/<phase-8-ts>/.run-logs/runner-*.log | head -20
    ```

    **步骤 2 — 决定是否补 audit log:**
    - 若 grep 命中 ≥1 行 → `openai_compatible.py:73-103` 已经在打 log,**0-line code commit**,跳到步骤 3
    - 若 grep 0 命中 *但* batch 中确实出现了截断(Task 1 已确认):这是 *审计失败信号 1* — CR-05 路径无可观测 log。补 1 行(单行,审计性质):
      在 `except ProviderResponseError as exc:` 分支(line 88 附近)添加:
      ```
      print(f"[provider] parse_retry node={node_id} attempt={_attempt+1}/{_parse_max_retries()+1}", file=sys.stderr)
      ```
      位置:紧贴 `except ProviderResponseError as exc:` 之后第一行。
      **禁止** 打出 exc message(per forbid_list,可能含 secret/payload);只打 node_id + attempt 计数。
      **禁止** 改 `_parse_max_retries` 默认值。
      **禁止** 在 _parse_args 中尝试修复 JSON。

    **步骤 3 — audit 通过判定**(对 ≥1 截断请求执行):
    定位 batch 中含截断的请求 ID,组装如下证据:
    - (a) 该请求的 `evidence/<node>/messages.jsonl` 含截断行(可见 tool_call.arguments 中字段不完整,如 "evidence_refs:" 单字段无值)
    - (b) 该请求最终结果二选一:
       - 成功返回 valid tool_calls_out → 对应 batch 中该请求 `evidence/<node>/tool_calls.jsonl` 含有效 tool_call entry,且 retry budget 未耗尽
       - 或 raise ProviderResponseError → 对应 `index.json` 中该 row `exception` 非 null,`failure_class == "malformed_tool_args"`(plan 08-03 落地)
    - (c) 如果是 fail-fast 路径:`index.json` row `failure_class == "malformed_tool_args"`,且后续 stage 因 fail-fast 中断(D-19 路由把 ProviderResponseError 标 provider_error → fail-fast,见 `exception_classifier.py:90-95`)

    若 (a)+(b)+(c) 全成立 → **audit 通过**。
    若 (a) 成立但 (b)(c) 中有任一不成立 → **audit 失败**:不要在 phase 8 修补(per forbid_list),记录到 08-VERIFICATION.md Findings 作为新 WR 项归档(plan 08-13 已含 closeout 编辑该文件的步骤)。

    **步骤 4 — audit 结果文档化:**
    无论 pass / fail / 0-line / 1-line,都写 audit 判定到 08-12-SUMMARY.md(本 plan output 段),供 plan 08-13 closeout 引用。
  </action>
  <verify>
    <automated>grep -c "parse_retry" seers_harness/provider_runtime/openai_compatible.py ; pytest -q tests/test_provider_openai_compatible.py -x 2>&1 | tail -10</automated>
    <human-check>若决定补 1 行:`parse_retry` 在 openai_compatible.py 出现 ≥ 1 次;若 0-line commit:`parse_retry` 出现次数与 plan 08-01 落地后一致;pytest 全套不回归。audit 判定(pass/fail)在 08-12-SUMMARY.md 落字。</human-check>
  </verify>
  <done>
    - audit 判定结论(pass / fail)落字到 08-12-SUMMARY.md
    - 若补 1 行 audit log:`parse_retry` print 行在 openai_compatible.py except 分支落地,**仅 node_id + attempt 数字,不含 exc message**
    - 若 0-line commit:`git diff` 显示无生产代码改动,SUMMARY.md 单独 commit 记录 audit
    - audit fail → 新 WR 项内容写到 08-VERIFICATION.md Findings(plan 08-13 编辑该文件)
    - pytest 全套不回归
  </done>
</task>

<task type="auto">
  <name>Task 3: 若无截断 — audit 标记为"条件未触发,待后续 batch 验证"</name>
  <files>seers_harness/provider_runtime/openai_compatible.py</files>
  <read_first>
    - .planning/phases/08-evolution-wiring-and-runner-debt/08-RESEARCH.md(§5.4 实测证据矩阵 C 行:若 batch 无截断,以 "条件未触发" 收尾)
  </read_first>
  <action>
    本 task 仅在 Task 1 输出 "no-truncation-observed" 时触发。

    1. **不做任何代码改动** — `openai_compatible.py` 零修改。
    2. 在 08-12-SUMMARY.md 标:audit 条件未触发 — phase-8 batch 中无 tool_call.arguments 截断自然发生;CR-05 路径在本 phase 不能用实测证明 firing。pytest tests/test_provider_openai_compatible.py 中已有的契约测试是唯一证据。
    3. 在 08-VERIFICATION.md Findings 中加一条 "C audit 条件未触发,转至后续 batch 自然观察" — plan 08-13 closeout 时一并写入(本 task 不直接编辑 08-VERIFICATION.md,避免与 plan 08-13 冲突)。
    4. **禁止** 把 audit 标 "pass" — 没有截断证据不算 pass,只能标 "条件未触发"。
    5. **禁止** 主动注入截断生成证据 — per forbid_list。
  </action>
  <verify>
    <automated>git diff --stat seers_harness/provider_runtime/openai_compatible.py</automated>
    <human-check>`git diff --stat seers_harness/provider_runtime/openai_compatible.py` 显示 0 lines changed;08-12-SUMMARY.md 含 "条件未触发"。</human-check>
  </verify>
  <done>
    - openai_compatible.py 零改动(git diff 验证)
    - 08-12-SUMMARY.md 标 audit 状态为 "条件未触发,转至后续 batch 自然观察"
    - phase 8 acceptance 中 C 项以 pytest 单测为唯一证据(per RESEARCH §5.4 C 行)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit operator ↔ batch log | operator 读 .run-logs 决定 audit 状态;若日志含 secret,safe_exc 已 redacted;但 audit 输出本身不复制 raw log 到 SUMMARY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12-01 | Information Disclosure | audit log 行打出 exc message 含 raw model output 含 PII | mitigate | print 仅含 node_id + attempt 计数,forbid_list 禁打 exc message |
| T-08-12-02 | Repudiation | audit 标 pass 但未实际验证 (b)(c) 条件 | mitigate | done 项要求 SUMMARY.md 列出具体证据(request_id + evidence path + failure_class 值),不能空过 |
| T-08-12-03 | Tampering | audit fail 时偷偷在 phase 8 补代码 | mitigate | forbid_list 显式禁;closeout plan (08-13) 把 audit fail 写到 Findings 作新 WR 项 |
</threat_model>

<verification>
- 本 plan 大概率 0-line code commit;若补 1 行 audit log,grep `parse_retry` 在 openai_compatible.py 出现 ≥ 1
- pytest 全套不回归
- 实测证据(D8-C):见 Task 2 步骤 3 (a)(b)(c) 三条件
- audit 判定(pass / fail / 条件未触发)落字到 08-12-SUMMARY.md,作为 phase 8 acceptance evidence
</verification>

<success_criteria>
- D8-C 落地:CR-05 路径在真实 DeepSeek 截断面前的行为通过 audit(或显式标 audit fail/条件未触发)
- 无主动注入,无 contract 改动(_parse_max_retries / _parse_args 字节级不变)
- audit log 行(若补)只含 node_id + attempt,无 exc message leak
- audit fail → 新 WR 项归档,phase 8 仍以 audit 完成 收尾
</success_criteria>

<output>
完成后写 `.planning/phases/08-evolution-wiring-and-runner-debt/08-12-SUMMARY.md`,记录:
- audit 判定:pass / fail / 条件未触发(三选一)
- 是否补 1 行 audit log:yes (commit SHA) / no
- 若 pass:列具体 request_id + evidence path + failure_class 值
- 若 fail:列 (a)(b)(c) 哪一条不成立 + 新 WR 项 草案
- 若条件未触发:确认 0-line commit + pytest 是唯一证据
</output>
