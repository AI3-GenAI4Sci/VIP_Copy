---
context: phase
phase: 08-evolution-wiring-and-runner-debt
task: 08-G5-recovery
total_tasks: 5
status: gaps_found
last_updated: 2026-05-28T16:07:42Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These constraints were learned from failed Phase 8 recovery runs. A new session must explicitly honor them before continuing.

- [ ] CONSTRAINT: Real Stage 3 is the acceptance gate - local pytest passing is only a precondition. Phase 8 cannot pass until a real DeepSeek Stage 3 run completes and the signed evidence gates pass.
- [ ] CONSTRAINT: Do not accept portfolio visibility as trial wiring - visible deltas in `evolution_snapshot.json` prove propagation, but not trial selection, journal/status transitions, or patched-prompt execution.
- [ ] CONSTRAINT: Trial patching must be real mechanism - patched trials must reach model prompts through isolated `skill_root`; baseline/control must remain production prompt and must not pollute `trials[]`.
- [ ] CONSTRAINT: No static magic values or heuristic fake signals - trial gate pressure and selection evidence must come from runtime-observable state, not hardcoded probabilities or convenience branches.
- [ ] CONSTRAINT: Real-provider malformed tool JSON is a live blocker - do not paper over it as success. Diagnose from raw provider response, parse retry behavior, and node prompt/tool contracts.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Pytest closure | Treating `387 passed` as Phase 8 acceptance | blocking | Require real Stage 3 artifacts and `08-VERIFICATION.md` gate update |
| Visibility-only closure | Counting non-empty `delta_portfolio_before` as selected trials | blocking | Inspect `trials[]`, `trial_gate`, `portfolio_journal.jsonl`, and status transitions |
| Prompt-disconnected trials | Patching temp files without routing runtime skill loading to them | blocking | Verify `WorkflowRuntime.skill_root` / trial temp root evidence |
| Static trial pressure | Using hardcoded values to force or suppress trial selection | blocking | Inspect runtime-derived `trial_gate` fields in snapshots |
| Acceptance laundering | Calling an external provider parse failure a pass without signed decision | blocking | Keep `gaps_found` unless user explicitly accepts a scoped external limitation |

<current_state>

Phase 08 is still executing and currently `gaps_found`.

Latest committed recovery work:
- `8b0749e fix(08-recovery): surface trial gate evidence`
- `4d0bafb docs(08-recovery): record latest stage3 failure`
- `fc686da fix(08-recovery): wire trial skill roots`
- `786308d fix(08-recovery): surface stage3 portfolio evidence`

Latest local verification:
- Full local suite passed: `387 passed in 46.65s`.
- Latest real Stage 3 batch: `20260528T070707Z`.
- Batch reached real Stage 3 with `n=20 concurrency=20`.
- Bootstrap succeeded and distillation produced 2 deltas: `d1_brand_register_awareness`, `d2_sensory_anchor_requirement`.
- Stage 3 produced 19 OK rows and 1 `malformed_tool_args` failure at `factor_discovery` request `-6833932762567548368`.
- All 20 snapshots showed visible bootstrap deltas, proving portfolio propagation.
- `trials[]` total remained 0.
- `portfolio_journal.jsonl` was absent.
- Copy `prompt_cache_miss_tokens` mean was `16328.11`, above signed target `[500, 5000]`.

The user has now said quota/balance was replenished. The next session should run a fresh real Stage 3 after commit `8b0749e` so the new `trial_gate` observability can explain why trials are or are not selected.

</current_state>

<completed_work>

- Stage3-only bootstrap now runs current-code trace and distills portfolio.
- Evidence writer aggregates multiple provider turns and writes `usage_turns.jsonl`.
- 402 / insufficient balance is classified as non-retryable `auth`.
- Trial patching reaches model prompts through isolated `skill_root`.
- Baseline/control stays on production prompt and no longer pollutes `trials[]`.
- Trial snapshot rows include `delta_id`.
- Journal uplift includes artifact-derived behavioral metrics.
- Trial RNG default is no longer fixed `Random(0)`; reproducible seed is available via `SEERS_TRIAL_RNG_SEED`.
- `evolution_snapshot.json.trial_gate` now records `recent_failure_rate`, `token_budget_pressure`, `production_pressure`, `trial_prob`, `eligible_delta_count`, and `selected_delta_id`.

</completed_work>

<remaining_work>

Immediate recovery work:
- Run a fresh real DeepSeek Stage 3 with `set -o pipefail`.
- Inspect `batch_summary.json`, `index.json`, request `evolution_snapshot.json` files, `trial_gate` fields, `portfolio_journal.jsonl`, and copy `usage.json` / `usage_turns.jsonl`.
- If Stage 3 still fails on `malformed_tool_args`, diagnose and fix the `factor_discovery` prompt/tool contract instead of marking passed.
- If `trial_gate.trial_prob` is 0 or very low, identify which pressure source caused it.
- If `eligible_delta_count` is 0, inspect distilled deltas' `target_skill`, `applicable_surface`, and status.
- If trials are selected, verify patched-prompt execution, `delta_id` rows, journal/status transitions, and uplift metrics.
- Re-evaluate the cache-miss target only with real aggregate evidence; do not silently move the band.
- Only after automated gates pass, run the locked manual copy-quality spot-check of 5 sampled outputs.
- Only after that, close the Phase 7 WR/IN scheduled items to real Phase 8 commit refs.

</remaining_work>

<decisions_made>

- Phase 8 remains the right place for this recovery; do not open Phase 9.
- Real Stage 3 artifacts are the acceptance evidence; local tests are necessary but insufficient.
- Trial work must be mechanism-connected through runtime skill loading, not just files patched on disk.
- Trial gate observability was added so the next run can explain selection/non-selection rather than guessing.
- Balance/quota failure was external; malformed tool JSON and zero trials remain engineering/acceptance blockers until resolved or explicitly accepted by the user.

</decisions_made>

<blockers>

- Real Stage 3 has not passed since the Phase 8 recovery fixes.
- Latest real run failed on malformed DeepSeek tool-call JSON after 4 parse attempts.
- Trial selection has not been proven in a real run.
- Journal/status transitions have not appeared in real run artifacts.
- Copy cache-miss mean is outside signed target band under current aggregate measurement.
- `07-WRIN-TRIAGE.md` cannot close until Phase 8 acceptance passes.

</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` - current project state and active watchlist.
2. `.planning/phases/08-evolution-wiring-and-runner-debt/08-VERIFICATION.md` - latest real Stage 3 failure and gates.
3. `.planning/phases/08-evolution-wiring-and-runner-debt/08-G5-SUMMARY.md` - G5 context.
4. `.planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner-20260528T070707Z.log` - latest real Stage 3 log.
5. `tests/smoke/.runs/20260528T070707Z/stage3/batch_summary.json` - failure-class summary.
6. `tests/smoke/.runs/20260528T070707Z/stage3/index.json` - per-request rows.
7. `seers_harness/validation/runner.py` - Stage 3 runner and bootstrap path.
8. `seers_harness/validation/evolution_snapshot.py` - snapshot reducer.
9. `seers_harness/evolution/trial_runner.py` - trial execution and skill-root isolation.
10. `seers_harness/workflow/dag_runner.py` - runtime node execution and skill loading.
11. `seers_harness/provider_runtime/openai_compatible.py` - tool-call argument parse/retry behavior.

## Critical Anti-Patterns (do NOT repeat these)

- Do not mark Phase 8 passed because pytest is green.
- Do not count visible portfolio propagation as selected-trial proof.
- Do not add hardcoded forcing just to make `trials[]` non-empty.
- Do not update `07-WRIN-TRIAGE.md` until Stage 3 and G5 pass.
- Do not touch unrelated dirty/untracked user files while preparing or running recovery.

## Infrastructure State

- Workspace: `/Users/macbook/Documents/VIPS/SEERS-Harness-v3/workspace`
- Branch: `main`
- Current relevant HEAD before this handoff: `8b0749e`
- `.env.local` exists and is used by the real runner.
- User reports quota/balance has been replenished.
- Latest run pointer: `.planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner.latest`
- Real Stage 3 artifacts are under `tests/smoke/.runs/<batch>/`.
- There are many unrelated dirty/untracked files in the working tree; do not revert them.

## Pre-Execution Critique Required

Not required before the next command. The next step is evidence generation by real Stage 3 rerun. Critique becomes required if changing acceptance criteria or reclassifying malformed provider output as an accepted external limitation.

<context>

This session is being paused specifically so a fresh Codex session can continue with GSD. The immediate value of the new session is not more summary; it is a real Stage 3 rerun that uses the latest `trial_gate` evidence. Keep the stance strict: prove mechanism connection through artifacts, then update verification. If the real run fails, document the exact failed gate and fix the root cause.

</context>

<next_action>

Start the new session with:

```bash
$gsd-resume-work 08
```

Then run:

```bash
ts=$(date -u +%Y%m%dT%H%M%SZ)
log=".planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner-${ts}.log"
mkdir -p .planning/phases/08-evolution-wiring-and-runner-debt/.run-logs
printf '%s\n' "$log" > .planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner.latest
set -o pipefail
.venv/bin/python -u -m seers_harness.validation.runner --env-file .env.local --stage 3 2>&1 | tee "$log"
```

After the run, inspect:

```bash
tests/smoke/.runs/<batch>/stage3/batch_summary.json
tests/smoke/.runs/<batch>/stage3/index.json
tests/smoke/.runs/<batch>/stage3/*/evolution_snapshot.json
tests/smoke/.runs/<batch>/portfolio_journal.jsonl
tests/smoke/.runs/<batch>/stage3/*/copy_generation/usage.json
tests/smoke/.runs/<batch>/stage3/*/copy_generation/usage_turns.jsonl
```

</next_action>
