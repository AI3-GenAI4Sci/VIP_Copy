---
context: phase
phase: 08-evolution-wiring-and-runner-debt
task: 08-G5-recovery
total_tasks: 5
status: gaps_found (G5 Stage 3 failed; recovery required)
last_updated: 2026-05-28T04:05:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> 本会话用 4 份 finding (F-08-A/B/C/D) + 5 份 gap-closure plan + G1 完成 + G2-T1 完成并签字落定。
> 每条 constraint 必须 demonstrably 理解后才能继续。

- [ ] CONSTRAINT: 真实 LLM 证据必须 — Phase-8 acceptance 唯一接受门是 G5 真实 DeepSeek Stage 3 (n=20, c=20) batch 跑通,pytest 绿不算。F-08-B 是 unit test 全过但 batch 暴露 LLM 裸奔的经典案例,不要重复。
- [ ] CONSTRAINT: 根因诊断 — 任何 batch 失败必须从 logs / trace / state / provider response 上溯,不在表层补丁。
- [ ] CONSTRAINT: 真机制不假装 — bandit 信号源必须 runtime-observable(rolling deque、Semaphore counter),禁止 hardcoded 0.0 / 启发式 if-elif。
- [ ] CONSTRAINT: SKILL prose 不写用户口头举例 — 用项目术语(disposition / hook / anchor / floor / pattern)统一表达,grep gate 把守"低价大牌"/"周三早9点"/"代理父亲"/"信息饕餮"/"多娃妈妈" 等字面词不出现。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Smoke-test closure | pytest 绿当成接受证据 | blocking | G5 是唯一 acceptance gate;pytest 是 pre-condition,不是 final |
| Surface patching | 修 raise 那一行不溯架构 seam | blocking | 每个 G plan 的 read_first 列指向 finding 文档 + 上游设计文件 |
| 用户举例字面化 | F-08-A 25 angles 是规划材料不是 prose 模板 | blocking | G3 SKILL rewrite acceptance_criteria 含 grep gate(0 hits 5 forbid 词) |
| 静态值伪装真机制 | hardcoded 0.0 trial signals | blocking | G4 truths 显式要求 ProductionSignalWindow / Semaphore 等 runtime source |
| 跨文件契约不一致 | F-08-01 主因:SKILL prose / tool spec / patch helper 三处 target_skill 说法不一 | advisory | G3 acceptance 含 3-site grep 一致性 check |

<current_state>
**Gap-closure 进度: G1-G4 implemented locally; G5 failed; GSD commit-chain recovery required**

- ✅ 08-G1 (commit `a215264`): unified SKILL loader primitive `load_skill_prose` + `NODE_SKILL_BINDING` registry. F-08-B 修复 — `dag_runner.py:84` SKILL_BODY 字面已删除,`_distill_after_stage1` 收编进同一 primitive。pytest 334 green。
- ✅ 08-G2-T1 (commit `33eda34`): `seers_harness/workflow/CONTEXT_DISCLOSURE_ANALYSIS.md` 300 行,3 节 — 字段稳定性枚举 / 信息密度评估(映射 F-08-A 维度 D1..D7)/ 最佳披露边界提案。
- ✅ 08-G2-T1 sign-off (commit `6650872`): 用户 `approve`,字段集合 + SKILL line 41 改写 + cache_miss 目标区间 [500, 5000] 已签字 2026-05-28。
- ✅ 08-G2 local summary exists: context disclosure boundary implemented in local working tree.
- ✅ 08-G3 local summary exists: distill target_skill contract + evidence persistence implemented in local working tree.
- ✅ 08-G4 local summary exists: bandit/journal/status wiring implemented in local working tree.
- ❌ 08-G5 failed: real DeepSeek Stage 3 batch `20260528T032645Z` ended `gaps_found`.

Important recovery caveat: G2-G4 changes and summaries exist locally but are not
represented by a clean GSD task/summary commit chain. Do not mark Phase 8
passed until this anomaly is repaired or explicitly superseded.

HEAD: `3851d4d wip(08-G2): pause-work after T1 sign-off — resume from G2-T2`
pytest: 377 passed after local G4 implementation; rerun required after recovery edits
</current_state>

<completed_work>

Phase-8 main wave (pre-gap-closure):
- 08-01..08-11 全部 done(timeout / env-file / failure_class / runner cleanup / evolution wiring (broken) / token_cost / M1-M5 / backoff / drain / finally / narrow)
- pre-batch-fix: request_preprocessor 模块级 import (commit `cbb6c5e`)
- real-LLM batch (`tests/smoke/.runs/20260527T123110Z/`): Stage 1 PASSED + Stage 2 10/20 + SIGTERM at STOP B (zero trials)

Findings (commit `224a91a`):
- F-08-A: 25 personalization angles, F-08-A.md (454 lines)
- F-08-B: dag_runner.py:84 字面 "SKILL_BODY" — 3 production node 裸奔(deepest root cause)
- F-08-C: copy_payload_for strips user_state, cache_miss=120 tokens
- F-08-D: orphan map of 06-02 functions + 6 plan drafts

Gap-closure plans (commit `66ebf05`):
- 08-G1 ~ 08-G5 全部 PLAN.md ready

Gap-closure execution:
- 08-G1 (commit `a215264`): SKILL loader primitive — DONE
- 08-G2-T1 + sign-off (commits `33eda34`, `6650872`): research + approval — DONE
</completed_work>

<remaining_work>

**Immediate next: 08-G5 recovery, not pass closeout.**

1. Repair GSD commit-chain anomaly:
   - G2-G4 code changes and summaries exist locally but are uncommitted.
   - New G2-G4 tests are ignored by `.gitignore:19` (`tests/`), so any valid commit chain must consciously `git add -f` intended test files or adjust policy.
   - Do not mix pre-existing dirty SKILL files into unrelated commits.
2. Diagnose/fix real Stage 3 gaps from `08-VERIFICATION.md`:
   - malformed `tool_call.arguments` at `factor_discovery` for request `-6833651210813617137`
   - copy_generation prompt cache miss mean 247.42, below signed [500, 5000]
   - zero trial triggering, no trial workspace, no portfolio journal
3. Re-run `.venv/bin/python -m pytest -q`.
4. Re-run real DeepSeek Stage 3 with `.venv/bin/python -u -m seers_harness.validation.runner --env-file .env.local --stage 3`.
5. Only if Stage 3 passes automated gates, present 5 copy_generation samples for user spot-check.
6. Only after pass + spot-check, close `07-WRIN-TRIAGE.md` to actual phase-8 commit refs.

</remaining_work>

<decisions_made>

- 不开 phase 9,gap-closure 留在 phase 8
- SKILL loader 是 single primitive,绑定是 extension layer
- Context disclosure 是信息论问题,先研究再实现 (sign-off gate 已过)
- SKILL prose 不写用户口头举例
- 真机制 only,信号源 runtime-observable
- Stage 3 acceptance(skip Stage 1/2)
- run-without-stop 直到 Stage 3 PASSED;唯一 checkpoint 已过(G2 sign-off);剩唯一 checkpoint 是 G5 user spot-check
</decisions_made>

<blockers>

- workflow-skills/current/*/SKILL.md 三个文件 pre-existing dirty,G2-T2 / G3 触前必先 `git diff` 确认 — 不要默认覆盖
- 本 session 后段 subagent 多次因 API drop / quota 失败;G3+G4+G5 plan 是 orchestrator 直写。新 session 中执行 G2-T2 / G3 / G4 / G5 时,subagent 风险仍在;若 subagent 反复断,fallback 为 orchestrator 直接编辑(已 proven 可行)
</blockers>

## Required Reading (in order)

1. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-B-copy-quality-rootcause.md` — 最深 root cause
2. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-C-context-budget.md` — G2 信号
3. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-A-personalization-angles.md` — 25 angles(规划材料,**不字面引用**)
4. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-D-delta-mechanism-redesign.md` — G3+G4 设计骨架
5. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-01-d8f-trial-unreachable.md` — 表层 finding
6. `seers_harness/workflow/CONTEXT_DISCLOSURE_ANALYSIS.md` — G2-T2 的实施合约 (signed off)
7. `.planning/phases/08-evolution-wiring-and-runner-debt/08-G1-SUMMARY.md` — G1 落地详情
8. `.planning/phases/08-evolution-wiring-and-runner-debt/08-G{2,3,4,5}-PLAN.md` — 待执行
9. `.planning/phases/06-evolution-chain-production-hardening/06-02-PLAN.md` + `06-02-SUMMARY.md` — orphan code 设计 (G4 不重写,只接通)

## Critical Anti-Patterns (do NOT repeat)

- ANTI-PATTERN: 重写 06-02 orphan 函数 → wired-through only,git diff 守门
- ANTI-PATTERN: blanket whitelist all user_state into copy_payload_for → §3 信息密度边界,目标 cache_miss [500, 5000]
- ANTI-PATTERN: G3 SKILL prose 引用用户口头举例 → grep gate(5 forbid 词必须 0 hits)
- ANTI-PATTERN: G4 trial signals 用 hardcoded 0.0 → ProductionSignalWindow / Semaphore 必须 wired
- ANTI-PATTERN: G5 没有 user spot-check 就标 passed → autonomous=false 守门

## Infrastructure State

- HEAD: `6650872 docs(08-G2): user sign-off context disclosure boundary`
- pytest baseline: 334 passed (post-G1)
- runner.py: 13 + G1 改动后状态;G2-T2 / G3 / G4 待修
- skill_loader.py:已 G1 落地,后续 plan 都通过它读 SKILL prose
- LIVE_SKILL_ROOT = workspace/workflow-skills/(已 lock 在 runner.py:183 + skill_loader.py)
- workflow-skills/current/*/SKILL.md:3 files dirty(pre-existing),G2-T2 / G3 触前必 `git diff`
- `.env.local` 含 DEEPSEEK_API_KEY suffix `****ab06`(G5 用)
- 上次 batch artifact:`tests/smoke/.runs/20260527T123110Z/`(gitignored,Stage 1 完整 + Stage 2 10/20)
- 上次 batch log:`.planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner-20260527T123110Z.log`(已 commit)

## Pre-Execution Critique Required

不需要。G2-T1 已 sign-off;G3 / G4 / G5 plan 已 commit `66ebf05`。直接执行。

<context>
本 session 的关键收获:
1. F-08-B(SKILL prose 没下发)是真正最深 root cause,不是 F-08-01(target_skill 路径)。
2. 06-02 已写好 bandit / sedimentation / status 的真函数,但 plan 08-05 写了一个绕过它们的 deterministic loop。G4 是接通,不是重写。
3. F-08-A 的 25 angles 是 *规划阶段* 判断"哪些维度有差异化潜力"的材料 —— SKILL prose 用项目术语统一表达,绝不字面引用 angle 名。
4. context 披露不是 whitelist,是 cache prefix 稳定性 + 信息密度的折中。ANALYSIS §3 已给出 stable 组 A + per-request 组 B 的双层方案。
5. Subagent reliability 在长 context 后变差(API drops 多)。新 session 一开始无负担,应该顺。

立即下一步: GSD recovery, not G2-T2. Read:
1. `08-VERIFICATION.md`
2. `08-G5-SUMMARY.md`
3. latest run log `.run-logs/runner-20260528T032645Z.log`

Then repair the G2-G4 commit-chain anomaly and Stage 3 gaps:
- malformed tool_call.arguments failure at `factor_discovery`
- copy_generation cache miss below [500, 5000]
- zero trial triggering / no portfolio journal
- WRIN closeout blocked until a passing Stage 3 rerun
</context>

<next_action>
Resume:
1. `/gsd-resume-work` 拉起 HANDOFF.json 上下文。
2. Read `08-VERIFICATION.md`, `08-G5-SUMMARY.md`, and `.run-logs/g5-verification-20260528T032645Z.txt`.
3. Repair GSD commit-chain anomaly and Stage 3 gaps before any pass/closeout claim.
4. Re-run Stage 3; only then perform G5 user spot-check.
</next_action>
