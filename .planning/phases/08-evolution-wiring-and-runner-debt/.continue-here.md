---
context: phase
phase: 08-evolution-wiring-and-runner-debt
task: 0
total_tasks: 6
status: in_progress (gap-closure)
last_updated: 2026-05-27T17:20:14.245Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure during this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: 真实 LLM 证据必须 — Phase 8 走完 Stage 1+2 之后,F-08-B 暴露:`pytest -q` 全绿、subagent 单测全绿,但真实 batch 显示三个 production node 拿到的 system message 是 10 字节字面字符串 `"SKILL_BODY"`,SKILL.md prose 从未下发。任何 phase-8 gap-closure 接受声明必须基于真实 DeepSeek Stage 3 (c=20) 跑出的 trace 证据,不能仅靠 unit test 通过。
- [ ] CONSTRAINT: 根因诊断 — 任何真实 LLM 失败必须从 logs / trace / runner state / provider response / state files 上溯定位再写代码。具体例:F-08-01 看似是 target_skill 路径 bug,实际深层是 plan 08-05 在设计 must_haves 时绕过了 06-02 已建好的 bandit selector(`select_trial_delta`),改成 deterministic loop —— surface fix 修了 target_skill 也不能修复机制错位。
- [ ] CONSTRAINT: 不能用启发式或静态值伪装真机制 — F-08-D Gap 3 的真信号源(rolling baseline window / Semaphore inflight counter)是必需的;不能用 hardcoded 0.0 让 trial_prob=1 装作"机制在跑"。每个 bandit 输入都必须能溯源到真实运行时观测。
- [ ] CONSTRAINT: SKILL prose 不写举例 — 用户在沟通时举的例子("低价大牌"/"风格质感"/"周三早 9 点"等)是用来传达意图的,不是写进 SKILL prose 的字面材料。重写 SKILL 时必须用统一通顺的项目词汇重新构筑表达,不能把用户举的例子直接做成 prose 的 anchor 模板。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Smoke-test closure | pytest 绿 / FakeProvider 绿 当成 phase 接受证据。F-08-B 隐藏在 unit test 里 —— `run_skill_via_tools` 接到的 `skill_bundle` 是个 string,test 传啥都不报错,真实 batch 才暴露。 | blocking | Phase-8 gap-closure 完成后必须真实 DeepSeek Stage 3 batch 跑通才能 mark complete;`08-VALIDATION.md` 每个 D8-X gate 都标 real-evidence 路径要求。 |
| Surface patching | 修 raise 那一行,不溯源到上游设计错位。F-08-01(target_skill 无格式)是 plan 08-05 设计 must_haves 时绕过 06-02 selector 的产物;`_patch_from_portfolio_row` 加 fuzzy 路径解析只是在错误的层修。 | blocking | 每个 gap-closure plan 必须 read_first 列指向上游 finding 文档(F-08-A..D),写 plan 时必须解释这个修法触及的是哪个架构 seam,不只是哪一行报错。 |
| 把用户举例写成 SKILL prose | 用户用例子传达意图,prose 不应字面引用。当前 generate-copy-candidates SKILL.md 的 `_REFLECT_DIVERSITY` 与 `product_anchor` 字段名都让 LLM 误读;新 prose 必须用项目统一词汇重新构筑。 | blocking | SKILL prose rewrite 后必须人工 review:确认 prose 不含用户口头举的字面例子('低价大牌'/'周三早9点'等),用项目术语(disposition / hook / floor / anchor type)统一表达。 |
| 用静态值伪装真机制 | `recent_failure_rate=0.0` / `token_budget_pressure=0.0` / `production_pressure=0.0` 三个常量让 `trial_prob=1`,看似 bandit 在跑,实际全部走 deterministic 分支。 | blocking | 每个 trial signal 输入都必须能 trace 到真实运行时观测点(rolling deque / Semaphore counter / journal),plan 描述时必须列出"信号源"列。 |
| 文档 / 实现 / 单测三方对齐缺失 | F-08-01 主因:SKILL prose / tool spec / patch helper 三处对 target_skill 格式说法不一致,LLM 自由发挥。 | advisory | 涉及多文件契约时,plan 必须在 must_haves 列出三方一致的 invariant。 |

<current_state>
Phase 8 已落地 1-7 个 plan(timeout 60→180s / --env-file / failure_class / runner cleanup / evolution wiring (broken) / token_cost / M1-M5)+ 8-08..8-11(backoff / drain / finally / narrow)+ pre-batch fix(request_preprocessor 模块级 import)。

2026-05-27 真实 DeepSeek batch (`tests/smoke/.runs/20260527T123110Z/`,commit `cbb6c5e`) 跑完 Stage 1 (1/1) + Stage 2 (10/20),由 STOP B 条件 SIGTERM(累计 10 个 stage 2 req,全 trial=0)。Stage 3 未启动。

四份 finding 已落 commit `224a91a`:
- F-08-A: 25 个个性化角度,verbatim user_state evidence;richest dimension 为 economic posture;意外发现 day_of_week=3 零方差(sampler bug)+ 4/11 男账号实为 proxy 父亲。
- F-08-B(最深 root cause):`seers_harness/workflow/dag_runner.py:84` 写死 `skill_bundle="SKILL_BODY"` 字面字符串。3 个 production node 的 system message[0].content 全部是 10 字节字符串 `"SKILL_BODY"`,SKILL.md prose 完全没下发到 LLM。
- F-08-C(次深):`seers_harness/workflow/payloads.py:86-112` `copy_payload_for(...)` 显式剥离 `user_state`,只塞 factor 列表。copy_generation node prompt_cache_miss=120 tokens;47 条 user_state 路径中 27 条永远到不了 copy 节点。SKILL line 41 "Hook words come only from factor.evidence_refs[].value" 在规则层焊死。
- F-08-D(机制重设计):orphan map 显示 06-02 写好的 `select_trial_delta` / `buffer_trajectory` / `sediment_trajectories` / `apply_status_transitions` 全是孤儿(只有 unit test,production 一行没调)。plan 08-05 写 deterministic loop 绕过它们。设计了 6 个 gap-closure plan 09-01..09-06 用 Wilson 95% LCB / journal-then-fold / paired control / ProductionSignalWindow,真机制非启发式。

四份 finding 揭示因果链:F-08-B → F-08-C → F-08-A 角度 → F-08-D 机制。修任一层不解决问题。

用户决定:不开 phase 9,在 phase 8 内 gap-closure 解决。修完后直接跑 Stage 3 (c=20) 一次性验证所有问题。subagent 推理强度 opus / xhigh。GSD 流程主导。
</current_state>

<completed_work>

Completed Tasks (Phase 8):
- 08-01: D8-A timeout 60→180s — Done (commit `2d8c6e6`/`779717a`)
- 08-02: D8-D `--env-file` flag — Done
- 08-03: D8-E failure_class column + by_failure_class aggregation — Done
- 08-04: WR-03/04 callsite + IN-08 cleanup — Done
- 08-05: D8-F evolution wiring (BROKEN — F-08-01 / F-08-D) — Done as written, but design绕过 06-02
- 08-06: IN-01 token_cost_observed from trace — Done
- 08-07: M1-M5 behavioral metrics — Done
- 08-08: D8-B transient backoff in tool_loop (RED→GREEN, 7 tests) — Done (commit `20305f6`/`e9b96c0`)
- 08-09: WR-01 Stage 3 fail-fast drain — Done (commit `a99e65f`/`344c290`)
- 08-10: WR-02 finally best-effort writers — Done (commit `c740574`)
- 08-11: WR-05 trial_runner exception narrowing — Done (commit `c858e09`)
- pre-batch-fix: request_preprocessor module-level hoist — Done (commit `cbb6c5e`)
- real-LLM batch on `cbb6c5e` — Done (Stage 1 PASSED + Stage 2 10/20 + SIGTERM at STOP B)
- F-08-A..D investigations + commits — Done (commit `224a91a`)
</completed_work>

<remaining_work>

- 08-G1: SKILL loader unification — Not started
  - 单一 harness primitive(读取 + cache + 错误处理),"node→skill" 绑定作为扩展类/hook 落在 primitive 之上
  - 修复 `dag_runner.py:84` 字面 `"SKILL_BODY"` bug
  - 同时让 `_distill_after_stage1` 改用同一 primitive(目前 `runner.py:491-493` 是另一处独立 read_text — 不能存在两个 loader)

- 08-G2: Context disclosure boundary — Not started
  - 先研究信息论边界:cache hit rate 与 user_state 字段披露范围的关系。F-08-C 观察到 cache_miss=120 tokens 是过窄,blanket-whitelist 又会让 cache miss 暴涨(F-08-03 已示)。
  - 修 `copy_payload_for` / `rubric_payload_for`,做"必要 + 不破坏 cache prefix 稳定性"的折中
  - 同时改 generate-copy-candidates SKILL line 41 那条规则(rules 层焊死必须松开)

- 08-G3: Distill 改造 — Not started
  - dual-track success/failure pattern mining(distill 的输入要分别标 success / failure trajectory bucket)
  - target_skill 三处格式契约:SKILL prose + tool spec(`RECORD_DELTA_CHANGE_SPEC.target_skill` 加 description+pattern)+ `_patch_from_portfolio_row` runtime gate
  - distill RecordingProvider 的 request_log 必须落盘(F-08-D Gap 6)— 当前 zero on-disk audit

- 08-G4: 替换 deterministic loop — Not started
  - 把 `_run_one_request` line ~634 的 `for index, portfolio_row` 整段循环替换为:
    `select_trial_delta` (06-02 已写) + paired-control (新) + journal-then-fold (新) + uplift evaluator (新)
  - 信号源:ProductionSignalWindow (rolling deque) + Semaphore inflight counter
  - F-08-D 09-01..09-05 的实质内容,在 phase-8 内合并落地

- 08-G5: 真实 DeepSeek Stage 3 batch — Not started
  - 修复全部落地、`pytest -q` 全绿后,直接跑 Stage 3 (n=20, c=20) 一次性验证 F-08-B/C/D 与原 D8-ACC-1..6
  - 接受门:`stage 3 PASSED` + 至少 1 个 delta 完成 status transition + per-node `messages.jsonl[0].content` 长度 > 100(SKILL prose 真下发)+ copy_generation prompt_cache_miss > 500 但 < 5000(信息密度合理区间)+ 文案质量人工 spot-check 不再"商品名+几字模板"

- 08-13-closeout: 07-WRIN-TRIAGE.md + 08-VERIFICATION.md — Not started
</remaining_work>

<decisions_made>

- 不开 phase 9 — gap-closure 留在 phase 8 内
- SKILL loader 是单一 harness primitive,"node→skill" 是扩展类/hook —— 不是 dag_runner 写死字面字符串
- Context 披露是信息论问题 —— 先研究 cache-hit 与信息密度边界,不做 blanket whitelist
- SKILL prose rewrite 不引入用户口头举例 —— 用项目统一词汇重写
- 修复后直接跑 Stage 3(c=20 接近生产),不重跑 Stage 1/2
- subagent 派遣使用 opus / xhigh 推理强度(`model="claude-opus-4-7"` 传给 Agent())
- GSD 流程主导:`/gsd-plan-phase 08 --gaps` 读 finding 生成 gap-closure plans → `/gsd-execute-phase 08 --gaps-only` 执行 → 真实 batch 验证 → `08-VERIFICATION.md`
- 不停 — 主 agent 编排,直到 Stage 3 跑完
</decisions_made>

<blockers>

- F-08-B 阻塞 F-08-C/D 落地的真实信号:LLM 在裸奔时,distill 拿到的轨迹本身是垃圾,bandit 信号也是垃圾。修复顺序必须 G1 → G2 → G3/G4 并行 → G5。
- workflow-skills/current/*/SKILL.md 三个文件是 pre-existing dirty(本会话之前就改过),08-G1/G2/G3 涉及 SKILL prose 重写时需要先确认这些 dirty 改动该保留还是丢弃 — 不要默认覆盖。
</blockers>

## Required Reading (in order)

1. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-B-copy-quality-rootcause.md` — 最深 root cause + 7 条原子修复
2. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-C-context-budget.md` — payload diff 表 + 信息密度分析 + concrete proposals
3. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-D-delta-mechanism-redesign.md` — 6 个 gap-closure plan(09-01..09-06)的真机制设计,phase-8 gap-closure 直接借鉴
4. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-A-personalization-angles.md` — 25 个个性化角度(下游 SKILL prose rewrite 用作 angle 词汇空间,但不字面引用)
5. `.planning/phases/08-evolution-wiring-and-runner-debt/findings/F-08-01-d8f-trial-unreachable.md` — 表层 finding,与 F-08-D 配合阅读
6. `.planning/phases/06-evolution-chain-production-hardening/06-02-PLAN.md` 与 `06-02-SUMMARY.md` — 06-02 已写好的 bandit selector / sedimentation 真实接口,phase-8 gap-closure 不要重写
7. `seers_harness/workflow/dag_runner.py:84` — F-08-B 的 ground zero
8. `seers_harness/workflow/payloads.py:86-112` — F-08-C 的 ground zero
9. `seers_harness/validation/runner.py:_run_one_request:634` — F-08-D 的 ground zero
10. `seers_harness/evolution/delta_portfolio.py` — `select_trial_delta` 等 orphan 函数定义

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: 把 SKILL_BODY 字面字符串当成 prose loader 的占位符 → 必须有 read_skill_prose(skill_name) primitive,在多处复用,不存在两个 loader 实现
- ANTI-PATTERN: copy_payload_for blanket-strip user_state → 必须基于 cache prefix 稳定性 + 信息密度的折中,先研究再实现
- ANTI-PATTERN: deterministic for-each-row trial loop 绕过 bandit selector → 必须调 `select_trial_delta`,信号源真实,不用静态 0
- ANTI-PATTERN: 把用户举例(低价大牌 / 周三早 9 点 / 代理父亲 等)字面写进 SKILL prose → SKILL 用项目统一词汇,例子留在 finding 里供 review
- ANTI-PATTERN: 单测过即标 done → 真实 Stage 3 batch trace 才是 phase-8 acceptance gate

## Infrastructure State

- HEAD: `224a91a docs(08): findings F-08-A..D from real-LLM batch + runner log artifacts`
- pytest baseline: 321 passed (after `cbb6c5e` request_preprocessor fix)
- runner.py: 13 个 plan 落地后状态;`_run_one_request` line ~634 trial loop 待替换
- dag_runner.py:84: 字面 `"SKILL_BODY"`,待修
- payloads.py: copy_payload_for 待研究 + 修
- workflow-skills/current/*/SKILL.md: 3 文件 pre-existing dirty,需 user 确认处理
- 真实 LLM batch 输出在 `tests/smoke/.runs/20260527T123110Z/` (gitignored;Stage 1 完整 + Stage 2 10/20 + Stage 2 第 11 req partial + Stage 3 未跑)
- run-logs 在 `.planning/phases/08-evolution-wiring-and-runner-debt/.run-logs/runner-20260527T123110Z.log`(已 commit)
- `.env.local` 含有效 DEEPSEEK_API_KEY(suffix `****ab06`)

## Pre-Execution Critique Required

- 设计阶段(SKILL loader primitive 形状 + context disclosure 信息密度边界)需要在写代码前完成,且产出明确 design 文档(可以是 plan must_haves 中的 truths)
- 关键问题:loader primitive 的 cache 策略(每次 read_text 还是 module-level cache)+ context disclosure 字段白名单的研究方法学

<context>
本次 pause-work 的状态:phase 8 在执行中暴露 4 个深层 root cause(F-08-A/B/C/D),分别属于"production node 裸奔"、"copy 节点 context 缺失"、"原始数据 personalization 空间未挖"、"delta 机制 deterministic loop 绕过 bandit"四个独立问题。它们叠加产生 F-08-01 表层症状(trial 永不触发)。

之前我以为修 F-08-01 一处即可,investigation 后认识到必须分 4 层修。用户决定不开 phase 9,在 phase 8 内 gap-closure。下一步 GSD 流程:`/gsd-plan-phase 08 --gaps` 读 finding 生成 gap-closure plans;`/gsd-execute-phase 08 --gaps-only` 用 opus subagent 执行;真实 Stage 3 batch 接受。

修复顺序的硬约束:G1 (SKILL loader) 在 G2/G3/G4 之前。G2 (context) 在 G3 (distill) 之前(distill 的轨迹 payload 来自 production node 输出,production 信息密度修了 distill 才有信号挖)。G3+G4 可并行(distill agent 与 trial 机制是独立面)。G5 (Stage 3) 在 G1/2/3/4 全部落地后。

主 agent 编排不停顿,直到 Stage 3 跑完。
</context>

<next_action>
Resume 从 `/gsd-plan-phase 08 --gaps` 开始。规划器读 4 份 finding,生成 5 个 gap-closure plan(对应 G1..G5),写入 `.planning/phases/08-evolution-wiring-and-runner-debt/`。然后 `/gsd-execute-phase 08 --gaps-only`,subagent 推理强度 opus。最后 launch 真实 DeepSeek Stage 3 batch:`python -m seers_harness.validation.runner --env-file .env.local --stage 3`(注意 `--stage 3` 跳过 1/2,Stage 3 一次性验证)。

主 agent 不停顿直到 Stage 3 PASSED + 文案质量人工 spot-check 通过 + at least 1 delta 状态转移。
</next_action>
