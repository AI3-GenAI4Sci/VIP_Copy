---
phase: 05-cleanup-deletes-tests-regression
plan_id: 05-01
wave: 1
depends_on: []
files_modified:
  - seers_harness/domain/models.py
  - seers_harness/workflow/dag_runner.py
  - seers_harness/workflow/payloads.py
  - seers_harness/provider_runtime/base.py
  - seers_harness/tools/skill_tools.py
  - tests/test_provider_result_shape.py
  - tests/test_provider_line_budget.py
  - tests/test_dag_runner_integration.py
  - tests/test_payloads_loop06_audit.py
  - tests/test_models_bridge.py
  - tests/test_models_factor.py
  - tests/test_models_rubric.py
autonomous: true
requirements: [CLEAN-01, CLEAN-04]
---

# Plan 05-01: c14/c15/c16 标记清理

## Objective

把 `workspace/seers_harness/` 与 `workspace/tests/` 下所有 c14/c15/c16 历史
溯源标签从注释、docstring、测试函数名中清除。断言体保留——按当前 schema
形态命名，不是按历史血统命名。

## must_haves

- truths:
  - `rg 'c14|c15|c16|verbatim from c' seers_harness/ tests/` 命中 0 行（除
    `__pycache__/` 等 build 产物外）
  - 122/122 test baseline 保持绿（无新增、无删除测试用例，仅改名 + 改
    docstring + 改注释）
  - 所有改名后的测试函数仍通过
- forbid:
  - 新增 lint 门 / grep CI / 工程级禁词表（修订后的 D-04）
  - 删除任何断言体
  - 改动 fixture 数据（fixture 里的 `维生素 / 防晒霜` 等真实类目数据保留）
  - 改动 `intake/categories.py` 的 `TARGET_CATEGORIES`

## Tasks

<task id="01-01">
  <action>
    Rewrite the file-level docstring in `seers_harness/domain/models.py` (lines
    1-8). Current docstring claims `model_config = {"extra": "ignore"}` is for
    "legacy c14/c15 JSON on disk decodes silently". The `extra="ignore"` claim
    is about to be flipped to `forbid` in plan 05-04, so rewrite the docstring
    around the current schema purpose only — drop the historical lineage
    sentence entirely. Keep the RESEARCH Open Q2 reference about pydantic
    `model_json_schema()` declaration order (that is current schema fact, not
    historical).
  </action>
  <read_first>
    - seers_harness/domain/models.py (lines 1-15 — current docstring)
    - .planning/phases/05-cleanup-deletes-tests-regression/05-RESEARCH.md §2 (类 A 清单), §4 (forbid 翻转上下文)
    - .planning/intel/decisions.md ADR-01-PRINCIPLE-08 (no c14/c15 baggage)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' seers_harness/domain/models.py` 命中 0 行
    - File-level docstring 不再提 "legacy" / "JSON on disk" / "decodes silently"
    - Docstring 仍然提及 declaration order 对 critique-before-verdict tools 的影响
  </acceptance_criteria>
</task>

<task id="01-02">
  <action>
    In `seers_harness/workflow/dag_runner.py`, rewrite the file-level docstring
    (lines 1-10) and the `NodeSpec` docstring (lines 33-39). Drop both c16
    references: "The c16 polling/closure/re-injection machinery is gone" →
    rewrite as a positive statement about what `_run_node` does
    (single-call-to-tool-loop + Pydantic validation); "The c16 NodeSpec carries
    provider/temperature/skill_registry routing" → rewrite as a positive
    statement of the c17 NodeSpec contract (id, skill_name, output_model,
    max_attempts).
  </action>
  <read_first>
    - seers_harness/workflow/dag_runner.py (lines 1-43)
    - .planning/phases/05-cleanup-deletes-tests-regression/05-RESEARCH.md §2
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' seers_harness/workflow/dag_runner.py` 命中 0 行
    - File-level docstring 仍然引用 RESEARCH §4 / §8（c17 研究文档），引用方式按
      "RESEARCH §X" 形式而非 "the c16 X"
    - `NodeSpec` 的 4 个字段（id, skill_name, output_model, max_attempts）
      在 docstring 里以正向定义出现
  </acceptance_criteria>
</task>

<task id="01-03">
  <action>
    In `seers_harness/workflow/payloads.py`, rewrite the file-level docstring
    line 12 ("The c16 legacy fan-out quota fields are deliberately ABSENT from
    this module") to a positive statement: the `candidate_generation_policy`
    dict has exactly two keys (`unit`, `score_all_candidates_together_after_hard_rules`),
    and any other quota field outside this dict is structurally rejected by
    `tests/test_payloads_loop06_audit.py`. The audit test still grep-checks
    legacy field absence — that is current schema enforcement, not historical
    framing.
  </action>
  <read_first>
    - seers_harness/workflow/payloads.py (lines 1-30)
    - tests/test_payloads_loop06_audit.py (full — confirms the audit test name will be updated in task 01-09)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' seers_harness/workflow/payloads.py` 命中 0 行
    - Docstring 仍然提到 master_plan §4.5 锁定 `candidate_generation_policy` 两个 key
    - `candidate_generation_policy` 字典本身、`_scenario_dict`、其他函数体不变
  </acceptance_criteria>
</task>

<task id="01-04">
  <action>
    In `seers_harness/provider_runtime/base.py`, rewrite three locations:
    (a) line 5 "PROV-01 deletes the c16 parallel JSON method at the Protocol
    level" → "PROV-01: only `generate_with_tools` is exposed; no JSON
    fallback method exists on this Protocol";
    (b) lines 53-56 (the `Field order rationale` comment block above
    `ProviderResult`) — the "c16 ProviderResult signature" / "c16 ordering
    verbatim" framing is purely historical, drop the whole rationale block
    and replace with a one-line comment describing the current field-order
    invariant (`payload` first for positional construction back-compat is
    fine to keep, but state it as "positional construction in Phase 2 tests"
    not "c16 ProviderResult signature").
  </action>
  <read_first>
    - seers_harness/provider_runtime/base.py (full)
    - tests/test_provider_result_shape.py (uses positional `ProviderResult(payload=...)` — confirm field order claim)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' seers_harness/provider_runtime/base.py` 命中 0 行
    - File-level docstring 仍然引用 PROV-01..PROV-05 + ADR-PROBE-7.1 + RESEARCH `research/probe_reasoning_with_tools_result.md`
    - `ProviderResult` 字段定义和顺序未改动
    - Field order rationale 仍然提到 positional construction 不破坏现有测试
  </acceptance_criteria>
</task>

<task id="01-05">
  <action>
    In `seers_harness/tools/skill_tools.py`, delete the line 38 banner
    "Verbatim helpers from c16 check_tools.py" (the dashed `# ----` separator
    block at lines 37-39). Replace with a single comment line describing what
    the section contains in current terms — these are pure-function helpers
    for evidence-path resolution, Chinese-digit detection, ad-copy character
    counting, used by `record_factor` / `record_candidate` / `judge_candidate`.
  </action>
  <read_first>
    - seers_harness/tools/skill_tools.py (lines 1-100)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' seers_harness/tools/skill_tools.py` 命中 0 行
    - 行 38 不再含 "Verbatim helpers from c16"
    - 行 38 替换为一行陈述当前用途的注释
    - 函数体（`_resolve_path`, `_visible_chinese_chars`, 等）零改动
  </acceptance_criteria>
</task>

<task id="01-06">
  <action>
    In `tests/test_provider_result_shape.py`, rewrite the docstring at line 86
    ("PROV-01: the c16 parallel JSON path is DELETED at the contract level")
    to drop the c16 reference. The assertion body verifies that the Provider
    Protocol exposes only `generate_with_tools` — restate the docstring around
    that current-schema fact. Do not rename the test function unless the
    function name itself contains "c16" (it likely does not).
  </action>
  <read_first>
    - tests/test_provider_result_shape.py (lines 80-110, find the function containing line 86)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_provider_result_shape.py` 命中 0 行
    - 受影响测试运行通过：`uv run --python 3.12 --extra dev python -m pytest tests/test_provider_result_shape.py -q`
  </acceptance_criteria>
</task>

<task id="01-07">
  <action>
    In `tests/test_provider_line_budget.py`, rewrite the docstring at line 41
    ("PROV-01 — the c16 dual entry point is deleted") to drop the c16
    reference. Restate around the current-schema fact that the Protocol
    exposes only `generate_with_tools`.
  </action>
  <read_first>
    - tests/test_provider_line_budget.py (lines 35-60)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_provider_line_budget.py` 命中 0 行
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_provider_line_budget.py -q` 通过
  </acceptance_criteria>
</task>

<task id="01-08">
  <action>
    In `tests/test_dag_runner_integration.py`, rewrite three docstring/comment
    sites (lines 6, 108, 128) that frame the renamed trace event as "the c16
    `agent_loop_summary`". The c17 trace emits `tool_loop_summary`; restate
    each docstring around the positive c17 contract. The string literal
    `"agent_loop_summary"` itself appears as the value being asserted absent
    in `types` (line 128) — keep the string literal in the assertion, but
    rephrase the surrounding comment so it reads as "the legacy event name is
    rejected by this assertion" rather than "c16 event name".
  </action>
  <read_first>
    - tests/test_dag_runner_integration.py (lines 1-30, 100-135)
  </read_first>
  <acceptance_criteria>
    - `rg '\\bc14\\b|\\bc15\\b|\\bc16\\b' tests/test_dag_runner_integration.py` 命中 0 行（注：`agent_loop_summary` 字面量保留为断言对象，不命中上述 grep）
    - 断言行 `assert 'agent_loop_summary' not in types` 仍然存在并通过
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_dag_runner_integration.py -q` 通过
  </acceptance_criteria>
</task>

<task id="01-09">
  <action>
    In `tests/test_payloads_loop06_audit.py`, rewrite three docstring/comment
    sites (lines 9, 32, 40) that frame the audit as "c16 legacy quota fields
    absent". Rename to current-schema framing — these tests assert
    `master_plan §4.5 invariant 1`: only the two locked keys
    (`unit`, `score_all_candidates_together_after_hard_rules`) live in
    `candidate_generation_policy`, and the file declares no other quota field.
    The error message in the assertion (line 40) currently says "LOOP-06
    violation — legacy c16 quota fields found"; rewrite to "LOOP-06 violation
    — non-policy quota field found" or similar that does not reference c16.
  </action>
  <read_first>
    - tests/test_payloads_loop06_audit.py (full file — small)
    - seers_harness/workflow/payloads.py (lines 24-30 — current `candidate_generation_policy` definition)
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_payloads_loop06_audit.py` 命中 0 行
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_payloads_loop06_audit.py -q` 通过
    - Assertion error message 不再包含 "c16"
  </acceptance_criteria>
</task>

<task id="01-10">
  <action>
    In `tests/test_models_bridge.py`, rename two test functions and rewrite
    the file-level docstring (lines 1-5):
    - `test_c16_pair_present` → `test_anchor_pair_present`
    - `test_c15_slots_absent` → `test_legacy_bridge_slots_absent`
    - File-level docstring "c17 BridgeLogic carries only the c16 anchor
      pair; the c15 slots..." → rewrite around the current-schema fact:
      `BridgeLogic` declares `product_anchor` and `relation_anchor`; this
      module asserts those two fields exist and that no other plain-text
      slot reappears.
    - Inline docstring at line 22 ("DATA-03 negative — c15 BridgeLogic slots
      removed (Q7 RESOLVED, Principle 8)") → rewrite drop "c15", keep
      DATA-03 / Principle 8 references.
    - Assertion error string at line 25 "BridgeLogic still declares c15
      slots: {overlap}" → rewrite drop "c15".
    Assertion bodies (the field-name list being checked, the schema being
    queried) are NOT changed.
  </action>
  <read_first>
    - tests/test_models_bridge.py (full)
    - seers_harness/domain/models.py (lines 51-55 — `BridgeLogic` definition)
    - .planning/REQUIREMENTS.md DATA-03
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_models_bridge.py` 命中 0 行
    - `rg 'def test_c1[456]_' tests/test_models_bridge.py` 命中 0 行
    - 两个新函数名 `test_anchor_pair_present`, `test_legacy_bridge_slots_absent` 存在
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_models_bridge.py -q` 通过且测试数与改前相同
  </acceptance_criteria>
</task>

<task id="01-11">
  <action>
    In `tests/test_models_factor.py`, rename one test function and rewrite
    the related docstring/error message:
    - `test_c15_legacy_fields_absent` → `test_legacy_plain_text_fields_absent`
    - Inline docstring at line 45 ("DATA-02 / Principle 8 — clean delete
      of c15 legacy plain-text fields") → drop "c15".
    - Assertion error string at line 59 "PersonalizationFactor still declares
      c15 legacy fields: {overlap}" → rewrite drop "c15".
    Assertion bodies unchanged.
  </action>
  <read_first>
    - tests/test_models_factor.py (full)
    - seers_harness/domain/models.py (lines 26-49 — `PersonalizationFactor`)
    - .planning/REQUIREMENTS.md DATA-02
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_models_factor.py` 命中 0 行
    - 新函数名 `test_legacy_plain_text_fields_absent` 存在
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_models_factor.py -q` 通过且测试数不变
  </acceptance_criteria>
</task>

<task id="01-12">
  <action>
    In `tests/test_models_rubric.py`, rename one test function and rewrite
    related docstring/error string:
    - The test function whose docstring (line 40) says "DATA-05 — c15 legacy
      aggregate fields removed (PATTERNS line 169)" — rename function from
      `test_c15_legacy_aggregate_fields_absent` (or whatever its current
      `test_c15_*` name is) to `test_legacy_aggregate_fields_absent`.
    - Docstring drop "c15", keep DATA-05 + PATTERNS line 169 references.
    - Assertion error string at line 50 "PersonalizedCopyRubricJudgment
      still declares c15 fields: {overlap}" → drop "c15".
    Assertion bodies unchanged.
  </action>
  <read_first>
    - tests/test_models_rubric.py (full)
    - seers_harness/domain/models.py (lines 97-110 — `PersonalizedCopyRubricJudgment`)
    - .planning/REQUIREMENTS.md DATA-05
  </read_first>
  <acceptance_criteria>
    - `rg 'c14|c15|c16' tests/test_models_rubric.py` 命中 0 行
    - 新函数名 `test_legacy_aggregate_fields_absent` 存在
    - `uv run --python 3.12 --extra dev python -m pytest tests/test_models_rubric.py -q` 通过且测试数不变
  </acceptance_criteria>
</task>

<task id="01-13">
  <action>
    Run the full regression gate from `workspace/`:
    `uv run --python 3.12 --extra dev python -m pytest -q`
    Assert 122/122 (or whatever count tasks 01-10..01-12 produced after
    renames — count must equal 122 because no test was added or removed).
    Then run the cleanup grep gate:
    `rg 'c14|c15|c16|verbatim from c' seers_harness/ tests/ --type py`
    Assert 0 hits. The `agent_loop_summary` literal in
    `tests/test_dag_runner_integration.py` does not contain `c14/c15/c16`
    so it does not match the grep.
    If either step fails, do NOT mark plan complete.
  </action>
  <read_first>
    - .planning/phases/05-cleanup-deletes-tests-regression/05-RESEARCH.md §2 §7
  </read_first>
  <acceptance_criteria>
    - `uv run --python 3.12 --extra dev python -m pytest -q` 输出包含 `122 passed`
    - `rg 'c14|c15|c16|verbatim from c' seers_harness/ tests/ --type py` 退出码 = 1（无命中）
  </acceptance_criteria>
</task>

## Verification

Wave 1 ready when 01-13 通过。下一 wave 的 `extra="forbid"` 翻转依赖此处
122-test baseline 的稳定。

## Skills/Methods

- `dispatching-parallel-agents`：tasks 01-01..01-12 互相独立（不同文件 / 不同
  函数体），可并行 dispatch。
- `verification-before-completion`：task 01-13 是终态门，先跑测试再断 grep。
