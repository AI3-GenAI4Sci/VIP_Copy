# Continue Here — Phase 7 → Phase 8 Handoff

> **Pause-work checkpoint:** 2026-05-27T03:55Z

Phase 7 was reopened by the user (PARTIAL acceptance retracted) and is now **BLOCKED on phase 8**. The 2026-05-26 real-LLM batch surfaced three operational root causes that need a runner-touch sweep to fix. Phase 8 batches those fixes with the runner ↔ evolution wiring and the 7 phase-7 WR/IN runner-debt items.

## Resume in 3 steps

1. **Open the phase-8 charter.** Three groups, runs as one sweep:
   ```bash
   $EDITOR .planning/phases/08-evolution-wiring-and-runner-debt/08-CHARTER.md
   ```
   - Group 1 (A-E): phase-7 real-LLM hardening (timeout 60→180s, transient retry, CR-05 audit, --env-file, failure_class column)
   - Group 2 (F): runner ↔ evolution wiring (PRIMARY phase-8 deliverable)
   - Group 3 (G): 7 WR/IN runner-debt items from `07-WRIN-TRIAGE.md`

2. **Drive phase 8 planning/execution.** Suggested next command:
   ```
   /gsd-plan-phase 8
   ```
   If a plan already exists: `/gsd-execute-phase 8`.

3. **After phase 8 lands and pytest is green:**
   ```bash
   python -m seers_harness.validation.runner --env-file .env.local
   ```
   This is the cleanest invocation — Group 1 item D adds the `--env-file` flag so the runner reads `.env.local` itself (no more shell ENV indirection that bit us on 2026-05-26 in RC-03).

## Phase-7 acceptance bar (still open)

Until ALL five hold, phase 7 stays at `gaps_found`:

- [ ] Stage 1 + Stage 2 (20/20) + Stage 3 (20/20) clean on phase-8 commit
- [ ] case_analysis.md F1..F4 user-judged excellent (use `case_analysis_template.md` for trajectory-level reading)
- [ ] evolution_snapshot.json carries at least one non-empty `trials[]`
- [ ] Stage 3 actually exercises c=20 on real DeepSeek
- [ ] 07-WRIN-TRIAGE.md 7 scheduled items → phase-8 commit refs

## What's already done

- 11 fix-now commits landed (CR-01..05 + 7 WR/IN). pytest 253/253 throughout.
- 07-WRIN-TRIAGE.md closed (7 fix-now, 7 scheduled, 0 waived, 0 TBD).
- 08-CHARTER.md fully expanded with Groups A-G.
- STATE.md, 07-VERIFICATION.md, 07-UAT.md, case_analysis_template.md all current.

## What was preserved (do NOT touch)

- `seers_harness/validation/runner.py` — left at `aa49f06` baseline so phase 8 lands all runner-touch changes in a single coherent sweep.
- `workflow-skills/current/*/SKILL.md` — pre-existing edits, not part of this session.
- `.run-logs/`, `.claude/`, `.planning/config.json` — ephemeral / local.

## Key root-cause references

- **RC-01 (timeout):** `tests/smoke/.runs/20260526T183142Z/runner-20260526T183141Z.log` — phase-8 fixes via Group 1 item A.
- **RC-02 (truncation):** `tests/smoke/.runs/20260526T183142Z/runner-20260526T174639Z.log` — CR-05 (`fc25187`) already addresses; phase-8 item C is an AUDIT only.
- **RC-03 (stale env):** `tests/smoke/.runs/20260526T174546Z/runner-*.log` — phase-8 fixes via Group 1 item D.

**Confirmed NOT a root cause:** max_tokens / per-request budget. `grep -rn "max_tokens"` returns 0 hits in `seers_harness/`; max observed `completion_tokens` in the failing batch was 7019 (well under any cap). D-06 is honored.

## Full handoff JSON

`.planning/handoffs/2026-05-27T03-55Z-phase7-to-phase8.json`
